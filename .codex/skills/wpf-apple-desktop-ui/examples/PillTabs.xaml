<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Style x:Key="WpfApple.PillTabItem" TargetType="{x:Type TabItem}">
    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
    <Setter Property="VerticalContentAlignment" Value="Stretch"/>
    <Setter Property="Foreground" Value="{DynamicResource WpfApple.SecondaryTextBrush}"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type TabItem}">
          <Grid Margin="0,0,8,0">
            <Border x:Name="Pill" Margin="1" Padding="14,8"
                    Background="{DynamicResource WpfApple.SecondarySurfaceBrush}"
                    BorderBrush="{DynamicResource WpfApple.DividerBrush}"
                    BorderThickness="1" CornerRadius="12">
              <ContentPresenter ContentSource="Header" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
          </Grid>
          <ControlTemplate.Triggers>
            <Trigger Property="IsSelected" Value="True">
              <Setter TargetName="Pill" Property="Background" Value="{DynamicResource WpfApple.AccentSoftBrush}"/>
              <Setter TargetName="Pill" Property="BorderBrush" Value="{DynamicResource WpfApple.AccentBrush}"/>
              <Setter Property="Foreground" Value="{DynamicResource WpfApple.AccentBrush}"/>
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
</ResourceDictionary>
