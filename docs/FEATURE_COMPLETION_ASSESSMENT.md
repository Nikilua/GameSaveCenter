# 功能完成度评估

更新时间：2026-09-03
评估版本：`0.6.70-development-preview`

## 2026-09-03 当前增补

- 设备冲突决策和媒体类型筛选已移除会抢写绑定值的静态首项，改为由 ViewModel 双向状态唯一驱动；远端备份隔离下载结果现在在设备详情中持续可见，并补齐相关控件的可访问名称。
- 任务中心已补充“重试可恢复”批量操作；按游戏与任务类型合并历史失败项，只重试最新项，单项失败不阻塞其他任务，完成后统一汇总并刷新列表。
- Playnite 游戏右键菜单已补充“同步媒体”，支持单选/多选游戏，沿用现有媒体同步 IPC、媒体开关和云端上传设置；不需要打开 Dashboard 才能从游戏库发起媒体归档。
- 游戏级云端状态已增强为存档上传与已归类媒体上传的合并状态；Dashboard、游戏选择器和详情页共享同一 SQLite 汇总结果，媒体失败/排队不会再被存档“已上传”状态掩盖。
- 新增 Worker 持久化回归；本机自动化基线为 Release 0 warning/0 error、Core `65/65`、Worker `234/234`、Playnite `325/382`（57 跳过）和源码/WPF 静态门禁通过。真实 Rclone 远端、媒体长传输、Playnite 主题/DPI 与恢复流程仍不计入自动完成度。

## 0.6.70 当前证据（2026-08-06）

- 修改器中心工具设置检查器不再在宽屏使用固定 280 DIP 高度；宽屏填充三栏工作区，窄窗口使用受控内部滚动，下载任务详情也使用 Demo 阅读卡。
- 已补充静态回归门禁；源码校验、XAML 门禁、Release 测试和构建应在本轮完成，真实 Playnite 宿主渲染仍未验证。

## 0.6.69 当前证据（2026-08-06）

- Overview 首页加入 Demo 的独立“今日工作台”动作卡，真实命令未改变；宽度低于 720 DIP 时按钮自动纵向排列。
- 通过 XAML 结构检查、源码校验和 Playnite 测试；尚未在 Playnite 宿主中完成像素级渲染验证。

## 0.6.68 当前证据（2026-08-05）

- 一键打包不再信任旧构建输出中的 `extension.yaml`，始终复制源码清单；安装脚本对暂存目录增加存在性保护，避免 0.6.64/0.6.65 这类版本错配进入安装流程。
- 已通过源码校验、Release 测试、构建和包内容版本检查；真实 Playnite 安装仍需用户环境验证。

## 0.6.67 当前证据（2026-08-05）

- 六个生产工作区共用的普通阅读卡已匹配 Demo 的 16/18/无阴影层级；专用表格、浮层和状态资源仍覆盖各自需要的边界。
- 共享资源和自动测试覆盖通过；真实 Playnite 像素级渲染仍需宿主验证。

## 0.6.66 当前证据（2026-08-05）

- Dashboard 已恢复 Demo 的顶部/内容/底部三段层级；底部说明使用省略和短窗口降级，不读取或改变业务状态。
- 自动门禁和 Release 构建覆盖了新 footer 节点；Playnite 实际宿主渲染仍未在当前环境验证。

## 0.6.65 当前证据（2026-08-05）

- 工作区宿主已经改为透明 PageHost，顶部表面与 Demo 层级分离；保留所有现有工作区绑定、命令、虚拟化和内部滚动。
- 源码校验、Release 自动测试、构建和 Playnite 包生成通过；真实 Playnite 宿主渲染、DPI、主题和 900+ 游戏库生命周期仍待用户环境验证。

## 0.6.60 当前证据（2026-08-05）

- 已移除生产 Dashboard/设置页的 WPF-UI SnackbarPresenter，并为 WPF-UI token 提供确定性本地 fallback，降低 Playnite 宿主 Arrange 阶段的 UnsetValue 崩溃风险。
- 源码校验、149 项自动测试和 Release 构建通过；仍未在真实 Playnite 宿主中完成崩溃修复、DPI 和主题渲染验证。

- 共享 demo 卡片别名已统一几何与阴影层级，避免不同工作区因为继承链不同而出现卡片厚重、间距不一致。
- 源码校验、149 项自动测试和 Release 构建通过；仍未在真实 Playnite 宿主中完成视觉、DPI 和主题渲染验证。

## 0.6.59 当前证据（2026-08-05）

- 修复 `CornerRadius=UnsetValue` 导致 Playnite 布局线程崩溃的共享模板风险；源码校验、Core 13、Worker 23、Playnite 112，共 148 项自动测试和 Release 构建通过。
- 当前仍未在此环境真实运行 Playnite，因此安装后的宿主渲染、DPI、主题和大库生命周期需要用户环境验证。

## 0.6.57 UI-064 当前证据（2026-08-05）

- 六个提取工作区已移除根级页面滚动；维护中心设备表格和异常审计表格改为 Grid 星号行，表格继续使用虚拟化与内部滚动。
- `python scripts/validate-source.py` 通过；Release 构建 0 警告/0 错误；Core 13、Worker 23、Playnite 107，共 143 项自动测试通过。
- Demo 级完整迁移和真实 Playnite/900+ 游戏库渲染、DPI、主题、键盘及生命周期回归仍未在当前环境完成。

## 0.6.49 当前证据（2026-08-05）

- 新增 Worker 版本握手，避免 0.6.22 等旧版进程通过稳定命名管道被误复用；真实 Playnite 启动、900+ 游戏库和独立 LudusaviPlaynite A/B 仍需用户环境验证。

## 0.6.48 当前证据（2026-08-05）

- 共享和 Dashboard 兼容 DataGrid 行模板已恢复 WPF 选择性滚动结构；源码校验、131 项自动测试、Release 构建和 0.6.48 打包已通过。
- 超大库规模观察值已改为单调保持，避免 Playnite 导入/关闭期间的瞬时空快照降低 Worker 恢复安全级别。
- 仍需在真实 Playnite 中验证宿主模板、横向滚动、行详情、主题、DPI 和 900+ 游戏库启动行为；当前环境不能宣称这些宿主证据已完成。

## 0.6.46 当前证据（2026-08-05）

- Dashboard 的延迟 Dispatcher 回调、平移动画和缩放动画均在 UI 边界捕获异常，降低主题切换、视图卸载和共享 Freezable 导致 Playnite 扩展宿主崩溃的风险。
- 仍需在真实 Playnite 中验证 900+ 游戏库、重复打开/关闭 Dashboard、DPI 和主题切换；当前环境不能宣称宿主渲染已验证。

## 0.6.44 当前证据（2026-08-05）

- 共享表格视口提高到 520–820 DIP，行高 60 DIP、表头 50 DIP；六个工作区继续使用页面级纵向滚动和 DataGrid/ListBox 内部滚动。
- `validate-source.py`、自动测试和 Release 构建需在本次提交后重新运行；Playnite 宿主渲染、主题和 DPI 仍不能在当前环境宣称完成。

## 0.6.43 当前证据（2026-08-05）

- 已补齐 Playnite 库尚未准备完成时的零游戏启动竞态防护；500+ 游戏自动同步在捕获真实快照后于 Worker/IPC 之前熔断。
- 自动化校验、Release 测试、构建和 PEXT 打包待本轮完成；真实 900+ 游戏 Playnite 启动、窗口渲染和独立 LudusaviPlaynite 共存仍需用户 Windows 回归。

## 0.6.42 当前证据（2026-08-05）

- 所有工作区表格现在共享 `GscRedesignTableFrame` 和统一 DataGrid 几何/主题资源，表格默认显示更多可读行，外层页面滚动保持下方操作可达。
- 500+ 游戏库只在 Dashboard 打开时显示 SQLite 缓存；自动库事件和首次打开不会再启动整库 Ludusavi 匹配，显式刷新与游戏启动仍保留按需路径。源码校验通过；Playnite 真机、主题、DPI 和 900+ 游戏库耗时仍待用户环境验证。

## 0.6.41 当前证据（2026-08-05）

- 共享上下文操作的 disabled 状态不再设置 `Visibility=Collapsed`，而是保留在布局中并使用动态主题透明度；对应门禁测试已加入。
- 自动化证据为：Core 13、Worker 23、Playnite 93，共 129 项 Release 测试通过；源码校验通过。真实 Playnite、主题、DPI 和 900+ 游戏库回归仍待 Windows 环境验证。

## 0.6.40 当前证据（2026-08-05）

- 自动化证据为：Core 13、Worker 23、Playnite 92，共 128 项 Release 测试通过；Release 构建 0 错误（仅 NuGet 漏洞源不可达警告）；源码校验与 `git diff --check` 通过。真实 Playnite、主题、DPI 和 900+ 游戏库回归仍待 Windows 环境验证。

- 设置页低高度仍保留标题说明和保存语义提示；存档安全提示不再因 680 DIP 阈值消失；异常兜底视图使用系统窗口/文字资源，能随黑白高对比度环境适配。
- 真实 Playnite 900+ 游戏启动、窗口渲染、主题、DPI 和独立 LudusaviPlaynite 对照仍未在本环境完成。

## 0.6.39 当前证据（2026-08-05）

- Settings 与侧栏的滚动条现在统一使用 `GscPageScrollViewer`，避免设置页或长导航退回宿主默认样式。
- Overview、Media、Tasks、Maintenance 的摘要在低高度仍保留，由页面级滚动承载溢出；表格有限视口提高以显示更多行。
- 自动化证据为：Core 13、Worker 23、Playnite 91，共 127 项 Release 测试通过；Release 构建 0 错误（仅 NuGet 漏洞源不可达警告），PEXT CRC 与 Worker `0.6.39.0` 文件版本 smoke 通过。
- 真实 Playnite 900+ 游戏启动、窗口渲染、主题、DPI 和独立 LudusaviPlaynite 对照仍未在本环境完成；此前用户日志加载的是旧版 0.6.22。

本项目必须区分“源码已经实现”和“Windows/Playnite/真实游戏已经验证”。百分比是工程评估，不等于发布承诺。

## 0.6.38 当前证据（2026-08-05）

- 大型库同步路径已避免对未变化游戏重复写入 SQLite；匹配输入变化时会清理旧匹配并重试，Worker 冷启动等待有真实 30 秒墙钟截止，Dashboard 首次缓存读取和任务事件连接均采用不会长时间阻塞宿主的策略。
- 自动化证据为：源码门禁通过；Release 构建 0 错误（仅 NuGet 漏洞源不可达警告）；Core 13、Worker 23、Playnite 89，共 125 项测试通过。
- 真实 Playnite 900+ 游戏启动、窗口渲染、主题、DPI 和独立 LudusaviPlaynite 对照仍未在本环境完成；用户日志中加载的是旧版 0.6.22，不能作为 0.6.38 已验证证据。

## 2026-08-01 交付快照

- Apple-inspired WPF 源码重构已覆盖 Dashboard 的六个工作区与 Settings：共享令牌、控件状态、紧凑布局、数值输入、命令入口、焦点与高对比度/关闭玻璃/关闭动画降级均已有源码和自动化证据。
- 当前自动化证据为：源码门禁通过；Release 构建 0 警告/0 错误；Core 13、Worker 21、Playnite 80，共 114 项测试通过；0.6.24 PEXT 打包和 Worker `0.6.24.0` 文件版本 smoke 通过。真实大库启动和 Playnite 宿主渲染仍需用户侧回归。
- 这不等于真实 Playnite 已验收。`ENV-001` 尚未建立可证明隔离的 Playnite 数据根、扩展目录、测试库和 PID 边界；复制的 `.tmp` 安装副本可能共享用户 AppData，且 Playnite 单实例保护会转交至用户实例。不得为取得证据而关闭、覆盖或操作用户正在使用的 Playnite。

## 总体判断

| 维度 | 当前估算 | 说明 |
|---|---:|---|
| 源码功能覆盖度 | 约 95% | 主要模块、命令和 UI 入口已存在；多设备摘要、隔离下载、校验、受保护远端恢复、实时任务事件和媒体来源控制已接入 |
| MVP 源码完成度 | 约 98% | 插件/Worker、匹配、备份恢复、任务、媒体收件箱、修改器管理、FLiNG 应用内目录和事件通知均已实现 |
| Windows 真机闭环验证 | 约 50% | 已验证既有构建、安装、加载、游戏同步、匹配和 ZIP 备份；本轮 UI 的隔离 Playnite 回归、恢复、云端与自动候选仍缺证据 |
| 可安全日常使用度 | 约 45% | 可作为额外备份实验，但尚不能作为重要存档唯一保障，也不能默认执行恢复 |
| 正式稳定版准备度 | 约 35% | 缺完整回归矩阵、错误恢复证据、性能测试、发布升级和长期兼容验证 |

## 分阶段完成度

| 阶段 | 源码完成度 | 真机验证度 | 主要剩余 |
|---|---:|---:|---|
| 第一阶段：MVP | 93% | 68% | 退出/定时备份长期运行、更多平台媒体、后台主动通知 |
| 第二阶段：可靠性 | 82% | 38% | 云端状态闭环、重试队列、保留清理执行、压力与异常中断测试 |
| 第三阶段：安全恢复 | 86% | 10% | 本机与远端隔离恢复、PreRestore、失败回滚和撤销恢复的真实低风险验证 |
| 第四阶段：自动识别 | 66% | 8% | 会话候选调优、未知进程学习、远端设备摘要、完整冲突 UI |
| 第五阶段：完整 UX | 86% | 35% | 多主题/DPI 回归、Toast/对话框统一、继续页面拆分和可访问性验证 |

## 已经实现的核心功能

### 修改器与游戏工具

- 每游戏多个修改器、多个 Cheat Table 和多版本 SQLite 模型。
- EXE、ZIP、目录和 CT 本地导入、SHA-256、Zip Slip 防护和文件缺失状态。
- 每项独立启用、手动启动、自动启动、延迟、管理员权限和退出关闭。
- FLiNG 应用内目录缓存、搜索、版本读取、下载、安全解压和自动绑定。
- 常见反作弊线索默认阻止自动启动；不提供绕过、Defender 关闭或白名单。

### 工程与安装

- 完整 Git 历史、中文提交和固定英文作者。
- Windows build/test/publish/package/dev-install 流程。
- 一键构建安装运行、动态版本、实际目录发现、原子替换和安装版本核验。
- 跨平台 JSON/XML/YAML/XAML/C# 结构门禁。
- Apple-inspired WPF 规范和 UI 变更门禁已经落库。

### 存档与任务

- Ludusavi 健康、匹配、单游戏/全部/退出/游玩中定时备份。
- ZIP 多版本、压缩和完整/差异保留参数。
- 历史索引、备注、锁定、差异、保留预览。
- 任务队列、进度、取消、Worker 重启遗留任务修复。
- 真实错误码、stdout/stderr 诊断、复制详情。
- 失败/取消的 Backup、MediaSync 安全重试。

### 恢复与校验

- 文件数量、体积、零字节和异常下降校验。
- PreRestore、恢复确认、恢复后校验、失败回滚和撤销恢复源码。
- 自动恢复始终关闭。

### 游戏识别

- Playnite 启动/退出事件。
- 外部进程、安装路径、Game Action 和常见 MOD loader 识别。
- 未匹配游戏的启动前/退出后有界文件快照。
- 候选评分、解释、持久化、接受草案和忽略流程。
- Xbox WGS 辅助候选。

### 媒体与云端

- Steam、Game Bar、Windows、平台相邻目录和自定义媒体来源。
- 稳定写入、原子复制、SHA-256 去重和重新归类。
- 公共媒体文件名匹配、无重叠会话时间窗口、全局待归类收件箱、人工归类和忽略保留副本。
- Rclone `copy/check` 安全单向适配。

### UI

- Apple-inspired 六工作区与 Settings 信息架构、圆角阅读表面、局部毛玻璃环境光和克制微动效。
- 跟随 Playnite、固定浅色、固定深色。
- 高对比度、关闭动画和关闭玻璃降级。
- 大型游戏库搜索、筛选、排序和 Recycling 虚拟化列表；紧凑窗口保持滚动或横向访问而非裁切功能。
- 任务详情、诊断中心、空状态和真实错误展示。

## 仍未完整实现的功能

### P0：必须在继续扩功能前完成的真实验证

1. 在 Windows 编译、安装当前版本，修复所有真实 C#/XAML 错误。
2. 连续产生至少三个 ZIP 历史版本并确认历史、刷新和重启后仍一致。
3. 使用可丢弃存档跑通恢复、PreRestore、失败回滚和撤销恢复。
4. 验证 Worker 重启后设置、任务、历史和候选均不丢失。
5. 在多套 Playnite 主题和 100%–200% DPI 下完成 UI 回归。

### P1：重要未闭环功能

1. 任务变化已接入 Worker 最佳努力事件管道，并保留信号唤醒的有界长轮询；仍需验证 Playnite 关闭面板、Worker 重启和长任务高频进度下的稳定性。
2. Rclone 上传、游戏级状态和失败重试已闭环；仍需真实远端验证 `copy/check`、断网恢复、取消及恢复闸门。
3. 公共截图会话归类与全局收件箱的真实文件时间、性能和交互调优。
4. 未匹配游戏会话快照的性能、扩展名、目录深度和阈值调优。

### P2：高级功能

1. 多设备分叉历史已支持人工决策、远端隔离下载、校验和受保护恢复；仍缺两个真实设备与 Rclone 后端的完整回归。
2. 未知进程、通用启动器和 MOD 链人工学习已实现，仍缺多类真实 loader 回归。
3. 自动保留清理执行；当前只给出安全预览。Ludusavi 公开 CLI 未提供删除单个版本的稳定操作，因此继续保持不自动删除的安全边界。
4. 媒体收藏、备注、存储统计、批量元数据、虚拟化缩略图缓存和视频内嵌预览已实现；仍需真实编解码器与大库回归。
5. 配置导入/导出和缺失路径迁移报告已实现；Playnite 官方更新清单已准备，仍需 Release 资产和 Add-ons 数据库 PR。
6. Playnite Add-ons 数据库发布和长期版本兼容测试。

## 下一阶段建议

在用户恢复 Windows 测试前，源码开发优先顺序为：

1. 完成 0.6.5 的 Playnite 后台通知、云端重试和修改器多入口真机回归。
2. 使用可丢弃存档完成恢复、回滚与撤销恢复证据。
3. 媒体收件箱与公共截图会话归类的 Windows 真机调优。
4. 设计多设备冲突的人工选择流程，但继续禁止自动覆盖。

用户可测试后，立即暂停新增功能，先完成 P0 真机闭环。只有恢复与撤销恢复通过后，才应把“可安全日常使用度”提高到 65% 以上。
