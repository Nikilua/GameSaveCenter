# Q18-04/Q18-07 动效时钟与卸载清理

采集日期：2026-09-15（Asia/Shanghai）。代码基线：`68b49a1`（`补充动效时钟终态运行时回归`）。生产实现基线为 `4414f05`（`修复动效完成与卸载清理`）。

关联台账：Q16-08、Q17-07、Q18-04、Q18-07。

## 发现的问题

WPF `BeginAnimation` 默认会保留完成态时钟。此前部分 Dashboard 入口没有显式生命周期策略：对话框关闭只清除透明度动画，没有清除位移；`ClearToasts()` 直接清空 Children，绕过了逐卡的动画和 DispatcherTimer 清理；通用位移、入场、状态胶囊和页面切换路径的完成态也没有统一收口。

## 当前实现

- `GscMotion.AnimateTranslate` 与 `AnimateEntrance` 在替换旧动画前捕获当前有效值，使用显式 `FillBehavior.HoldEnd` 保持到完成回调，再移除 X/Y/Opacity 时钟并写回最终值；重入不会回到旧起点。
- Dashboard 的页面/状态/缩放动效、对话框和 Toast 入场都在完成回调中清时钟并写回终态。对话框用 `dialogMotionGeneration` 使旧的异步完成回调在关闭或重新打开后失效，`StopDialogMotion()` 同时清理透明度和 Y 位移。
- Dashboard 卸载时先失效对话框动画代际，再归一化主壳、可见 PageHost、工作区面板、状态胶囊和任务详情卡；Toast 先复制当前卡片并逐一调用 `RemoveToast()`，由该入口停止 Timer、清除 Opacity/X 动画，最后才清空容器。
- AcrylicProductionShellView 的侧栏宽度、透明度和位移动效显式声明完成态策略，完成、reduced-motion 和 Unloaded 路径都清理对应时钟并写回目标宽度/位置。
- `UiFinesseFoundationTests.MotionAnimationsReleaseClocksAtTheirFinalValues` 在真实 STA WPF `Window`/PresentationSource 宿主中运行 `AnimateTranslate` 与 `AnimateEntrance`；动画完成后断言 X/Y/Opacity 均为终值，且 `DependencyPropertyHelper` 报告不再处于动画状态。

## 验证

- Debug 构建：0 warning / 0 error。
- 动效时钟终态运行时测试：`MotionAnimationsReleaseClocksAtTheirFinalValues` `1/1` 通过；在 Release 下连续独立回放 5 次为 `5/5`，真实 STA WPF Window 宿主完成动画后均无活动时钟。
- 定向 Playnite 测试（Foundation、WPF 资源/源码、生产壳层）：164 通过、39 跳过、0 失败（203 总计）。
- Release 全量：Core `83/83`，Worker `310/311`（1 skip），Playnite `486/549`（63 skip），失败 `0`。
- `python scripts/validate-source.py` 与 `scripts/check-xaml.ps1` 均通过，`git diff --check` 通过。

这些证据覆盖源码级的完成、取消、重入和卸载清理契约，以及受控 WPF Window 中动画完成后的终态/时钟释放；尚未运行真实 Playnite 反复 Loaded/Unloaded 100 次、宿主窗口关闭、Rendering/ETW 采样或物理屏幕帧。因此 Q18-04/Q18-07 的真实宿主/视觉列仍按账本保留待验，不把受控运行时门禁升级成宿主验收。
