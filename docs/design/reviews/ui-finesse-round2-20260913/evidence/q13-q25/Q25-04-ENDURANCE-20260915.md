# Q25-04 30 分钟耐久专项复核（2026-09-15）

## 结论

受控 WPF STA Window 耐久探针已完成 1800 秒窗口，循环覆盖六个生产工作区、Light/Dark 主题、Media 预览分段、列表/表格选择和详情入口；运行无动作异常，资源时间序列完整，未使用强制 GC。该结果可以签收受控自动验证，不能替代 Playnite 嵌入宿主、真实用户输入、真实图片/网络任务或目标机显卡合成耐久。

## 可复现运行

- 代码基线：`7d7e2cb793b1ba22d556d2a4c38e531e87ad3aa4`（`补充三十分钟耐久探针`），运行时 `WorkingTreeClean: True`。
- 探针入口：`tests/GameSaveCenter.RenderHarness/Program.cs` 的 `enduranceprobe`，默认时长为 `1800` 秒；源码契约测试 `EnduranceProbeUsesARealDispatcherWindowWithoutForcedGc` 属于 `UiFinesseRound2ControlSourceTests`。
- 运行形态：真实 WPF STA `Window`，`1040×700` DIP，窗口以 `Opacity=0.01`、无任务栏方式承载生产 `AcrylicProductionShellView` 和六个生产工作区；报告明确记录为 `ControlledWpfWindow`，不推断 Playnite 宿主或真实屏幕 DPI。
- 循环动作：每 250 ms 切换工作区；依次切换 Light/Dark 主题；Media 选择预览分段；对列表/DataGrid 选择并定位项目；对可见详情按钮执行打开/关闭。
- 资源采样：约每 10 秒记录 `GC.GetTotalMemory(false)`、`PrivateMemorySize64`、`WorkingSet64`、线程数和句柄数；耐久探针代码段没有 `GC.Collect` 或 `WaitForPendingFinalizers`。
- 命令：`GameSaveCenter.RenderHarness.exe enduranceprobe .tmp/endurance-clean-20260915 1800`。
- 原始时间序列：[`.tmp/endurance-clean-20260915/enduranceprobe-report.txt`](../../../../../../.tmp/endurance-clean-20260915/enduranceprobe-report.txt)。

## 结果

- `duration_s=1800.4/1800`，`cycles=3847`，`completedActions=13462`，`samples=176`，`actionFailures=0`，最终 `enduranceprobe OK`。
- 托管内存：首样本 `19,032,624` bytes，末样本 `28,492,032` bytes，`managed_delta=9,459,408`，峰值增量 `25,124,144`；全窗口线性趋势约 `8,466.61 bytes/min`。
- 私有字节：首样本 `153,894,912` bytes，末样本 `274,464,768` bytes，`private_delta=120,569,856`，峰值增量 `150,548,480`；首 10 样本均值 `261,278,106`，末 10 样本均值 `277,500,723`，全窗口线性趋势约 `348,564.72 bytes/min`。中段和末段均有回落，未观察到随循环数单调增长。
- 资源计数：全窗口线程 `22–31`、句柄 `1090–1190`；约 135 秒后的预热后区间线程 `22–25`、句柄 `1170–1177`，末样本为线程 `24`、句柄 `1171`。
- 这些数值是一个受控 30 分钟窗口的边界观察，不是“数学上无界增长已被证明”；报告保留首尾、峰值、均值和趋势，便于后续真实宿主复测对比。

## 尚未签收的边界

Playnite 嵌入窗口、真实用户输入/焦点、真实图片解码与 Worker 网络任务、ETW 呈现帧和 >100 ms UI 线程调用栈仍需宿主/性能工具条件。Q25-04 因此保持“自动验证通过、宿主外部阻塞、最终未完成”。
