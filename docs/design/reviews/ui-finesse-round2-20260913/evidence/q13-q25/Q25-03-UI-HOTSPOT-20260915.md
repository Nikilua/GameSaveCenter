# Q25-03 UI 线程热点边界专项复核（2026-09-15）

## 结论

RenderHarness 现在会记录每个耐久动作周期的耗时、p95、最大值和 `>100 ms` 计数，并在超阈值周期完成后保留最多 8 条当前线程栈。干净 120 秒受控 WPF 运行稳定复现了慢动作边界，但这些栈是在动作完成后的 `finally` 中取得，不是 ETW/采样器在停顿期间取得的调用栈；因此本轮没有把它们写成已确认的 UI 线程热点，也没有据此修改业务实现。

## 可复现运行

- 代码基线：`ed97d2c1b7d738eb722c4794ec848bb04f863fb0`（`补充UI动作热点边界探针`），报告 `WorkingTreeClean: True`。
- 探针入口：`tests/GameSaveCenter.RenderHarness/Program.cs` 的 `enduranceprobe`；动作周期仍覆盖六个生产工作区、主题、Media 预览、列表/DataGrid 选择和详情入口。
- 计时边界：`RunActionCycle` 从进入到返回计时；`>100 ms` 只代表这个受控动作周期超过阈值。
- 栈边界：超阈值周期在动作返回后的 `finally` 中读取 `Environment.StackTrace`，报告显式标记 `captured after action completion`，不能代表停顿发生时的业务调用栈。
- 命令：`GameSaveCenter.RenderHarness.exe enduranceprobe .tmp/q25-03-hotspot-clean-20260915 120`。
- 原始报告：[`.tmp/q25-03-hotspot-clean-20260915/enduranceprobe-report.txt`](../../../../../../.tmp/q25-03-hotspot-clean-20260915/enduranceprobe-report.txt)。

## 结果

- `duration_s=120.4/120`，`cycles=254`，`completedActions=886`，`samples=13`，`actionFailures=0`，最终 `enduranceprobe OK`。
- `ui_action_p95_ms=249.94`，`ui_action_max_ms=483.44`，`ui_action_slow_over_100ms=254/254`。
- 报告保留 8 条超阈值后的现场栈；共同路径落在 `RunActionCycle`、`DispatcherTimer` 和 WPF `Dispatcher`，这是动作完成后的宿主线程栈，不足以定位具体业务热点。
- 源码契约测试 `EnduranceProbeRecordsUiActionHotspotBoundariesWithoutClaimingEtwStacks` 已补入 `UiFinesseRound2ControlSourceTests`；构建前定向契约集合为 `11/11` 通过。

## 尚未签收的边界

本轮没有 ETW/PerfView/等价采样器的停顿期间调用栈，也没有 Playnite 真实宿主前后对照或基于调用栈的优化回归。因此 Q25-03 可签收“代码与受控动作边界记录”，但真实 `>100 ms` UI 线程热点仍受性能工具/宿主条件外部阻塞，最终结论保持未完成。
