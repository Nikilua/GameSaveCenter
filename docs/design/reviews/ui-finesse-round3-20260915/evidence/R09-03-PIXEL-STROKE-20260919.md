# R09-03 一像素描边证据

日期：2026-09-19  
实现提交：`083b7a22`（`收口分数DPI描边像素对齐`）  
分支：`codex/ui-finesse-round2`

## 结论

R09-03 在当前可控条件下已满足。共享工作区 Tab、Dashboard 的对应 Tab、Acrylic 壳层导航和 Dashboard 导航的圆角 Chrome 均明确启用 `SnapsToDevicePixels=True`；现有 `UseLayoutRounding=True`、1 DIP 边框和共享分隔线保持不变。没有替换游戏选框或滚动条系统，也没有改变命令、绑定、取消/错误语义、恢复保护或列表策略。

## 实现与行为证据

- `Redesign.xaml` 的 `GscRedesignWorkspaceTabItem` 将 Chrome 从显式 `SnapsToDevicePixels=False` 收口为 `True`，保留 `UseLayoutRounding=True`、`ClipToBounds=False` 和 1 DIP 边框；这样选中状态的边框与圆角连接使用同一像素对齐契约。
- `DashboardView.xaml` 的同构 `GscTabItem` 同步该契约；两个导航模板（Dashboard 本地 `GscNavItem`、共享 `AcrylicNavItem`）对圆角 Chrome 明确设置 `SnapsToDevicePixels=True` 与 `UseLayoutRounding=True`。
- `R09PixelStrokeBehaviorTests.ProductionSelectedChromeAndNavigationChromeKeepOneDipRoundedStrokeContract` 在 STA WPF Window 中实例化真实生产 `TabItem`/`RadioButton`，进入选中/勾选状态后读取实际模板 Chrome，确认 1 DIP `BorderThickness`、像素吸附、布局取整和边框资源均保留。
- 同一夹具用真实 `RenderTargetBitmap`/`UiDiagnosticsExporters.SavePng` 对 1 DIP 分隔线和 1 DIP 圆角边框执行明确缩放模拟：`1.00`、`1.25`、`1.50`、`1.75`、`2.00`。带宽按显式 96-DPI 基线转换后的有效亮像素带检查，限制在随 scale 增长的 floor/ceil 边界内；圆角外角不泄漏成方角，圆弧后的直线连接存在。
- 相邻 `R09IconSemanticBehaviorTests` 及两项共享资源解析回归合计 `6/6` 通过，避免只用源码字符串断言签收。

## 构建与门禁

- 提交 `083b7a22` 重新执行 `scripts/build.ps1 -SkipTests`：XAML `24/24`，Release solution `0 warning / 0 error`，包含 Playnite `net462` 测试程序集。
- 同一提交身份运行 R09-03、R09-02 和共享资源定向回归：`6/6` 通过、`0` skipped。
- `python scripts/validate-source.py` 通过；`scripts/check-xaml.ps1` 为 `24/24`；`git diff --check` 通过。

## DPI 与验收边界

- `100/125/150/175/200%` 本阶段均为明确模拟：使用离屏 STA WPF 控件和 `RenderTargetBitmap` 的显式 render scale，位图保持 96-DPI 基线；这是可复算的缩放探针，不是 Windows 显示器物理 DPI 或真实用户呈现帧。
- 未宣称真实 Playnite 嵌入窗口、物理 DPI/跨屏、presented frame、UIA/读屏、IME、ETW 或宿主性能通过；未运行真实安装器，也未写入真实存档、媒体、云端或用户 Playnite。package-host 仍为 `not-provided`。
- Demo 原始目录不可用，本阶段继续沿用恢复生产基线。用户提供的 `DEV-INSTALL-008` main 全量 Playnite.Tests `73 failed / 588 passed / 57 skipped` 仍是合入后单一 checkout 的发布重跑边界，不与本阶段精确隔离验证混写。
- main 工作树已有的 `DashboardView.xaml.cs`、`src.zip`、两份 R08 基础设施文件和 R08 测试文件未触碰。

下一可执行任务：R09-04 阴影层次预算；先盘点浮层、卡片、输入框的共享 Effect 与低 Tier/高对比回退，再决定是否只补证据。
