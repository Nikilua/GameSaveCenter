# R15-01 任务阶段可读证据

日期：2026-09-20  
代码提交：`4e7ac33a 增加任务阶段可读性`  
分支：`codex/ui-finesse-round2`

## 实际完成

- 先核对 `TaskCoordinator`、任务 DTO、SQLite 任务查询和 `TaskCenterView` 的已有链路；没有重建任务服务。新增 Contracts 层共享 `TaskStageResolver`，把已有 Worker 阶段事件映射为扫描、校验、索引、上传、下载、恢复、清理等可读阶段。
- `TaskStatusDto.StageMessage` 单独保存最后一个真实阶段事件。失败或取消时保留最后阶段，`Message` 仍显示终态错误/取消语义；任务详情另行显示错误和技术详情，阶段标题不会遮住真实错误。
- 未知进度（包括运行中但没有可靠总量）显示 `—`，不伪造百分比；已知进度仍显示真实百分比。
- SQLite `tasks.stage_message` 使用现有迁移入口补列，并接入新增/更新/最近任务/分页查询；旧库默认空值可继续读取。
- Task Center 列表增加“阶段”列，详情增加当前阶段和原始阶段事件；没有改动当前任务命令绑定、取消入口、列表滚动或有限列表约束。

## 证据

- `python scripts/validate-source.py`：通过。
- `scripts/check-xaml.ps1 -ProjectRoot (Get-Location)`：24/24 通过。
- `git diff --check`：通过。
- Worker Release 隔离构建并执行 `TaskCoordinatorFailureTests`、`TaskQueryPersistenceTests`：12/12 通过，退出码 0。
- Playnite Release `net462` 构建并执行 `R15TaskStageTests`：2/2 通过，退出码 0。构建保留既有 `MediaCenterView.xaml.cs:664` nullable warning 2 条，无新增错误。
- WPF 静态质量检查：0 errors / 28 warnings / 177 info；新增任务阶段列表使用共享绑定，未添加不受限的阶段结果集合。

## 边界与未验项

- 测试使用合成 DTO、fake/隔离持久层和隔离构建输出；未写真实存档、媒体、云端或诊断，也未修改真实用户任务历史。
- Demo 原目录不可用，本批沿用恢复的生产基线。未宣称真实 Playnite/RenderHarness、最终 presented frame、物理 DPI/跨屏、UIA/IME、ETW 或宿主性能已验证；本批没有取得这些证据。
- `R15-01` 的真实宿主阶段事件覆盖仍需在可用宿主流程中复跑，尤其是各类备份、下载、校验、恢复、清理消息未覆盖到的未知文案；未知阶段会保持“阶段未知”，不会伪造百分比。
- main 分支的用户未提交改动和 `src.zip` 未触碰、未合并。

下一可执行任务：`R15-02 取消过程展示`，先核对现有取消请求、取消中状态和成功/取消竞争的终态收敛，再补最小行为夹具。
