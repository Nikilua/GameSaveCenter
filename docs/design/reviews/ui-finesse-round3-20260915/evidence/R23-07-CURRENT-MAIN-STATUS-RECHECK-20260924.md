# R23-07 当前 main 状态分类复核

日期：2026-09-24（Asia/Shanghai）
分支：`main`
审计基线：`e0a17e3a`（本次前一提交，仅文档审计；未改变代码和测试）
任务：R23-07「任务去重与收尾」，既有状态为“已满足”

## 当前账本复算

只统计 `ROUND3_PROGRESS.md` 中正式 8 列表头之后的任务行，排除表前 R13/R14 的 10 行摘要。R00–R23 共 `192` 行、`192` 个唯一 ID。按当前逐行状态原样分组：

| 当前状态组 | 数量 | 口径 |
| --- | ---: | --- |
| 已满足 / 当前 main 受控复核通过 | 106 | 含已满足并另记审计风险的行；不含带明确待验环境的状态 |
| 已满足，待明确环境门禁 | 83 | 含正常宿主、包身份、真实触控板/宿主等逐行注明的后续验证 |
| 外部阻塞 | 1 | R02-06：Playnite 原生菜单 visual tree 与输入由宿主拥有 |
| 部分满足 | 1 | R23-05：代理与断点几何有证据，真实 presented frame/宿主性能仍未验 |
| 不适用 | 1 | R05-05，按该行记录的范围不适用 |
| **合计** | **192** | 没有把摘要行计入任务数 |

当前正式表内没有“待开始”“实施中”或“代码完成待验收”状态。上述 `83` 行保留各自环境门禁，不能并入已完成宿主验收的数字。旧报告 `R23-07-TASK-DEDUPE-CLOSURE-20260922.md` 的 `112/29/45/1/3/1/1` 是 9 月 22 日当时的分类快照，不覆盖当前 main 状态；当前计数以本复核和逐行表为准。

## 第二轮 Q 队列复核

`ROUND2_PROGRESS.md` 正式表有 `208` 个唯一 Q ID。最终结论仍为 `5` 项“已验收”、`203` 项“未完成”；自动/视觉列已有通过不等于最终验收，R23 的 R 表状态也不用于提升 Q 表结论。跨屏、真实宿主与呈现帧限制仍由各自 Q/R 行承接。

## 当前保留的未验边界

- R23-04：已满足可控 runner/包流程，正常 Playnite 主窗体与 UIA/Controlled 仍待宿主环境。最近一次窗口探测只发现 `Startup Error`，CEF `platform_channel 0x5` 拒绝访问；条件未变化，本复核没有重试或绕过。
- R23-05：隔离 WPF `Rendering`/Stopwatch 和断点几何只作代理证据；真实 presented frame、ETW/WPR/xperf 与 Playnite 宿主性能仍未验，继续遵守权限拒绝边界。
- R02-06：需由正常 Playnite 宿主验证原生菜单状态、键盘与关闭行为；插件树上的静态契约不能替代宿主输入。
- Q24-03：当前单显示器不能证明物理跨屏 Popup/DPI 行为。
- 用户报告的 Media、Task、Save、Settings 四页窗口几何已有当前 main Light/Dark 隔离行为 `8/8`；旧安装 DLL 与截图进程未关联，候选 `e83d8ba9` 尚未安装。该行为结果不等于用户真实窗口已修复，见[四页当前 main 复核](USER-REPORTED-LAYOUT-CURRENT-MAIN-RECHECK-20260924.md)。

## 处理与下一步

本次只复算队列行数、唯一 ID 和逐行状态，并复用既有行级证据链接；没有重跑 192 项行为测试、改业务代码或把待验状态改成通过。R23-07 继续保持“已满足”。下一可执行宿主动作是：当隔离 Playnite 会话能正常显示主窗体时，用当前候选身份重跑 R23-04 UIA/Controlled；在宿主条件未变时不重复启动失败流程。若取得合规 ETW/WPR/xperf 权限，再补 R23-05 真实帧样本；否则继续保留阻塞。
