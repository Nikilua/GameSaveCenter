# R21-02 控件名称与值收口证据

日期：2026-09-21  
收口依据：`6b56a467`、`efb42b7b`（行为证据），`564c1b10`、`03f8d33f`（证据与账本）  
分支：`codex/ui-finesse-round2`

## 完成条件核对

R21-02 要求核对图标按钮、复合选择器、开关和进度条的 UIA 名称、状态和值。当前分支已按控件族和负例逐项核对：

- 图标/动作按钮：实际 peer 读取稳定中文名称，glyph Content 不作为名称；保存、归类和批量动作通过 `IInvokeProvider` 验证。
- 复合选择器：实际 peer 覆盖媒体、维护、Trainer、Dashboard、SaveCenter 和任务筛选器的名称/选值；无选中 ComboBox 的 `GetSelection()` 返回 `null`，选中后返回单项。
- 开关：收藏、策略和设置开关覆盖 `Off → On`；共享三态 ToggleSwitch 额外验证 `Indeterminate → Off → On`，并明确不把三态能力冒充当前业务 Binding 行为。
- 进度条：TaskCenter 生产 Binding 的实际 peer 覆盖名称、`RangeValue`、`0–100` 范围、正常值、未知 `—` 和越界钳制；Maintenance/Overview/Trainer/Dashboard 的生产名称与 Binding 由对应契约测试和既有状态行为覆盖。

负例不是只靠源码字符串：当前 `R21AutomationValueBehaviorTests` 使用 WPF AutomationPeer 实际检查名称、Invoke、Selection、Toggle 和 RangeValue；相关 R06/R12/媒体批量行为测试覆盖未知、空选、越界、忙碌禁用、取消/失败/终态等语义边界。既有生产命令、Binding、取消/错误、恢复保护、游戏选框、滚动条、虚拟化和列表性能未被本阶段替换。

## 验证结果

- `R21AutomationValueBehaviorTests`：`21/21`；相关定向筛选 `R21AutomationValueBehaviorTests | R06TaskProgressBehaviorTests | R03NumericAlignmentTests`：`35/35`。
- 最新行为提交 `efb42b7b3dd6f259954cb3c8d170e97776428c9d` 的 D 盘 source-copy Release 构建 Playnite `net462` / Tests `net472`：`0 errors`，仅 `MediaCenterView.xaml.cs:671` 的 2 条既有 `CS8602` warning。
- `validate-source.py` 通过；XAML `24/24`；`git diff --check` 通过；WPF 静态检查 `0 errors / 27 warnings / 162 info`，无本阶段新增诊断。
- 逐批证据：[`R21-02 任务进度`](R21-02-TASK-PROGRESS-PEER-20260921.md)、[`R21-02 选择器与开关负例`](R21-02-SELECTOR-TOGGLE-NEGATIVE-20260921.md)，以及已有的 [`初批控件名称与值`](R21-02-AUTOMATION-VALUE-20260921.md)、[`进度控件`](R21-02-PROGRESS-AUTOMATION-20260921.md)、[`媒体选择器`](R21-02-MEDIA-SELECTORS-AUTOMATION-20260921.md)、[`SaveCenter 策略 peer`](R21-02-SAVECENTER-POLICY-PEER-20260921.md)。

## 收口边界

R21-02 在当前可控条件下记为“已满足，待环境验证”，不宣称真实 Playnite/package-host、Windows UIA/读屏、OS 输入、IME、物理 DPI/跨屏、最终 presented frame、ETW 或宿主性能。Demo 原目录不可用，沿用已恢复的生产基线；链接工作树 `_wpftmp.csproj` 的 `Access denied` 未绕过；main 用户改动未碰、未合并。本阶段 D 盘 source-copy/build 已清理。

下一可执行任务：进入 R21-03，先复用现有验证/错误 Binding 与状态 presenter，补“错误出现→必要通知、修复后旧错误状态消失”的实际行为证据；真实宿主读屏与呈现仍需后续环境验证。
