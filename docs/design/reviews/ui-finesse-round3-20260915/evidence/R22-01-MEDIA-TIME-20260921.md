# R22-01 MediaCenter 媒体时间入口（2026-09-21）

## 本子批次结论

提交 `e4389a0904ec5e027a5c04525e18d9c1126209b1` 将 `MediaItemDto` 的媒体拍摄时间接入共享 `TimeDisplayFormatter`，并收口 MediaCenter 当前游戏媒体、媒体收件箱、选中媒体详情和重复组条目的展示入口。R22-01 整项仍为“部分满足，待继续”，没有把分类建议、批次摘要或维护证据时间字段误写成已迁移。

## 复用与实现

- `MediaItemDto` 保留 `CapturedLocal` 兼容属性，新增 `CapturedRelativeDisplay`、`CapturedFullDisplay` 和 `CapturedRawUtcDisplay`；相对文本由共享 formatter 计算，完整提示保留本地时区和原始 UTC 值。
- `MediaCenterView.xaml` 的 Media Inbox 表格、当前游戏媒体卡片、选中媒体详情和重复组条目改用相对时间；完整时间进入 Tooltip/Automation HelpText。表格的复制参数、虚拟化、列宽、游戏选框、滚动条、命令/Binding 和媒体写入语义未改。
- 测试通过源码身份门禁读取真实生产 XAML，并验证 DTO 的完整/原始时间合同及入口绑定；没有用 `Assert.Contains` 单独签收交互、呈现或性能。

## 实际验证

- `R22TimeDisplayBehaviorTests` `9/9`；`R18TableContainerBudgetTests | R19DraftRefreshBehaviorTests | R14SourceRulePreviewTests` `4/4`。
- 合并的相邻媒体/分类回归为 `26 passed / 3 failed / 0 skipped`；3 条失败均为本批前已存在的基线漂移：`R14ClassificationSelectionTests.ClassificationTargetsShowVisualAndStableIdentityWithoutIndexSelection`、`TypographyDiagnosticsTests.ProductionTypographyUsesSharedHierarchyWithoutMicroSizeDrift`、`R14ClassificationEvidenceTests.ClassificationPreviewShowsStructuredEvidenceAndPendingNegativeState`。未把它们改写为通过。
- 精确提交身份 Release 构建：Playnite `net462` `0 errors`、Playnite Tests `net472` `0 errors`；Playnite 主项目仍有 2 条既有 `MediaCenterView.xaml.cs:671 CS8602` warning。`validate-source.py`、XAML `24/24`、`git diff --check` 和 WPF 静态检查 `0 errors / 27 warnings / 177 info` 通过。

## 未验边界

- `MediaClassificationSuggestionDto.CapturedLocal`、分类批次 `CreatedLocal/UpdatedLocal/ExpiresLocal` 及 `ValidationFindingDto.EvidenceTimeDisplay` 尚未全部迁移；这些字段要分别核对现有页面直显、摘要兼容和负例语义，不能由本子批次代签。
- 真实剪贴板、Playnite/package-host、Windows UIA/读屏、OS 输入/IME、DPI/跨屏、最终呈现、ETW 和宿主性能未验；Demo 原目录不可用，继续使用恢复生产基线。验证只使用合成 DTO、fake/隔离 testhost 和隔离目录，未写真实存档、删除真实媒体、写用户云端或外发诊断。
- `main` 的用户未提交改动未碰、未合并；本轮完整目标尚未收口，因此暂不进行分支整合。

## 下一步

继续 R22-01：先盘点 `MediaClassificationDtos` 的 `CapturedLocal`、批次摘要时间和 `ValidationFinding` 证据时间是否有当前页面直显，优先复用已有 formatter 与技术文本/帮助控件，再决定最小行为证据范围。
