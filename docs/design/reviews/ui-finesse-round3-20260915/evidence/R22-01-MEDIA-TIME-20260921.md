# R22-01 MediaCenter 媒体与分类时间入口（2026-09-21）

## 本子批次结论

提交 `e4389a0904ec5e027a5c04525e18d9c1126209b1`、`a799317b` 与 `eb6a8682` 将 `MediaItemDto`、分类建议、分类批次和 Maintenance `ValidationFindingDto` 证据时间接入共享 `TimeDisplayFormatter`，收口 MediaCenter 当前游戏媒体、媒体收件箱、选中媒体详情、重复组条目、归类预览、批次历史和两个诊断详情入口的展示合同。R22-01 整项仍为“部分满足，待继续”，其他 Save/恢复未直显字段没有由本批代签。

## 复用与实现

- `MediaItemDto` 保留 `CapturedLocal` 兼容属性，新增 `CapturedRelativeDisplay`、`CapturedFullDisplay` 和 `CapturedRawUtcDisplay`；相对文本由共享 formatter 计算，完整提示保留本地时区和原始 UTC 值。
- `MediaCenterView.xaml` 的 Media Inbox 表格、当前游戏媒体卡片、选中媒体详情和重复组条目改用相对时间；完整时间进入 Tooltip/Automation HelpText。表格的复制参数、虚拟化、列宽、游戏选框、滚动条、命令/Binding 和媒体写入语义未改。
- `MediaClassificationSuggestionDto`、`MediaClassificationPreviewDto` 和 `MediaClassificationBatchSummaryDto` 增加共享时间属性；预览摘要、建议条目和批次历史用相对时间，完整时间进入 Tooltip/Automation HelpText。保留 `CapturedLocal`、`CreatedLocal`、`UpdatedLocal`、`ExpiresLocal` 与 `DetailDisplay` 兼容属性，分类预览的纳入、目标覆盖、应用、撤销、过期和恢复语义未改。
- `ValidationFindingDto` 增加相对/完整/原始 UTC 证据时间；保留 `CreatedLocal`、旧 `EvidenceTimeDisplay` 和 `DateTime.MinValue` 的“证据时间未知”。Maintenance 两个 `SelectedFinding` 诊断详情入口改用相对显示，完整时间进入 Tooltip/Automation HelpText；表格、复制、导航、命令和错误/取消语义未改。
- 测试通过源码身份门禁读取真实生产 XAML，并验证 DTO 的完整/原始时间合同及入口绑定；没有用 `Assert.Contains` 单独签收交互、呈现或性能。

## 实际验证

- `R22TimeDisplayBehaviorTests` `11/11`；`MaintenanceReportSourceTests | R17FindingTriageBehaviorTests` `7/7`；`R18TableContainerBudgetTests | R19DraftRefreshBehaviorTests | R14SourceRulePreviewTests` `4/4`。
- `MediaSyncServiceTests` 为 `19 passed / 1 failed / 0 skipped`；唯一失败是本批前已存在的 `ClassificationApplyUsesSelectedStableIdsAndValidatedTargetOverride` 在 `MediaSyncServiceTests.cs:570` 的 `NullReferenceException`，排除该已知测试后其余 `19/19` 通过。
- 最终提交身份下合并的相邻媒体/分类/诊断时间回归为 `28 passed / 3 failed / 0 skipped`；3 条失败仍为既有基线漂移：`R14ClassificationSelectionTests.ClassificationTargetsShowVisualAndStableIdentityWithoutIndexSelection`、`TypographyDiagnosticsTests.ProductionTypographyUsesSharedHierarchyWithoutMicroSizeDrift`、`R14ClassificationEvidenceTests.ClassificationPreviewShowsStructuredEvidenceAndPendingNegativeState`。未把它们改写为通过。
- 精确提交身份 `eb6a8682` Release 构建：Playnite `net462` `0 errors`、Playnite Tests `net472` `0 errors`；Playnite 主项目仍有 2 条既有 `MediaCenterView.xaml.cs:671 CS8602` warning。`validate-source.py`、XAML `24/24`、`git diff --check` 和 WPF 静态检查 `0 errors / 27 warnings / 177 info` 通过。

## 未验边界

- 其他未直显的 Save/恢复时间字段尚未迁移；要分别核对现有页面直显、摘要兼容和未知时间负例，不能由本子批次代签。`ValidationFinding` 本子批次只覆盖两个 Maintenance 诊断详情入口。
- 真实剪贴板、Playnite/package-host、Windows UIA/读屏、OS 输入/IME、DPI/跨屏、最终呈现、ETW 和宿主性能未验；Demo 原目录不可用，继续使用恢复生产基线。验证只使用合成 DTO、fake/隔离 testhost 和隔离目录，未写真实存档、删除真实媒体、写用户云端或外发诊断。
- `main` 的用户未提交改动未碰、未合并；本轮完整目标尚未收口，因此暂不进行分支整合。

## 下一步

继续 R22-01：盘点其他 Save/恢复时间直显与兼容字段，优先复用已有 formatter 与技术文本/帮助控件，并为每个入口补未知/负例行为证据。
