# R22-01 时间显示统一（2026-09-21）

## 本阶段结论

R22-01 本阶段为“部分满足，待继续”。提交 `1250aaadc22218a4dd3be6ac26f6d09a36182e1e` 建立共享时间显示契约并接入任务时间线，`538fcae9058c63cbc84d6f6e62e51e1f4ed48cdb` 补任务单调时长与 SQLite/Worker 重启收口，`b53ab44fd48c3e2a879cb886f410ffb155c3b2fb` 接入 Task Center/Overview，`2be8627db39304a2a3f929a797799c5c366b28c0` 和 `c211a04f5c28b400bac976cfc2223cae2a709299` 接入 Save/Maintenance 主要时间入口；剩余时间字段、复制与真实宿主边界仍未完成。详见 [Save 与 Maintenance](R22-01-SAVE-MAINTENANCE-TIME-20260921.md)、[Overview 与任务入口](R22-01-OVERVIEW-TIME-20260921.md) 和 [任务单调时长与恢复](R22-01-MONOTONIC-TASK-DURATION-20260921.md)。

## 先查到的现有能力

- 之前没有共享时间显示服务或 DTO 格式化器。任务时间线已有 `OccurredUtc`，构建器按 UTC 时间和序号排序；本阶段没有用显示文本替代排序键。
- 时间线原有本地时间、UTC 时间和未知时间文案，保留其兼容属性；新增相对时间、完整时间和原始 UTC 文本属性。
- Worker 吞吐采样已有 `Stopwatch.GetTimestamp()`；第一子批次时 `TaskStatusDto.DurationDisplay` 仍由墙上时间计算，后续单独子批次已把同一计数器能力接入任务运行/终态/重启快照。旧任务没有单调字段时仍保留 legacy fallback。

## 已实现与实际验证

- `GameSaveCenter.Contracts.TimeDisplayFormatter` 将 `Unspecified` 按已持久化 UTC 处理，提供可注入 `nowUtc` 的“刚刚/分钟/今天/昨天/日期”相对时间、带本地时区偏移的完整时间，以及 round-trip UTC 原值。
- `TaskTimelineEntryDto` 暴露 `RelativeTimeDisplay`、`FullTimeDisplay` 和 `RawUtcTimeDisplay`；`TaskCenterView` 的时间线显示相对时间，Tooltip 和 Automation HelpText 提供完整时间及原始 UTC 值。未知时间仍显示“时间未知/未记录 UTC 时间”。
- `R22TimeDisplayBehaviorTests` `5/5`：59 秒/60 秒边界、未来短时间、昨天边界、时区提示与原始 UTC 保留、UTC 排序独立于相对文本、未知时间负例。
- 既有 `R15TaskTimelineTests` `3/3`：观察到的 UTC 顺序、缺失中间事件不推断、未知时间和时间线源契约保持通过。
- 后续 `R22TaskDurationBehaviorTests 2/2`、`TaskQueryPersistenceTests 9/9`、`TaskCoordinatorFailureTests 7/7`、`DatabaseMigrationHarnessTests 4/4` 和隔离 Worker 硬重启 `1/1` 通过；完整事实见单调时长证据。
- `b53ab44f` 接入 Task Center 任务表/详情和 Overview 最近任务/全局活动；`R22TimeDisplayBehaviorTests 6/6`、`R15TaskTimelineTests 3/3`、`OverviewInteractionTests 1/1`、`R10RecentAccessBehaviorTests 2/2`，本批合计 `12/12`。任务表列宽、选择/分页、滚动、复制、命令与取消语义保持不变。
- `2be8627d` 接入 Save 历史/详情和 Maintenance 审计表；`R22TimeDisplayBehaviorTests 7/7`、`R06SortingBehaviorTests 4/4`、`R11HistoryTimeNavigationBehaviorTests 3/3`，相关 WPF 方法 `3/3`，另 1 条既有真实宿主条件测试跳过；本子批次 `17 passed / 1 skipped`。完整事实见 [Save 与 Maintenance](R22-01-SAVE-MAINTENANCE-TIME-20260921.md)。
- 第一子批次提交身份构建：Playnite `net462` `0 errors / 2` 条既有 `MediaCenterView.xaml.cs:671 CS8602` warning；Tests `net472` `0 warnings / 0 errors`。`validate-source.py`、XAML `24/24`、`git diff --check` 通过；WPF 静态检查 `0 errors / 27 warnings / 177 info`，告警来自既有布局/颜色规则，未发现新增错误。

## 尚未满足与边界

- Maintenance 保留预览及其他 Save/恢复时间入口尚未全部接入共享相对/完整/时区显示；相对时间随 DTO 刷新或绑定重建更新，系统时间回拨/跳跃与跨系统启动周期仍待真实边界验证。
- 本阶段测试使用固定 UTC 和合成 DTO；没有实际改变系统时钟，也没有把合成排序验证写成系统时钟跳变或真实宿主证据。
- 原始 UTC 已保留在可绑定属性和完整提示中，但没有新增真实剪贴板动作；真实 Playnite/package-host、Windows UIA/读屏、OS 输入/IME、DPI/跨屏、最终呈现、ETW 和宿主性能未验。
- Demo 原目录不可用，继续使用恢复生产基线；未写真实存档、媒体、云端或诊断，main 用户改动未碰、未合并。

## 下一步

继续 R22-01 小批量：先盘点 Maintenance 保留预览及剩余明确时间字段和已有复制行为，复用当前 formatter/技术文本控件；随后补系统时钟跳变与真实宿主边界。
