# R00-01 / R00-02 受控证据

- 实现提交：`a95e900`（修正整组按压合成、保留渐变 stop offset、复用组合缩放树）；`e216e9b` 补充非等距 stop 的行为负例。
- clean-tree 验证基线：`e216e9bf0d2ed18adced62936d6897b8d84f0d58`，RenderHarness 两次报告均为 `WorkingTreeClean=True`。
- 运行环境：Release、Windows WPF、offscreen logical DIP、`DpiScale=1.00`、窗口 `1120×980 DIP`；这是受控视觉/行为证据，不等价真实 Playnite 宿主、物理 DPI 或呈现帧。

## 自动与行为

- `dotnet test tests/GameSaveCenter.Playnite.Tests/GameSaveCenter.Playnite.Tests.csproj --no-build --no-restore -c Release --filter "FullyQualifiedName~GradientContrastModelsWholeChromeOpacityAndNonUniformStops|FullyQualifiedName~SemanticStateContrastCoversGradientButtonSelectionInputDangerAndComplexSurfaces|FullyQualifiedName~ScaleTransformIsReusedInAStableCompositeTree|FullyQualifiedName~MotionAnimationsReleaseClocksAtTheirFinalValues"`：`5/5` 通过、`0` 失败、`0` 跳过。
- R00-01 负例直接计算黑父背景、白 chrome、黑字、`opacity=0.5`：按压背景为 `#808080`，有效文字为黑色，对比度达到门槛；同时断言 `0.9` 的非等距 stop 仍为白色、`1.0` 才为黑色。状态矩阵含 `normal`、`hover`、`focus`、`hover+focus`、`pressed`、`hover+pressed`、`pressed+focus` 和 `hover+pressed+focus`。
- R00-02 在冻结的 `TranslateTransform + RotateTransform + TransformGroup` 组合上连续调用 1000 次，复用同一 `ScaleTransform`，树节点数和深度稳定，既有平移/旋转值保留；第二个控件的缩放实例独立。

## 双主题受控探针

- `tests/GameSaveCenter.RenderHarness/bin/Release/net472/GameSaveCenter.RenderHarness.exe finesseprobe ... light`：exit `0`，`SemanticButtonContrast: samples=88 normal/hover/focus/pressed-combinations=all-stops violations=0`，`finesse-fixture OK`。
- 同命令 `dark`：exit `0`，同样 `88` 个状态样本、`0` 个 violation、`finesse-fixture OK`。
- 探针使用合成混合语言/状态/表格数据；日志明确 `ControlEvidenceBoundary=offscreen realized templates and DIP geometry`，IME、真实 Popup、Playnite 输入和物理屏幕仍需后续宿主验证。

## 2026-09-18 当前分支复核

- 当前 HEAD `89aa27e15e8ed8d2be5a68a6164541e9728d62ab` 重新执行 R00-01/02 定向行为测试：`4/4` 通过、`0` 失败、`0` 跳过；覆盖整组透明度/非等距 stop、语义状态矩阵和 1000 次组合变换树稳定性。R00-03 动效测试不计入本批次。
- 当前 HEAD 隔离 Release 构建 `.tmp/r00-01-02-build-current`：XAML `24/24`，解决方案 `0 warning / 0 error`，RenderHarness `0 warning / 0 error`。
- 从该隔离 RenderHarness 分别运行 Light/Dark `finesseprobe`：两主题均 `SemanticButtonContrast samples=88 ... violations=0`、`NumericReadability expected=4 realized=4 horizontalFit=4 verticalFit=4 allReadable=True`、非等距 stop/窄列负例 `must-fail=passed`，末尾均为 `finesse-fixture OK`；报告身份均为当前 HEAD、`WorkingTreeClean=True`、offscreen `DpiScale=1.00`。
- 因此 R00-01、R00-02 的代码与当前可控证据条件已满足；R00-02 仍把可变 Freezable 跨控件共享隔离保留给 R08-08，不把本批次扩大到未授权范围。

## 未验边界与下一步

- 未以真实鼠标按压、键盘焦点切换、Playnite 宿主或物理显示器确认最终呈现；不把本证据升级为宿主列通过。
- R00-02 对冻结树和独立实例已覆盖；可变 Freezable 被两个控件共享的所有权隔离属于 R08-08，继续单独补负例。
- 下一可执行任务：R00-03 的完成/取消/卸载/重入 Dispatcher 行为复核；随后处理 R00-04 的真实搜索变化样本。

## 2026-09-23 当前身份复核

- 当前提交 `b5c7a6d423a4bf23004c3b080e133b3b0b065fa5` 的隔离 Release RenderHarness 已重建，XAML `24/24`、solution `0 errors/2` 条既有 `MediaCenterView.xaml.cs:706` nullable warning，RenderHarness `0 warning/0 error`。
- Light/Dark `finesseprobe` 均为 `WorkingTreeClean=True`、`DpiScale=1.00` offscreen logical DIP；`SemanticButtonContrast samples=88`、`violations=0`，`NumericReadability expected=4 realized=4 horizontalFit=4 verticalFit=4 allReadable=True`，非等距 stop 与窄列负例均为 `must-fail=passed`。
- 同一提交的 `scaleprobe` 覆盖 backend `1000/5000/20000` 与 Media UI `2000`，末尾 `scaleprobe OK`；这是有限列表/虚拟化的离屏基准，不是 Playnite 宿主性能。

## 2026-09-23 组合态断言补强

- 最终 Release build identity `38d5b7b2d0488dc5e7234d77ff1435c9d4e521c0` 下，R00-01 对比测试 `3/3` 通过；`RepositoryIdentityTests 2/2` 验证该隔离构建读取指定仓库与提交。完整 solution XAML `24/24`、`0 errors`，保留两条 `MediaCenterView.xaml.cs:706 CS8602` warning。
- 复核旧计算断言后发现：`opacity=0.5` 的测试取样并验证了 `pressed@0.0`，但没有同时断言 `hover+pressed+focus`。现直接对该组合项核验背景 `#808080`、有效文字 `#000000` 和 `Actual >= 4.5`；这项检查测量算法结果，不依赖源码字符串。
- `5fbfc869` clean-tree Light/Dark 探针均有 88 个状态/stop 样本、0 violations；数值可读 `4/4`、窄列负例与深底黑字负例均 expected-fail。纯图标按钮标签已校正为“无文字样本”，不会再显示成控件缺失。
- `UI_EVIDENCE_BASELINE.json` 已增加 `UiDiagnosticsExporterTests.cs` 与 `UiFinesseFoundationTests.cs` 两个 R00-01/02 行为测试源路径，避免以后只改测试而 freshness 仍报告旧证据新鲜。

## 2026-09-23 当前 main 双主题证据刷新

- 探针与隔离程序集源码身份均为 `d752424ee46c861e080a8e57b01f90f19c3a7872`；本轮源码树干净。隔离 Release solution XAML `24/24`、0 errors，保留两条既有 `MediaCenterView.xaml.cs:706 CS8602` warning；RenderHarness 为 `0 warning/0 error`。
- Light/Dark `finesseprobe` 均 exit `0`、`WorkingTreeClean=True`、offscreen logical DIP `DpiScale=1.00`、窗口 `1120×980 DIP`。每主题 88 个 semantic button state/gradient-stop 样本、0 violations；数值可读 `4/4`，窄列裁切负例及黑字深底负例均 `must-fail=passed`。两个报告末尾都是 `finesse-fixture OK`。
- 定向过滤的 TRX 总数 `5/5`，精确组成是 R00-01/02 相关 `4/4`（`GradientContrastModelsWholeChromeOpacityAndNonUniformStops 1`、语义状态 Light/Dark `2`、`ScaleTransformIsReusedInAStableCompositeTree 1`），加上 R00-03 的 `MotionAnimationsReleaseClocksAtTheirFinalValues 1/1`；没有把这项动画测试算入 R00-01/02。R00-01/02 的 Light/Dark 探针是本轮分别重跑的。
- offscreen realized-template/DIP 探针不是真实 Playnite 屏幕像素、物理 DPI 或 presented frame。没有注入真实鼠标/键盘、Windows IME、宿主输入或 ETW；只使用合成多语言/状态/表格数据。

## 2026-09-23 当前 main HEAD `3a1dadd8` 复核

- 当前身份的 `UiFinesseFoundationTests` `9/9`、`UiDiagnosticsExporterTests` `11/11`；Light/Dark `finesseprobe` 均使用当前完整 SHA `3a1dadd80bae6152dce9c3f684d5d2c745f02bc8`，每主题 palette `11` checks、`0` violations，numeric readability `4/4`、窄列负例 `must-fail=passed`，结尾 `finesse-fixture OK`。
- 两次 probe 的 `WorkingTreeClean=False` 是因为当时工作区已有待提交的 R00/R01 文档，不是源文件身份不符；探针明确报告 source/build 完整 SHA 相同，`DpiScale=1.00` 仍是 offscreen logical DIP。
- 本节只刷新当前 identity 与受控算法/状态证据，不宣称真实 Playnite 按压、物理像素或宿主呈现。
