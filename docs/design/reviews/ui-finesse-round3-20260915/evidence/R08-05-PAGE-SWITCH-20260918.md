# R08-05 页面切换轻量化（2026-09-18）

## 结论

R08-05 已满足。最新生产壳层在 `Attach` 时一次创建六个真实工作区页面，`NavigateTo` 只在目标页面实例变化时替换 `PageHost.Content`；同页重复请求保留原视觉树。页面切换没有新增全页 Blur 或整页入场动画。

## 实现与身份

- 提交：`ccd71c27`（`收口页面切换轻量化`）。
- `AcrylicProductionShellView.NavigateTo` 增加 `ReferenceEquals(PageHost.Content, page)` 门槛；保留真实命令、绑定、游戏选框、滚动条、页面缓存和 Playnite/net462 契约。
- `AcrylicProductionShellView` 增加仅供测试读取的 `MeasureOverride`/`ArrangeOverride` 计数，不改变布局决策、业务状态或动画。
- 隔离源码根：`r08-05-source-ccd71c27`，源码 HEAD 与构建身份均为 `ccd71c27`。
- 隔离构建根：`r08-05-build-ccd71c27`；solution Release `0 warning / 0 error`，Playnite 目标 `net462`，XAML `24/24`。
- `python scripts/validate-source.py`、`check-xaml.ps1`、`git diff --check` 通过；WPF 静态审查为 `0 errors / 21 warnings / 177 infos`，warnings/infos 为仓库既有布局与资源提示，本项未新增 error。

## 实际 STA WPF 行为

测试：`R08PageSwitchBehaviorTests` `2/2`；相邻 R02/R08/壳层回归合计 `28/28`。

夹具使用真实 `AcrylicProductionShellView`、真实 `TaskCenterView`/`MediaCenterView` 页面、合成 `96` 条 `TaskStatusDto`、隔离 STA Window（`1366×900`，opacity `0.01`）和 offscreen logical DIP：

- 任务页 DataGrid 选中第 `55` 项，内部滚动 offset 为 `10`；切换到媒体页再回任务页后仍为 `10→10`。
- 任务页实例、DataContext、96 项 ItemsSource 和选中对象均保持同一引用，没有重新建树。
- 任务→媒体：外层 shell `Measure/Arrange=2/2`；媒体→任务：`1/1`。增量与一次 Content 替换及一次恢复相符。
- 回到任务页后再次请求同一任务页：额外 `Measure/Arrange=0/0`。
- PageHost 最终 `Opacity=1`、`Effect=null`，没有全页 Blur；静态入口也没有 `AnimateEntrance(PageHost)`。

## 边界

证据来自合成数据、真实生产 WPF 页面和隔离 STA/offscreen logical DIP；没有启动真实 Playnite 嵌入宿主或 Worker 长任务，不把该样本写成物理 DPI、跨屏、presented frame、UIA/读屏、ETW 或宿主性能证据。Demo 原始目录不可用，本阶段沿用恢复生产基线。该提交没有页面级 XAML/响应式布局变更，因此未把 `render-qa` 结果冒充本项证据；真实 Playnite 与物理呈现仍待 R23/宿主准入阶段验证。
