# R14-07 媒体详情浏览证据

- 日期：2026-09-20
- 分支：`codex/ui-finesse-round2`
- 实现提交：`c17d9bc7`
- 任务：详情提供上一项/下一项并保留列表锚点，视频信息和图片尺寸清楚；文件移走或打不开有回退，详情关闭回原行，预览加载可取消。

## 已实现事实

- 先复用现有 `Media` 分页窗口、`SelectedMedia` 和 `SelectionAnchorResolver`；上一项/下一项只在当前已加载窗口内按稳定 `MediaId` 移动，不伪造跨页结果。
- 详情导航按钮绑定 `PreviousMediaCommand`/`NextMediaCommand`，位置摘要显示当前项与已加载窗口数量；选中项变化后通过 `MediaGrid.ScrollIntoView` 和容器 `BringIntoView` 回到列表可见行，窄布局关闭详情时还会把焦点交还原媒体行。
- 详情摘要复用 `MediaItemDto` 的媒体类型、来源、大小和采集时间；`AsyncThumbnailImage` 暴露实际解码得到的 `PixelWidth × PixelHeight`，详情显示截图尺寸。
- 视频预览保留 `MediaVideoSourceConverter` 的本地文件/格式检查；缺失、不支持格式或 `MediaFailed` 时显示“录像文件不存在、格式不受支持或无法打开”回退，不抛出列表级错误。
- 异步截图加载继续沿用 generation、`CancellationToken`、卸载/不可见取消和 Missing/Unavailable/Failed 状态；没有新增网络下载、媒体移动或真实数据写入。

## 证据与边界

- `python scripts/validate-source.py`：通过。
- `scripts/check-xaml.ps1 -ProjectRoot (Get-Location)`：24/24，通过。
- `git diff --check`：通过。
- `dotnet build tests/GameSaveCenter.Playnite.Tests/GameSaveCenter.Playnite.Tests.csproj --no-restore -c Release`：退出码 1，仅输出 0 警告/0 错误，没有可诊断错误；未写成构建通过，未签收 Release/net462 或真实 Playnite 呈现。
- 新增截图尺寸行为断言和 R14-07 源码契约夹具；当前环境没有可签收的 testhost 运行摘要，不能把断言文本等同于真实动画、焦点、跨屏或视频解码呈现。
- 详情导航的边界是当前已加载媒体窗口；跨页上一项/下一项、真实视频编解码器、Playnite/RenderHarness 宿主呈现、物理 DPI/跨屏、UIA/IME、presented frame、ETW 和宿主性能仍待验。
- Demo 原目录不可用，沿用恢复生产基线；只使用合成/fake/隔离路径。main 用户改动、`src.zip` 未碰、未合并；本批无新增需保留的 `artifacts/`/`.tmp/`。

下一可执行任务：在可用 SDK/Workload 环境补跑 R14-04/R14-05/R14-06/R14-07 定向验证，随后核对 R14-08 的既有实现与依赖。
