# R20-08 状态语气统一（2026-09-21）

## 范围与事实

- 先核对已有能力：`WorkspaceStatePresenter` 已统一提供 Loading、Empty、Error、Degraded、Offline 的状态字段与重试入口；`ActionAvailabilityHints` 已覆盖恢复、媒体、云端和远端恢复的需要操作提示；`OverviewPriorityResolver` 已提供概览单一优先状态。没有重建服务、DTO 或状态模型。
- 实际缺口是生产 Shell 概览副标题固定显示“一切运行正常”，以及少数主状态把 Worker/Rclone 当作用户解释。提交 `176183ec` 让副标题随 `OverviewPriorityTitle` 更新，并收口加载、失败、空、完成和需要操作的主文案为“发生了什么 + 下一步”；内部 `Kind`、诊断详情和必要的工具标识仍保留。
- 任务中心、维护页、媒体/云端可用性提示同步移除主状态中的内部实现术语，命令、Binding、取消/错误语义、恢复保护、选框/滚动条和有限列表路径未改。

## 验证证据

- 定向行为套件：`OverviewPriorityResolverTests`、`R02ActionAvailabilityHintTests`、`WorkspaceStatePresenterBehaviorTests`、`R07StatusBannerBudgetBehaviorTests`、`R06EmptyStateBehaviorTests`，共 `30 passed / 0 failed / 0 skipped / 30 total`。覆盖概览副标题不再宣称全局健康、优先状态标题/描述/动作非空、`Worker`/`Rclone` 负例，以及实际 WPF `WorkspaceStatePresenter` 错误 Message 的正负例。
- 相关较宽套件：`25 passed / 1 skipped / 1 failed / 27 total`；唯一失败为既有未修改的 `TaskCenterViewResponsiveTests.FailedTaskDetailsPutUserReasonBeforeCollapsedTechnicalDetails` 源断言仍寻找旧的 `SelectedTask.ErrorMessage` 绑定。另 1 条为既有 `LegacyProductionUiBaselineFact` 跳过，均未改写为本阶段通过。
- 隔离 Release source-copy 构建通过：Playnite `net462`、Tests `net472`，`0 errors / 2` 条既有 `MediaCenterView.xaml.cs:671` `CS8602` warning。`validate-source.py`、XAML `24/24`、`git diff --check` 通过。

## 证据边界与后续

- 验证只使用合成 DTO、fake 服务、隔离 WPF testhost 和隔离 source-copy/构建目录；Demo 原目录不可用，参考已恢复生产基线。未运行真实 Playnite/package-host、Worker/Named Pipe、Ludusavi/Rclone/云端、屏幕呈现、物理 DPI/跨屏、UIA/读屏、IME、ETW 或宿主性能验证，也未写真实存档、媒体、云端或诊断数据。
- 下一可执行小批量为 `R21-01` 八入口纯键盘：先核对 Q24/UIA 键盘行为与最新入口实现，保留现有命令/焦点语义；上述真实环境边界仍待验。
