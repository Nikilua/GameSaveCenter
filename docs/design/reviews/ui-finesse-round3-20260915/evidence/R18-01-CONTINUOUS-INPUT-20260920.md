# R18-01 连续输入基准（2026-09-20）

## 结论

R18-01 在当前分支 `codex/ui-finesse-round2` 的代码提交 `10bc5789` 已满足受控完成条件：在 2,000 和 10,000 项合成游戏列表上回放 30 次连续英文查询、粘贴、删除和已提交的中文 IME 查询，输出每次同步过滤更新的延迟、过滤评估次数和托管堆增量代理值；现有真实 WPF 路由的 IME composition 正/负例继续由相邻回归覆盖。

本阶段只增加内部 `GamePickerPerformanceDiagnostics` 和测试夹具，不改变可见 ViewModel 契约、游戏选框、滚动条、命令绑定、取消/错误语义、恢复保护、虚拟化或 Playnite `net462` 目标。

## 受控运行结果

| 数据集 | 连续样本 | p95 / 最大同步过滤更新 | 连续样本过滤评估 | 最大托管堆增量代理 | 粘贴 / 删除 / IME 提交 | 最终防抖刷新 / 观测耗时 |
| ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 2,000 | 30 | 4.908 / 6.121 ms | 60,000（每次 2,000） | 310,888 bytes | 1.597 / 4.752 / 1.554 ms | 1 / 43.771 ms |
| 10,000 | 30 | 12.115 / 13.813 ms | 300,000（每次 10,000） | 1,448,112 bytes | 8.296 / 30.419 / 7.400 ms | 1 / 47.293 ms |

测试断言同时确认粘贴查询只留下 1 个可见项、删除恢复完整列表、中文提交查询只留下 1 个候选，以及连续快速输入最终只产生 1 次刷新；2,000/10,000 两档均通过。R18 基准测试 `1/1`，游戏选框、键盘/IME 和防抖相邻回归 `46/46`。

## 原始样本（可复算）

以下样本按测试执行顺序记录；`filter_evaluations` 与连续查询一一对应，`managed_delta_bytes` 是同一同步刷新前后采样的非负托管堆变化。

```text
2000 search_ms=2.457,4.908,1.571,1.567,1.577,1.586,1.574,1.584,1.609,1.823,1.605,1.588,1.597,1.596,1.59,1.593,1.586,1.612,1.646,1.663,1.667,1.664,6.121,1.577,1.621,1.595,1.591,1.596,1.588,1.591
2000 filter_evaluations=2000,2000,2000,2000,2000,2000,2000,2000,2000,2000,2000,2000,2000,2000,2000,2000,2000,2000,2000,2000,2000,2000,2000,2000,2000,2000,2000,2000,2000,2000
2000 managed_delta_bytes=294872,0,310888,302720,294536,294576,302696,294528,294576,286384,286384,294576,286384,286384,294576,286384,286384,294576,286384,296256,286720,286720,0,302736,294528,294536,294576,286384,302680,302728

10000 search_ms=13.813,7.857,7.788,7.819,8.194,7.771,7.881,7.837,8.238,7.847,8.051,7.794,12.115,9.076,7.936,8.095,8.036,8.449,7.942,8.014,7.942,8.287,8.03,7.96,8.045,8.005,8.324,7.903,8.02,8.016
10000 filter_evaluations=10000,10000,10000,10000,10000,10000,10000,10000,10000,10000,10000,10000,10000,10000,10000,10000,10000,10000,10000,10000,10000,10000,10000,10000,10000,10000,10000,10000,10000,10000
10000 managed_delta_bytes=0,1439920,1439920,1448112,0,1448112,1448112,1448112,0,1448112,1448112,1448112,1448112,0,1448112,1448112,1439920,0,1439920,1439920,1439920,0,1441760,1448112,1448112,1448112,0,1448112,1448112,1448112
```

`managed_delta_bytes` 使用 net472 可用的 `GC.GetTotalMemory(false)` 前后差值，未强制 GC；它是可复算的托管堆变化代理，不是 ETW、分配调用栈、私有字节或物理呈现帧的真实分配量。0 值和重复平台值保留，不做平滑或删除异常样本。20ms 防抖延迟来自现有本地筛选路径，并由最终刷新计数与实测等待窗口共同校对；本次没有用代理帧率替代 UI 呈现性能。

IME 覆盖分两层：本基准用已提交查询文本验证 VM 候选语义；同一分支的 `GamePickerKeyboardBehaviorTests` 继续用实际 STA WPF 路由验证 composition start/update 期间 Enter 负例、`ImeProcessed`/方向键保护和 commit 后有效候选确认。因而不能把本证据写成真实 Windows IME 候选窗口、Playnite 嵌入输入链或物理键盘通过。

## 门禁与边界

- `R18ContinuousInputBenchmarkTests`：`1/1`；`GamePicker|DebouncedRefreshTests`：`46/46`。
- `scripts/validate-source.py`：通过；`scripts/check-xaml.ps1`：`24/24`。
- Release 隔离 solution：`0 errors / 2 existing warnings`，警告均为既有 `MediaCenterView.xaml.cs:664` nullable；Playnite 目标仍为 `net462`。构建目录 `.tmp/r18-01-solution` 已在构建服务器关闭后清理。
- 本批没有 XAML、主题资源或视觉树代码变更，WPF 质量基线沿用最近当前提交的静态 `0 errors / 27 warnings / 162 info`；不把它写成新的宿主呈现通过。
- 只使用合成 DTO、fake/隔离 testhost 和内存列表；未读写真实存档、媒体、用户云端或诊断目录。Demo 原目录不可用，继续参考已恢复生产基线。
- 未验真实 Playnite/package-host、Windows IME 候选 UI、最终 presented frame、物理 DPI/跨屏、UIA/读屏、ETW 或宿主性能；没有绕过被拒绝的系统跟踪权限。

下一可执行小批量：R18-02“真实 Dispatcher 基准”，先复用现有生产 Dispatcher/测试窗口，分别记录 VM 数据完成与受控窗口可见反馈的原始时间戳，不用 `FilteredCount` 代替画面延迟。
