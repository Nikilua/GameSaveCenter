# R16-05 路径编辑一致（2026-09-20）

## 结论

R16-05 已实现，状态为“已实现，待环境验证”。当前提交为 `955dc52e`，已推送到 `origin/codex/ui-finesse-round2`。

## 现状核对与改动

- 原设置页已有全量异步 `SettingsPathValidationService`、路径粘贴标准化和设置导入/导出，但六个本地工具/目录字段没有统一的浏览、当前字段校验、打开、复制入口；Rclone 云端目标是远端文本，不作为本地路径处理。
- 新增统一“路径编辑”卡片：下拉选择当前 Worker/Ludusavi/Rclone 可执行文件或存档/媒体/镜像目录，复用原有 TextBox Binding；浏览按字段类型使用文件/文件夹选择器，取消选择不改草稿。
- `SettingsPathEditorService` 以只读 `File.GetAttributes` 和目录枚举区分有效、缺失、网络/磁盘不可达、文件/目录类型错误和无权限；打开前必须通过当前字段探测，只处理当前有效路径，不回退到父目录。复制复用已有脱敏与 `ClipboardRetry`，失败保留字段并给出反馈。
- 保留 Playnite 保存/取消、命令/Binding、原全量校验、现有滚动条与游戏选框、取消/错误/恢复保护、有限列表性能和 net462；浏览只改变当前编辑草稿，不写真实存档、媒体或云端。

## 证据

- `R16SettingsPathEditorBehaviorTests` 与 `R16SettingsPathEditorSourceTests` 及既有 `SettingsPathValidationTests` 定向测试：`6/6`。行为覆盖有效文件、有效目录、缺失目录、文件误作目录；源契约覆盖四个入口、剪贴板重试、严格打开门禁和无权限只读探测。未用真实网络共享或真实用户 ACL 制造负例。
- 外部隔离 Release solution：`0 errors / 2 warnings`；两条均为既有 `src/GameSaveCenter.Playnite/Views/MediaCenterView.xaml.cs:664` 的 CS8602。定向测试使用 `--no-restore` 与隔离输出，并显式绑定 `GscBuildCommit=955dc52e`，避免源码测试跨 checkout。
- `python scripts/validate-source.py` 通过；`scripts/check-xaml.ps1`：XAML `24/24`；`git diff --check` 通过；WPF 静态检查：`0 errors / 28 warnings / 162 info`。警告和信息是现有共享主题/滚动布局基线，未把它们写成视觉通过。

## 未验证边界

- 未运行真实 Playnite/package-host 设置窗口，未宣称最终浅/深主题呈现、DPI/跨屏、UIA/读屏、IME、RenderHarness presented frame、ETW 或宿主性能通过；Demo 原目录不可用，继续以恢复生产资源基线为视觉依据。
- 未使用真实网络共享、真实用户 ACL、真实剪贴板或真实 Explorer 动作；当前证据使用合成路径、隔离目录、源码契约和受控 net462 构建。真实宿主中的 WinForms 文件夹对话框归属、Explorer 启动失败提示仍待验。
- main 工作树的用户改动、`src.zip` 和未跟踪对话框文件均未碰、未合并；本阶段 `.tmp/r16-05-*` 构建输出在提交文档前清理。

下一可执行任务：`R16-06 生效条件说明`，先核对设置字段实际消费点、保存/应用/重启边界，避免把所有字段笼统标为需重启。
