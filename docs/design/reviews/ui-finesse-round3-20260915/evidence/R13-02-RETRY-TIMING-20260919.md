# R13-02 下次重试时间证据

日期：2026-09-19
分支：`codex/ui-finesse-round2`
代码提交：`6fd22892`（`补齐云端重试时间展示`）

## 完成范围

- 复用 `CloudTransferStatusDto.NextAttemptUtc/NextAttemptLocal`，新增 `RetryTimingDisplay`，在同一文本中给出本地绝对时间和有界相对提示。
- 未来时间显示“约 N 分钟/小时/天后”；到期或系统时钟跳到未来之后显示“可立即重试”，不会显示负倒计时；没有自动重试时间显示“无自动重试”。
- 维护页详情绑定该派生文本，保留既有错误码、错误详情和队列阶段；没有新增 Dispatcher、DispatcherTimer 或每行常驻倒计时器，因此关闭页面不会留下计时器。

## 行为与构建证据

- Core `UiDisplayMappingTests`：`29/29` 通过，覆盖未来、已到期和无自动重试三态。
- Playnite `R13CloudTransferStageBehaviorTests`：`2/2` 通过，确认维护页绑定 `RetryTimingDisplay`，不再直接渲染旧的裸 `NextAttemptLocal` 时间。
- Worker `CloudTransferStateTests`：`11/11` 通过，确认既有队列持久化、重试与校验状态保持。
- 外部隔离 Debug solution 构建：`0 warning / 0 error`；XAML `24/24`；`python scripts/validate-source.py` 与 `git diff --check` 通过。

## 边界

- 构建使用当前分支源码外部隔离副本；源码型 Playnite 测试通过显式 `GIT_DIR` 绑定当前 linked worktree 身份，验证后已清理外部副本和输出。
- 未运行真实 Playnite/package-host、真实远端、物理 DPI/跨屏、UIA/IME、presented frame、ETW 或宿主性能；未写真实云端、存档、媒体或诊断数据。Demo 原目录不可用，沿用恢复生产基线。
- `.tmp/r12-07-build-final` 仍是旧阶段遗留，精确删除逐项 Access denied；本阶段没有扩大清理或强杀未知进程。

下一可执行任务：`R13-03 手动重试范围`，先核对已有单项/媒体重试入口、幂等 requestId 和部分成功语义。
