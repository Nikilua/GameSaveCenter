# R22-01 Storage 分析时间合同证据

日期：2026-09-21（Asia/Shanghai）  
代码提交：`9d319296`（基于 `47bc1d00`）  
范围：`StorageAnalysisDto` / `StorageGameRankDto` 与 Maintenance 存储分析卡片的时间入口盘点。

## 盘点结论

- `StorageAnalysisDto.CheckedUtc` 当前只由 Worker 生成并未绑定到 Maintenance 卡片；卡片显示的是容量、索引体积、目录实测、趋势和稳定 ID 导航，不存在可见的 `CheckedUtc` 或旧本地时间直显入口。
- `StorageGameRankDto.LatestBackupDisplay` 是现有兼容属性，但 Maintenance 两处排行模板均未绑定它；本批不凭“属性存在”声称页面已呈现。为后续真实绑定保留一致合同，增加 `LatestBackupRelativeDisplay`、`LatestBackupFullDisplay` 和 `LatestBackupRawUtcDisplay`，旧 `LatestBackupDisplay` 不变。
- 存储分析仍为只读统计；逻辑索引大小、备份目录实测、失联路径说明、稳定 `PlayniteId`/`BackupId` 导航、刷新命令和不删除语义均未改变。

## 受控验证

- `R22TimeDisplayBehaviorTests | R17StorageAnalysisNavigationTests`：`22/22` 通过。行为测试覆盖已有本地兼容日期、相对“前”、完整时区/UTC、原始 UTC 和未知时间负例；R17 继续覆盖失联路径不当作零占用及稳定 ID 精确导航。
- `UiDisplayMappingTests`：`40/40` 通过。
- Release 隔离构建：XAML `24/24`，Playnite/Tests `0 errors`；仅保留既有 `MediaCenterView.xaml.cs:671` 的 2 条 CS8602 warning。`validate-source.py`、`git diff --check` 通过；本批没有 XAML 变更，沿用上一子批 WPF 静态基线 `0 errors / 27 warnings / 177 info`。

## 边界与下一步

本批只使用合成 DTO、fake/隔离测试宿主和隔离构建目录，没有读写真实存档、媒体、云端或诊断，也没有修改存储卡片布局。没有把 DTO/静态绑定盘点写成真实 Playnite/package-host、最终呈现、UIA/读屏、DPI/跨屏、ETW 或宿主性能通过；Demo 原目录不可用，main 用户改动未碰、未覆盖、未合并。

R22-01 仍为“部分满足，待继续”。下一可执行小批量是盘点其他 Save/恢复入口的旧本地时间直显，优先核对现有 DTO、绑定、复制和稳定排序，再补最小范围的兼容/未知负例。
