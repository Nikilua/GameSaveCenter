# R06-04 复制单元格与整行证据

日期：2026-09-18  
任务：`R06-04 | 复制单元格与整行`  
结论：在当前可控范围内已满足；代码已提交并推送到 `codex/ui-finesse-round2`。

## 1. 现有能力核对与范围

先核对当前 checkout 的复制入口、DTO 和表格接线：

- 复用 `DashboardViewModel.CopyPathCommand`、`CopyTaskErrorCommand`、`CopyDiagnosticsCommand`、`CopyMaintenanceReportCommand` 及既有 `CopyTextWithRetryAsync`；没有把 main 的旧实现覆盖到当前分支。
- 复用现有 `TaskStatusDto`、`BackupVersionDto`、`SavePathCandidateDto`、`MediaItemDto`、`ValidationFindingDto` 和生产 DataGrid。原始 Demo `DesignShellView.xaml`/`Pages` 目录在当前 checkout 不存在，本阶段记录事实并沿用恢复的生产基线。
- 复制范围只覆盖当前生产表格的明确字段白名单，不通过反射或 `ToString()` 猜测 DTO 内容；隐藏凭据类字段不进入复制文本，已有文本复制入口也经过同一脱敏写入点。

## 2. 实现

提交：`afe4aa55a24e306bbe219da1ce76d8675325c35e`（`增加表格行列复制与凭据脱敏`）。

新增共享 `DataGridClipboardBehavior` 和格式化器，接入 Save History、Save Candidates、Task Queue、Media Inbox、Maintenance Findings 五类实际表格：

- `Ctrl+C` 复制选中行，`Ctrl+Shift+C` 复制当前单元格；表格显式使用 `ClipboardCopyMode=None`，避免 WPF 默认格式绕过白名单。
- 行复制按当前 `Items` 显示顺序输出，使用稳定 ID 去重，批量选择不会因重复对象或刷新引用产生重复行；输出为稳定 TSV，行分隔为 CRLF，单元格中的 Tab/换行有明确转义。
- 技术字段使用完整原值，长路径保留盘符、文件名和全部 payload；展示省略只影响视觉，不影响复制内容。
- 保存历史、候选、任务、媒体收件箱和维护发现分别使用显式 profile；`Authorization: Bearer`、password/token/secret/API key/private key 等凭据语法统一输出为 `[已隐藏]`。`CopyTextWithRetryAsync` 也在公共写入点脱敏，覆盖原有诊断/错误/报告复制入口。
- 保留 FullRow 选择、现有滚动与虚拟化、游戏选框、命令/Binding、取消/错误/恢复保护、有限列表和 net462 兼容；没有写真实存档、媒体、云端或诊断数据。

## 3. 自动验证

均在当前提交对应的隔离构建中执行：

- `R06ClipboardBehaviorTests`：`3/3`。实际 STA WPF DataGrid 验证显示顺序、稳定分隔符、重复 ID 去重、当前单元格完整长路径、凭据脱敏和源契约；没有调用真实 OS 剪贴板，使用内部验证 seam。
- R06-03 选中/焦点回归：独立执行 `2/2`；R06-02 排序与 R06-01 列宽回归合计 `9/9`。
- `scripts/check-xaml.ps1`：`24/24`；Release 标准构建：`0 warning / 0 error`；`python scripts/validate-source.py` 与 `git diff --check` 通过。
- 全量 Playnite testhost 的一次观察值为 `573 passed / 57 skipped / 49 failed / 679`；失败集中在既有 WPF 单进程/Application、隐藏 Window、布局/焦点隔离假设，未把该合跑结果写成绿色，也没有放宽身份门禁。相关定向测试均按独立进程/当前 SHA 重建后通过。

## 4. 视觉与受控行为证据

保留的最新渲染证据：`.tmp/r06-04-render-final/render-qa-report.txt`。

- 报告绑定代码提交 `afe4aa55a24e306bbe219da1ce76d8675325c35e`，`WorkingTreeClean=True`，Light/Dark 双主题共 357 张 PNG。
- 覆盖 50/400/2000/4468 数据量、纵向/横向滚动、虚拟化以及 `2560×1440 → 1100×720 → 2560×1440` resize；报告结果为 `render-qa OK`。
- 已人工抽查 Light/Dark Task、Light Media、Dark Maintenance 代表图：选中/失败行色层、徽章、危险动作、表头和滚动区域仍可辨识，复制入口帮助文案未破坏表格布局。
- `DpiScale=1.00` 仅代表离屏 logical DIP；渲染报告不是真实呈现帧或物理 DPI 证据。

## 5. 未验边界与下一步

本阶段未启动真实 Playnite 宿主，也未验真实鼠标/键盘选择、OS 剪贴板、UI Automation、屏幕阅读器、IME、物理 DPI/跨屏、呈现帧、ETW 或宿主性能。`SelectionMode=Extended`、快捷键与 Tooltip 已由源码和隔离 WPF 行为覆盖，但不替代真实宿主输入链；稳定 TSV 的转义语义是当前实现契约。

本阶段清理了 `.tmp` 中未被当前证据引用的旧构建/审计/渲染目录，仅保留 `r06-03-render-final` 与 `r06-04-render-final` 当前证据目录。

下一可执行任务：`R06-05 列头说明`。先盘点生产表头、单位、排序/复制字段、现有 Tooltip/Automation 文本和可复用资源，再决定是否需要共享表头说明能力。
