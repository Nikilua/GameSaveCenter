# R10-02 当前游戏定位证据（2026-09-19）

## 结论

R10-02 已由现有能力和 R10-01 的任务入口满足；本阶段只补行为/接线证据，没有另建游戏选择器或按名称的定位路径。行为夹具提交为 `3c258873`（`补充当前游戏定位重名校验`），任务入口实现来自 `97770ed4`。

## 现有能力核对

- 任务详情的“查看关联游戏”命令读取 `SelectedTask.GameId`，在当前 `Games` 快照中按 `PlayniteId` 精确解析；找不到 ID 时不替换当前游戏并提示刷新。
- 媒体页沿用现有当前游戏 Shell：`GameContextButton` 显示 `SelectedGame.Name`/平台并打开 `GamePicker.ItemsView`；媒体页加载、重载和媒体操作均以 `SelectedGame.PlayniteId` 为请求身份，不以游戏名作为路由键。
- `GamePickerViewModel.SelectGame` 和 `SetItems` 已按大小写不敏感的 `PlayniteId` 保持选择；即使两个 synthetic 游戏显示同名，也不会选中列表中第一个同名对象。

## 验证

- `R10ContextGameBehaviorTests` `2/2`：真实 `GamePickerViewModel` 重名选择行为 `1/1`；任务/媒体/Shell 生产接线复核 `1/1`。
- R10-01、告警路由和页面接线相邻回归合计 `15/15`：R10Navigation `2/2`、PurposeNavigation `3/3`、FindingNavigationResolver `8/8`，R10-02 `2/2`。
- `scripts/build.ps1 -SkipTests -OutputRoot .tmp/r10-02-build`：Release solution `0 warning / 0 error`，Playnite `net462`；XAML `24/24`。`validate-source.py`、`check-xaml.ps1`、`git diff --check` 通过。

## 边界与未验事实

- 使用 synthetic `GameStatusDto`、隔离 testhost 和生产文件接线；没有读取或修改真实存档、媒体、云端、用户配置或诊断数据。
- 未在真实 Playnite 中操作任务/媒体详情和选框，未宣称最终 presented frame、物理 DPI/跨屏、UIA/读屏、IME、ETW、宿主性能或 package-host 通过；Demo 原目录不可用，沿用恢复生产基线。
- 用户提供的 `DEV-INSTALL-008` main checkout 全量日志仍独立记录为 `73 failed / 588 passed / 57 skipped`；编译、Core、Worker 通过但 WPF testhost/资源/布局/时序类失败混合，未在 dirty main 上覆盖或重跑安装器。

下一可执行任务：R10-03；真实 Playnite 中的定位操作和呈现边界仍未验。
