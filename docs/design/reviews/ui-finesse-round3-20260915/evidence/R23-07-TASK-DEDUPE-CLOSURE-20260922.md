# R23-07 任务去重与收尾

日期：2026-09-22  
分支：`codex/ui-finesse-round2`  
审计基线：`91a008fb`（R23-06 文档收口前，工作树干净）  
任务：R23-07「任务去重与收尾」

## 结论

本项只做 Q/R 两套队列的逐行核对、状态分类和证据入口整理，没有新增业务代码、服务、DTO、命令或测试夹具。当前账本仍把“已有实现”“受控条件满足”“真实宿主待验”和“外部阻塞”分开记录；没有把历史 Q 账本的已存在实现直接升级为第三轮最终验收。

## 第三轮 192 行分类

`ROUND3_PROGRESS.md` 当前正好包含 R00–R23 共 24 组、每组 8 行，即 192 行。下表按账本的实际状态统计；R23-07 本项已纳入“已满足”，R23-08 仍是唯一待开始项。

| 收口类别 | 数量 | 账本状态/范围 | 处理原则 |
| --- | ---: | --- | --- |
| 已验收 | 112 | `已满足` | 有当前实现和对应行为/证据定位；不再重复重建 |
| 代码完成 | 29 | `已实现，待环境验证` | 保留当前实现，下一次只补适用的环境证据 |
| 受控条件满足、环境待验 | 45 | `已满足，待环境验证` 44 行 + `已满足，待宿主环境验证` 的 R23-06 1 行 | 包含隔离 WPF/逻辑 DIP/合成数据或隔离包安装；不写成真实宿主通过 |
| 外部阻塞 | 1 | R02-06 | Playnite 宿主拥有菜单 visual tree，保留具体待验入口，不用静态契约替代 |
| 部分满足 | 3 | R22-01、R23-04、R23-05 | 保留未完成的时间边界、runner/UIA/manifest 边界和几何/真实呈现帧边界 |
| 不适用 | 1 | R05-05 | 按任务实际适用性记录原因，不强行补造性能结论 |
| 未实施 | 1 | R23-08 | 留作下一可执行任务；不以本项的统计报告代替准入清单 |

## 第二轮 Q 队列交叉核对

`ui-finesse-round2-20260913/ROUND2_PROGRESS.md` 的 Q00–Q25 共 208 行、26 组。原账本维度统计为：代码 `128` 行“代码完成”、`80` 行“已复核”；自动 `207` 行“通过”、`1` 行“待验”；视觉 `155` 行“通过”、`50` 行“待验”、`3` 行“不适用”；宿主 `4` 行“通过”、`8` 行“待验”、`195` 行“外部阻塞”、`1` 行“不适用”。最终列只有以下 5 行为“已验收”，其余 203 行仍为“未完成”：

- Q00-06 旧账本复审
- Q00-07 证据身份索引
- Q00-08 干净交付路径
- Q25-07 源码包宿主一致
- Q25-08 持续交付收尾

因此 Q 队列的自动/视觉通过不能被 R23 汇总成 208 项完成；尤其 Q20 页面宿主、Q24 跨屏、Q25 真实呈现帧和 ETW 边界继续由原 Q 证据与第三轮 R23-04/R23-05 承接。

## 已复用的证据入口

- R00/R01 的按压合成、变换复用、动画生命周期、搜索/IME、源码根绑定、数字裁切、负例和证据新鲜度，直接复用第三轮逐行证据；本项没有以新的字符串断言覆盖已有行为门禁。
- R12-01 至 R12-08 已有独立恢复流程、校验、目标核对、保护备份、远端进度、冲突、预览失效和结果报告证据；R12-04 不重复实施。
- R21-04/R21-05/R21-06/R21-07 的 UIA 名称、状态播报、可选技术文本和焦点证据继续按行为与真实未验边界拆分，不把 `Visibility=Collapsed` 或 Automation HelpText 直接升级为整页交互通过。
- R23-04、R23-05、R23-06 分别保留非空隔离宿主 runner、Rendering 代理/几何失败与包身份/隔离安装回退证据；R23-06 的包安装事实不覆盖当前真实宿主呈现。

## 仍待执行的真实边界

- 下一可执行任务是 R23-08 下一轮准入清单；它必须只保留可复现问题、用户价值、优先级、成本和证据入口。
- R23-04 仍缺 UIA 侧栏定位后的 `summary.json`/外层 manifest 收口；R23-05 仍有 5 项几何失败，且没有 ETL、PresentMon 或真实 DWM presented-frame 样本。
- R02-06 的 Playnite 原生菜单状态、Q24-03 的第二物理显示器跨屏、真实 Playnite UIA/读屏/IME/物理 DPI 仍是外部验收边界；不绕过被拒绝的系统跟踪权限。
- 本项未修改 main、真实 Playnite Extensions、真实存档、媒体、用户云端或外发诊断；主分支合并留到整轮验收条件具备后再做。

