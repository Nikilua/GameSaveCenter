# R14-06 批量目标防误选

- 日期：2026-09-20
- 分支：codex/ui-finesse-round2
- 代码提交：cfbb1279
- 任务：归类到游戏时展示封面/图标、平台和唯一对象信息；重名游戏可区分；过滤后已选目标不悄悄变化。

## 现有能力核对

- 全局 GamePickerViewModel 已使用 PlayniteId 作为缓存和选择恢复键；搜索、状态、平台筛选只改变 ItemsView，不会把被隐藏的 SelectedItem 换成第一个可见项。已有“当前选择不在当前筛选结果中”和“显示当前游戏”恢复入口。
- 媒体归类已有 Games、InboxTargetGame、MediaTargetGame、预览项 TargetPlayniteId 和真实归类命令；本阶段没有增加第二套目标服务或更改命令参数。

## 本阶段实现

- GameDescriptorDto/GameStatusDto 新增只读 IconPath，Playnite 适配器通过现有 Database.GetFullFilePath(game.Icon) 解析已经存在的本地引用；缺失、远端引用或异常返回空值，不下载图标。
- GameStatusDto.IdentityDisplay 和 GamePickerItem 投影提供稳定 Playnite ID 显示。全局游戏选框显示图标，空图标回退首字母；媒体批量归类、归类预览目标和重新归类目标复用 MediaGameTargetTemplate，显示名称、平台、图标/占位和稳定 ID。
- 目标控件继续使用 SelectedItem 或预览目标的稳定 TargetPlayniteId，没有 SelectedIndex 或按名称选目标；SnapshotComparers.Game 和策略克隆保留图标字段。

## 证据与验证

- 新增 PickerItemsExposeStableIdentityAndOptionalPlayniteIcon 行为夹具，已有重复名称按稳定 ID 选择及筛选隐藏保留测试继续覆盖选择语义。
- 新增 ClassificationTargetsShowVisualAndStableIdentityWithoutIndexSelection 源码契约夹具，检查三个媒体目标入口、模板字段和无 SelectedIndex；它不是运行时呈现证据。
- python scripts/validate-source.py：通过。
- scripts/check-xaml.ps1 -ProjectRoot (Get-Location)：24/24 通过。
- git diff --check：通过。
- 定向 dotnet test ... --filter GamePickerViewModelTests|R14ClassificationSelectionTests|R10ContextGameBehaviorTests 连续约 60 秒无输出，终止当前会话；未产生可签收的 build/testhost summary，不写成通过。Release/net462、Playnite 宿主和 RenderHarness 未运行。

## 边界

- 仅使用合成 DTO、fake/隔离源码和本地可重现的静态检查；未读取或写入真实存档、媒体、用户云端或外发诊断。
- Demo 原目录不可用，视觉基准沿用恢复生产基线。静态/XAML/夹具不等价真实 Playnite 嵌入、物理 DPI/跨屏、UIA/读屏/IME、presented frame、ETW 或宿主性能。
- main 上的用户改动和 src.zip 未碰、未覆盖、未合并；本阶段没有留下新的 artifacts/.tmp。

下一可执行任务：在可用 SDK/Workload 环境补跑 R14-04/R14-05/R14-06 定向验证；随后推进 R14-07 媒体详情浏览。
