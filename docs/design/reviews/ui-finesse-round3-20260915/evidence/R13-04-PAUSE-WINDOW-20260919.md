# R13-04 暂停与允许时段证据

日期：2026-09-19  
代码提交：`cca3f052`（已推送到 `codex/ui-finesse-round2`）

## 实现事实

- 复用 `WorkerOptions.CloudUploadQueuePaused`、允许时段字段、`CloudRetryService` 与现有设置页开关；暂停仍只影响后台自动重试，恢复后继续处理持久化队列。
- `CloudTransferSummaryDto.QueueControlDisplay` 的实际分支顺序为：用户暂停、允许时段外、`TotalCount <= 0` 的“队列空闲”、存在条目时的“自动队列运行中”。
- 设置页说明“恢复开关后会继续处理已保存队列”，并明确允许时段修改“在下一轮 Worker 检查时生效；已开始的上传不会被取消”。
- 源代码复核确认 Worker 在暂停时跳过自动 sweep、在允许时段外将到期条目持久化 defer；本项没有把源代码复核写成真实云端运行时证明，也没有加入会取消进行中上传的路径。

## 实际验证

- Core 队列摘要状态优先级定向测试：`1/1`。
- Worker 允许时段策略两项与 `CloudQueueWindowSettingsAreClampedAndRoundTrip` 暂停/边界持久化测试：`3/3`。
- Playnite `R13CloudTransferStageBehaviorTests`：`9/9`；既有 `PortableSettingsTests`：`10/10`。
- 外部隔离 Debug solution：`0 warning / 0 error`；Playnite 目标保持 `net462`；XAML structural validation：`24/24`；`scripts/validate-source.py` 与 `git diff --check` 通过。
- 本阶段隔离源码与构建输出已清理；没有修改当前工作区的旧 `.tmp/r12-07-build-final`。

## 边界

证据使用合成 DTO、fake/隔离设置与外部隔离构建；没有连接真实云端、真实存档、真实媒体或发送诊断。未验真实 Playnite/package-host、真实远端和进行中上传时序、UIA/读屏、OS 输入/IME、物理 DPI/跨屏、presented frame、ETW 与宿主性能。Demo 原目录不可用，本轮沿用恢复生产基线。main 工作树现有 R08 用户改动和 `src.zip` 未触碰、未合并。

下一项：`R13-05 远端证据详情`，核对远端验证/脱敏/“已上传不等于已验证”的详情入口。
