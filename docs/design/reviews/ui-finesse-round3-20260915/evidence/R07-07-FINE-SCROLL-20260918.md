# R07-07 触控板小增量

## 结论

R07-07 在当前分支已满足可控验收条件。最新代码提交为 `98e8b244`。核对后复用既有 `ScrollBoundaryRoutingBehavior`，只修正最终不可滚动边界：没有任何可移动的内层或外层时收口无效滚轮事件，避免 WPF 为每次无位移事件重复安排布局；存在可移动外层时仍按滚轮增量转发，不吞有效边界滚动。

## 行为与数据证据

当前身份隔离 Release shadow 输出为 `r07-07-build-98e8b244`；XAML 结构检查 `24/24`，solution 编译 `0 warning / 0 error`，Playnite 目标为 `net462`。因当前 worktree 的 WPF 标记编译临时项目写入受到环境限制，本次 bin/obj 放在已授权的外部隔离目录，源码快照来自 `98e8b244`，未写入 main 或仓库临时目录。

- `R07FineScrollBehaviorTests 2/2` 使用真实 WPF `DataGrid` 内部 `ScrollViewer`、隔离 STA Window 和合成 160 行：12 次 `-30` 小增量得到 offset `0,3,6,...,36`，未出现反向跳动；3 次反向 `+120` 得到 `33,30,27`；末端稳定在 `154/154`。
- 同一小增量序列每次观察实际可见 `DataGridRow` 容器，数量为 `6–9`；20 个合成滚轮事件的 `LayoutUpdated` 总数为 `17`，末端连续 5 个无位移事件的布局增量为 `0`。
- 加速边界场景使用嵌套 `ScrollViewer`：单个 `-360` 事件把外层 offset 从 `32` 推进到 `80/432`，内层保持 `628`，不越过外层最大值，事件处理后可见内容容器数为 `2`，布局事件数为 `1`。
- 既有 `R07ScrollOwnershipBehaviorTests 2/2` 仍通过；当前身份按 `FullyQualifiedName~R07` 的 R07 回归为 `13/13`，没有跳过。`python scripts/validate-source.py`、`git diff --check` 通过。

生产行为仍保持：内层沿当前方向可滚动时交给 WPF 原生处理；到边界且最近外层可移动时转发；仅在无可达滚动目标时收口无效事件。没有修改游戏选框、滚动条系统、`CanContentScroll`、虚拟化、命令/Binding、取消/错误/恢复保护或有限列表契约。

## 验收边界

测试使用合成行数据、真实生产滚动行为、隔离 STA WPF 和 offscreen logical DIP。`-30`/`+120`/`-360` 是 routed wheel 事件夹具，不是物理触控板轨迹；未启动真实 Playnite，不宣称触控板设备、OS 输入栈、物理 DPI/跨屏、UIA/读屏、presented frame、ETW、宿主帧率或性能。没有写真实存档、媒体、云端或诊断数据。Demo 原始目录仍不可用，继续沿用恢复生产基线。

下一项可执行任务：R07-08 resize 压力序列。R07-07 尚未验证真实宿主触控板设备和系统级性能计数器。
