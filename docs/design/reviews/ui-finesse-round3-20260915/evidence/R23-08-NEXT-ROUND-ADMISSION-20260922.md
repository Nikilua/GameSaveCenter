# R23-08 下一轮准入清单

日期：2026-09-22  
分支：`codex/ui-finesse-round2`  
审计基线：`4ad8bd17`（R23-07 任务去重收口）  
任务：R23-08「下一轮准入清单」

## 结论

本项只整理已经复现、具有明确用户价值或受具体环境阻塞的下一步，不新增页面体系，不把“还可以更精致”的想法加入待办。每一项都保留现有证据、依赖、成本和不可越过的边界；没有证据的候选不进入下一轮。

## 准入事项

| 优先级 | 入口 | 纳入理由与现有复现 | 预计成本 | 前置与完成定义 |
| --- | --- | --- | --- | --- |
| P0 | R23-04 runner UIA/summary/manifest 收口 | 当前真实 Playnite 隔离运行已取得非空 `Game` 集合和嵌入原图，但 UIA 未找到侧栏项，缺 `summary.json`/外层 `capture-manifest.json`；失败路径可复现且直接影响当前候选宿主证据 | 中 | 复用 `real-host-audit.ps1`、当前隔离 profile、当前包身份；先定位 UIA/窗口生命周期和 summary 生成，不替换成旧包或离屏结论；成功必须绑定当前 commit 并区分 Embedded/独立窗口 |
| P0 | R23-05 shell/media 五项几何失败 | `shellqa` 在 980/1040 header actions 和 Media 1040/1100/1366 inbox/batch row 间距上稳定报告失败；这是当前可重复的布局问题，不是性能数字推断 | 中 | 先用共享壳层/页面布局和真实几何负例定位；修复后重跑三尺寸/双主题 geometry gate，并保留当前游戏选框、滚动条、命令/绑定和列表性能 |
| P1 | R22-01 残余时间入口盘点 | R22-01 已覆盖主要页面和 DTO，但账本仍明确有概览/最近访问等残余 `ToLocalTime` 入口待逐项核对；统一相对/完整/原始时间对用户理解有直接价值 | 小—中 | 先查实际绑定入口和现有时间投影，只有发现真实不一致才改；补刚刚/未知/时区/系统时钟跳变负例，不改 SaveHistory/报告/日志的稳定完整时间语义 |
| P1 | R02-06 Playnite 原生菜单宿主回放 | 插件可控 `GameMenuItem` 已有 0/6 项行为证据，但禁用/勾选/危险/子菜单/快捷键/翻转由 Playnite visual tree 拥有，外部阻塞真实存在 | 中—高 | 仅在隔离 Playnite、合成游戏和非写入动作下回放；需真实 UIA/键盘/关闭序列证据，若宿主不暴露能力则保留阻塞，不凭本地菜单静态断言签收 |
| P1 | R23-06 当前候选真实宿主复测 | 包身份、zip/pext、隔离安装与目录回退已通过，但证据原图仍绑定旧的 `5b5d6305`；当前 `57754b33` 候选需要重新绑定宿主身份后才有资格比较 | 中 | 沿用隔离宿主流程，不触碰真实 Extensions/存档/云端；必须以当前六份程序集身份、当前 package 和 metadata 绑定截图/summary，不能只复用旧图 |
| P2（环境） | Q24-03 第二物理显示器跨屏 | 当前拓扑只有 `DISPLAY1`，`blocked-single-display` 是具体事实；Popup/字体/焦点/资源释放的跨屏清单已存在，用户价值明确但本机无法执行 | 中—高（环境） | 取得第二物理显示器后，在隔离宿主打开 Popup 并实际迁移窗口；记录物理位置、字体清晰度、焦点、主题资源释放和回迁，不把逻辑 DPI 模拟当物理跨屏 |
| P2（权限） | R23-05 真实 presented frame/停顿采样 | ETW/WPR/xperf 受到系统拒绝，当前只有 WPF Rendering/Stopwatch 代理；真实 DWM/PresentMon 数据会影响性能结论，但不能以权限绕过方式取得 | 高（外部） | 仅在获得合规采集权限和可复现实机环境后执行；否则保留代理与未验分账，不报告物理刷新率、掉帧率或停顿调用栈 |

## 不准入事项

- 不重复生成与当前 R23-02/R23-03 相同的离屏截图，也不把 `Assert.Contains`、`Visibility=Collapsed`、Automation HelpText 或逻辑 DIP 几何再次包装成真实交互通过。
- 不因任务表中写了“新增功能”就新建已有服务/DTO/命令；先查现有恢复、任务、时间、菜单和安装能力，只有出现可复现缺口才进入实现批次。
- 不绕过 ETW/系统跟踪权限，不把代理性能或离屏截图写成物理跨屏/真实呈现；不使用真实用户存档、媒体、云端或对外诊断作为验证夹具。
- 不在本轮未满足真实宿主边界前合并 main；当前 main 的用户改动与旧失败事实继续独立保留。

## 下一执行顺序

先处理 P0 的 R23-04 runner 收口；若宿主 runner 仍受外部窗口/UIA 条件阻塞，则转做 R23-05 五项几何的独立小批量。R22-01 只在盘点确认残余真实入口后改动，R02-06/R23-06 继续沿用隔离宿主流程；Q24-03 和真实 presented-frame 采样等待相应硬件/权限。

