# R16-02 策略差异预览证据

日期：2026-09-20  
实现提交：`b327d5ef`（`补齐策略差异预览`）  
分支：`codex/ui-finesse-round2`

## 实现事实

- 复用现有 `BackupPolicyDto`、`BackupPolicyTemplateDto`、`BackupPolicyTemplateCatalog`、Worker 策略 IPC 和 `DashboardViewModel` 的保存/刷新基线；新增 `BackupPolicyDiff`，对 13 个策略字段生成有界差异项，沿用既有策略归一化规则。
- `BackupPolicyDto` 增加字段变更通知，保存卡显示“当前已保存/继承基线”与“显式草稿值”；模板卡显示应用前基线与模板将覆盖值。模板仍明确是一次性复制，不建立实时继承关系。
- “取消未保存修改”只把保存基线复制回当前草稿，不调用 `RequestAsync`；保存仍走原 `SavePolicyAsync`，仅提交归一化后的预期策略字段。已有游戏草稿未保存时，模板应用命令保持禁用，避免覆盖本地草稿。

## 自动与受控行为验证

- 外部隔离源码副本 `D:\workplace\github\GameSaveCenter\.tmp\r16-02-source` 执行 Release solution 单节点构建：`0 errors/7 warnings`。7 条均为当前离线环境无法访问 NuGet 漏洞源的 `NU1900`；Playnite 目标仍为 `net462`。
- `BackupPolicyTemplateCatalogTests`（含差异、复制回退和字段通知）独立 testhost：`5/5`。
- `R16PolicyDiffSourceTests`：`1/1`；源码负例确认本地取消方法体没有 `RequestAsync`，并确认未保存游戏草稿时模板应用命令受保护。
- `python scripts/validate-source.py` 通过；XAML 结构门禁 `24/24`；`git diff --check` 通过；WPF 技能静态检查 `0 errors/28 warnings/177 info`。警告/信息为现有 Canvas、滚动容器和主题色启发式提示，本批未新增静态错误。

## 公共门禁与边界

- 保留当前游戏选框、滚动条系统、命令/Binding、取消/错误语义、恢复保护、有限差异列表性能和 Playnite `net462` 兼容；没有修改真实存档、媒体、云端或用户诊断路径。
- 证据只来自合成 DTO、fake/现有服务契约和外部隔离构建目录；没有执行真实 Playnite/package-host 中的最终呈现、浅深主题屏幕帧、DPI/跨屏、UIA/读屏、IME、ETW 或宿主性能验证。Demo 原目录不可用，沿用恢复生产基线；linked worktree 的 WPF `obj` 仍受 `Access denied` 影响。
- main 工作树中的用户改动和 `src.zip` 未碰、未合并；外部隔离目录在本阶段结束后清理。

下一可执行任务：`R16-03 模板应用范围`，先核对现有模板 DTO、应用命令和一次性复制边界，再补目标范围/取消与负例证据。
