# 第三轮：192 项质量、控件与功能实施任务

日期：2026-09-15。审查主基线 `35b82761c60b8ec8a3fedc283545e670bdf05127`，位于 `codex/ui-finesse-round2`；文档发布在 main，不能把 main 旧代码当成已实现的新基线。

用户要求继续检查已完成质量并扩充功能/UI。本文件包含 **24 组 × 8 项 = 192 个任务**。它是第二轮 208 项之后的增量与纠错队列，不是 192 个已经确认的缺陷；缺陷依据见[本轮质量审查](UI_FINESSE_QUALITY_REVIEW_2026-09-15.md)。R00 优先处理已发现问题及一个待验证输入风险，R01 补验收底座，其余以现有能力为基础增强。旧 Q 任务仍保留，不被本文件抹掉。

## 可直接交给实施 Agent 的总提示词

```text
请持续执行 docs/design/UI_FINESSE_ROUND3_192_TASKS_2026-09-15.md，并先阅读同目录质量审查和第三轮账本。
按 AGENTS 启动协议检查状态、记忆、日志、交接、git status/log。使用你现有的 codex/ui-finesse-round2 工作区接续，核对最新代码，不把 main 的旧实现覆盖到当前分支。
先收口正在修改的阶段，再做 R00/R01 的问题修复与证据校正，之后推进依赖已满足的 Q/R 任务。用户已要求持续实现，不必每个阶段再询问是否继续。
Demo-first 是视觉基准，WPF 技能用于质量检查。保留当前游戏选框与滚动条系统、Playnite/net462 兼容、命令绑定、取消/错误语义、恢复保护和有限列表性能。Demo 原目录不可用时记录事实，参考已恢复生产基线，不自行换成新的设计体系。
逐项先查已有能力：已存在的服务和 DTO 优先复用。若最新提交已实现本任务，补实际证据并记“已满足”，不要重建。表格中的新增功能是实施方向，不是对当前代码缺失的断言。
每项完成定义由组说明、表格具体条件及公共门禁共同组成。不能只添加 Assert.Contains 就签收交互、动画、焦点或性能；先确认原断言覆盖了什么，再补行为/负例。
常规阶段按实现→相关构建/测试→适用视觉或行为验证→同步账本/记忆/日志→中文 commit 和 push→下一项推进。每次只处理边界明确的小批量，避免 192 项形成一个巨型提交。
遇到外部阻塞，完成独立可做的代码、夹具和具体待验步骤，记录真正限制，继续其他依赖已满足任务。不要绕过被拒绝的 ETW/系统跟踪权限，不把代理性能或离屏截图写成真实呈现/物理跨屏。
业务验证使用合成数据、fake 服务和隔离目录；计划不授权修改真实存档、删除真实媒体、写用户云端或对外发送诊断。沿用当前已授权的隔离宿主流程；新高影响操作先准备具体可审阅结果。
保持用户文件与其他任务改动。提交文档时只同步本阶段事实，及时清理自己产生且未被证据引用的 artifacts/.tmp。停止前必须留下下一可执行任务与真实未验边界。
```

## 执行顺序与证据要求

- **第一批**：R00、R01。R00-08 是输入风险验证，未复现时不得宣称缺陷。已修正的入场中途接管（c76ce62）和宿主令牌解析（8dfe7fa）不重复重写；R00-03 检查的是终态和清钟。
- **第二批**：R02–R09 的控件精修，可按受影响控件与未完成 Q 任务合并实现，分别记账，不重复增加完成数。
- **第三批**：R10–R17、R20、R22 的功能体验；依赖相关服务和安全状态已就绪。先做只读/预览、再做有真实写入效果的交互。
- **贯穿执行**：R18 性能、R19 竞态、R21 辅助功能；根据变化范围选择验证，不要求每次文案编辑重复全套压力测试。
- **收口**：R23 的宿主/性能项只有相关新代码稳定后才签收。审查期间后续 `4414f05` 已补动画完成/卸载清理；R00-03 先复核该提交行为，不能重复重写已修正部分。

优先级：P1 表示影响结果可信度、基本可操作性或安全语义，P2 表示明显体验收益。P1/P2 组按具体影响排序；发现可复现的数据损坏/误操作应立刻升级。无需为了凑数量制造功能。

公共验收：UI 变更用真实生产样式验证浅/深主题、正常/窄/短内容区、焦点/禁用/错误/加载组合；至少一次反向/失败路径。交互使用实际事件或明确受控行为探针；源码测试只证明结构。性能保留环境、数据规模、原始样本、统计口径；真实宿主、受控 WPF、离屏和物理跨屏分账。无需增加只镜像实现的低价值测试。

路径说明：下表 `Infrastructure/`、`Views/`、`Themes/`、`Settings/`、`ViewModels/` 相对 `src/GameSaveCenter.Playnite/`；`Worker/` 相对 `src/GameSaveCenter.Worker/`；测试类位于 `tests/`。使用 rg 查找实际路径；文件已重命名时按符号定位，不新建重复模块。

状态统一使用：待开始、实施中、代码完成待验收、已验收、已满足、外部阻塞、不适用。每项记录实现 commit、自动验证、视觉/交互、宿主/性能、证据链接和下一步。外部阻塞不能替代可在本机完成的工作。

## 任务总览

| 组 | 主题 | 类型 | 优先级 | 衔接 |
| --- | --- | --- | --- | --- |
| R00 | 复核问题优先收口 | 修复/复核 | P1 | Q03、Q04、Q12、Q18、Q25 |
| R01 | 证据与质量门禁 | 验证增强 | P1 | Q00、Q01、Q02、Q24、Q25 |
| R02 | 按钮与上下文动作精修 | 交互增强 | P2 | Q04、Q05、Q15 |
| R03 | 中英文排版与信息密度 | 视觉增强 | P2 | Q01、Q02 |
| R04 | 输入框、校验与编辑体验 | 交互增强 | P2 | Q06、Q07、Q23 |
| R05 | 选择器、弹层与焦点 | 交互增强 | P2 | Q08、Q09、Q10、Q15、Q24 |
| R06 | 表格与列表专业化 | 交互/视觉增强 | P2 | Q11、Q12、Q19、Q20、Q22 |
| R07 | 滚动与响应布局 | 布局增强 | P2 | Q11、Q12、Q19、Q20、Q24 |
| R08 | 动效节奏与生命周期 | 动效增强 | P2 | Q18、Q25 |
| R09 | 主题、图标与像素细节 | 视觉增强 | P2 | Q03、Q13、Q14、Q24 |
| R10 | 导航与效率功能 | 功能增强 | P2 | Q05、Q19、Q24 |
| R11 | 备份历史与版本比较 | 功能增强 | P2 | Q19；既有 BackupDiff/RetentionPreview |
| R12 | 恢复流程的可理解性与保护 | 功能增强 | P1/P2 | Q19；既有 RestoreReadiness/RestoreOrchestrator |
| R13 | 云队列与同步状态 | 功能增强 | P2 | Q22；既有 CloudTransferCoordinator/CloudRetry |
| R14 | 媒体归类与批量操作 | 功能增强 | P2 | Q20；既有 MediaClassification/MediaSync |
| R15 | 任务中心与长操作反馈 | 功能增强 | P2 | Q16、Q17、Q22 |
| R16 | 设置、策略与配置体验 | 功能增强 | P2 | Q23；既有策略模板/设置验证 |
| R17 | 维护与诊断可操作性 | 功能增强 | P2 | Q22；既有 DiagnosticsPackage/Integrity/Retention |
| R18 | 性能测量与资源预算 | 性能增强 | P1/P2 | Q25；既有大库/缩略图/分页基准 |
| R19 | 异步竞态与故障恢复 | 可靠性增强 | P1/P2 | 既有 L25–L28；Q17、Q20、Q22、Q25 |
| R20 | 概览、空态与首次使用 | 功能/视觉增强 | P2 | Q17、Q19、Q22；既有 OverviewPriorityResolver |
| R21 | 键盘与辅助功能闭环 | 可访问性增强 | P1/P2 | Q24；现有 UIA/KeyboardFocus 测试 |
| R22 | 精细交互与一致性补充 | 交互增强 | P2 | Q05、Q06、Q15、Q16、Q19–Q23 |
| R23 | 发布收口与持续维护 | 交付验证 | P1 | Q00、Q24、Q25；所有本轮已实施任务 |

## R00 复核问题优先收口

类型：修复/复核；优先级：P1；旧任务衔接：Q03、Q04、Q12、Q18、Q25。

主要入口：Infrastructure/AdaptiveThemePaletteContrastGuard.cs；Infrastructure/GscMotion.cs；Themes/WpfUiProduction.xaml；Views/MediaCenterView.xaml.cs；LargeLibraryPerformanceTests.cs。所有条目继承公共验收，先查最新实现再动代码。

| ID | 任务 | 实施要求 | 具体完成条件 |
| --- | --- | --- | --- |
| R00-01 | 按压合成算法 | 修正整组 Opacity 对文字和 chrome 的共同合成，保留真实父背景；支持非等距渐变 stop | 黑底白按钮黑字、opacity=0.5 时输出灰背景和黑字；同时覆盖 hover+pressed+focus，不用字符串断言代替计算 |
| R00-02 | 组合缩放复用 | 为已有 Translate/Rotate/TransformGroup 的控件复用本实例 ScaleTransform，禁止每次包新 group | 连续调用 1000 次，变换树深度和节点数稳定；既有平移旋转和两实例隔离仍正确 |
| R00-03 | 动画结束基值 | 区分中途接管、完成、取消和卸载，清理时钟后保留可见的稳定终态 | 完成后清钟仍 opacity=1、位移=0；中途重入无跳起点，取消无透明残留；在 Dispatcher 上采样 |
| R00-04 | 搜索基准真实性 | 每个样本都触发不同搜索；预热和正式等待使用独立运行中的超时计时器 | 30 次输入全部引起真实集合变化；超时负例有限时间退出；输出原始样本与 p50/p95/max |
| R00-05 | 上下文禁用透明度 | 解决 ContextButton 外层 0.48 与 ButtonChrome 0.72 叠乘；按语义确定单一禁用方案 | 真实派生样式覆盖存档/媒体上下文动作；标签、图标、解释文字在双主题下可读且无行跳动 |
| R00-06 | 媒体四行门禁 | 根据实际表头、行高、边框和水平条计算可读区；修正 212 DIP 与 42+4×52 不一致 | 主表至少四个完整可见行或明确短窗降级；断言行与有效裁剪区域交集，不能只断言 ActualHeight≥212 |
| R00-07 | 审计排除项收窄 | 用工具栏用途和滚动可达性判别替换 TrainerToolsSettingsScrollViewer 整棵子树排除 | 正常长表单不误报；在同祖先注入超宽/不可达工具条仍报错；记录排除理由与几何数据 |
| R00-08 | 搜索框 Enter/IME | 核查选择器 PreviewKeyDown 用已有 SelectedGame 关闭弹层的逻辑，明确确认对象和组合输入行为 | 无结果 Enter 不确认旧游戏；中文候选确认不关闭弹层；方向键、Enter、Esc 和焦点返回覆盖真实事件 |

## R01 证据与质量门禁

类型：验证增强；优先级：P1；旧任务衔接：Q00、Q01、Q02、Q24、Q25。

主要入口：tests/GameSaveCenter.RenderHarness/Program.cs；UiAudit/UiLayoutAnalyzer.cs；tests/GameSaveCenter.Playnite.Tests；scripts/build.ps1。所有条目继承公共验收，先查最新实现再动代码。

| ID | 任务 | 实施要求 | 具体完成条件 |
| --- | --- | --- | --- |
| R01-01 | 测试源码根绑定 | 让源码型测试显式验证 repository root 与程序集构建身份，支持隔离 OutputRoot | 将输出放到另一个 checkout 目录也不能悄悄读取那里的源码；错根失败有清晰诊断 |
| R01-02 | 数字单元格裁切 | 扩展夹具的横向文字可读检测，覆盖 1,024 / 99,999、长负数和容量 | 当前夹具数值列不再截掉末位；行高通过而列宽不足的负例必须失败 |
| R01-03 | 每项证据直达 | 为共享聚合报告添加到具体控件/状态的索引，不用同一句自动通过覆盖几十项 | 抽查 20 项均能在 30 秒内找到对应执行结果、代码身份、样本和未验边界 |
| R01-04 | 动效行为替代字符串 | 将重入动画核心保证补成实际 WPF 状态采样；原有源码门禁只保留结构约束 | 删掉关键基值处理会导致行为回归失败；单纯改变注释和局部变量名不导致行为测试失败 |
| R01-05 | 负例注册表 | 为对比、裁切、焦点、层级和状态门禁各保留一个有意失败夹具 | 每个检测器证明能抓住其声称检测的问题，记录 expected-failure；不混入生产入口 |
| R01-06 | 宿主证据保全 | 把关键 manifest、summary 和精选截图从可清理 artifacts 归档到文档证据目录 | 换机器仅 clone 仓库仍可查阅身份与主要结果；全量图另存可复现索引，不能只留绝对临时路径 |
| R01-07 | 基线失效规则 | 代码/资源变更自动指出哪些旧证据需要重跑，避免沿用旧程序集的宿主签收 | 修改共享控件后关联页标待复验；纯文档修改不要求重新安装；展示源码与包的各自身份 |
| R01-08 | 跳过测试说明 | 按环境能力分类 63 项等 skip，记录哪些影响 IPC、取消、恢复和宿主判断 | 成功数、失败数、跳过数分开；可执行的补测独立完成，环境限制不可改成通过 |

## R02 按钮与上下文动作精修

类型：交互增强；优先级：P2；旧任务衔接：Q04、Q05、Q15。

主要入口：Themes/WpfUiProduction.xaml；Themes/Redesign.xaml；Views/SaveCenterView.xaml；Views/MediaCenterView.xaml。所有条目继承公共验收，先查最新实现再动代码。

| ID | 任务 | 实施要求 | 具体完成条件 |
| --- | --- | --- | --- |
| R02-01 | 动作优先级 | 按区域统一一个主动作，其余次要/危险/图标动作使用共享语义样式 | 主页、详情、批量栏不出现多个无差别高亮按钮；危险动作与正常主动作明显区分 |
| R02-02 | 忙碌宽度稳定 | 按钮忙碌时预留 spinner 和文本宽度，承接真实命令执行状态 | 点击后不横向跳动、不重复提交；失败/取消后恢复可用，焦点仍有意义 |
| R02-03 | 禁用原因可达 | 给不满足条件的动作提供相邻说明或可聚焦帮助，不依赖禁用控件 Tooltip | 键盘可知道为什么不能恢复/归类/上传，并可进入解决步骤；不把危险命令强行启用 |
| R02-04 | 图文光学居中 | 校对中文四字、英文双词、图标加数字按钮的基线与视觉中心 | 同高按钮的文本基线一致，16/20 图标不挤字；双主题截图对齐对照 |
| R02-05 | 命中区与间距 | 检查紧凑工具条和行内图标的点击范围，使用共享密度令牌 | 紧邻复制/删除不会误点；命中区不重叠，窄窗换行后仍保持操作分组 |
| R02-06 | 菜单状态完整 | 上下文菜单的禁用、勾选、危险、子菜单和长标签统一 | 键盘上下/左右/Enter/Esc 均正确，文字不被快捷键列盖住，菜单翻转后可达 |
| R02-07 | 异步菜单上下文 | 菜单打开后固定操作对象身份，处理列表刷新或对象删除 | 删除/移动目标不会因底层选中行变化而改成另一对象；失效时明确提示并中止 |
| R02-08 | 动作文案动词化 | 统一重试、重新加载、重新校验、下载到隔离区等不同动作名称 | 用户可从标签预判写入目标和结果；失败信息不把下载与恢复、上传与校验混为一谈 |

## R03 中英文排版与信息密度

类型：视觉增强；优先级：P2；旧任务衔接：Q01、Q02。

主要入口：Themes/Typography.xaml；Themes/DesignTokens.xaml；Infrastructure/TypographyDiagnostics.cs；各 Views。所有条目继承公共验收，先查最新实现再动代码。

| ID | 任务 | 实施要求 | 具体完成条件 |
| --- | --- | --- | --- |
| R03-01 | 真实落字证据 | 在 WPF 绘制输出中检查关键 GlyphRun/Typeface，保留候选与最终落字区别 | 中文、英文、数字、𠮷和组合重音分别标明证据等级；无法获取的字形不得推定命中 |
| R03-02 | 阅读层级校准 | 按标题、正文、辅助、技术文本建立实际使用清单并消除无意义字号变体 | 同语义跨页一致，正文不靠低透明度弱化；以生产窄窗截图确认无挤压 |
| R03-03 | 双语基线 | 校对 Save Center 存档中心、日期容量和中文标点的混排基线 | 同一行无明显上下漂移；必要时使用明确文本片段样式，不靠整行任意偏移 |
| R03-04 | 数字列对齐 | 计数、容量、百分比和时间按列语义对齐，处理负数与未知值 | 9→10、99→100 时列锚点稳定；未知使用统一占位，不把未知显示为零 |
| R03-05 | 长路径分层 | 路径显示保留盘符/文件名重点并提供完整复制与可选择详情 | 超过 260 字符仍不推宽页面；换行不产生错误路径，复制内容无省略号 |
| R03-06 | 双语长度压力 | 加入英文长句、中文长游戏名及窄宽度组合样本 | 核心动作不截断成歧义词，标题可截断但可查看完整值；保持正常文案首屏密度 |
| R03-07 | 文案标点统一 | 整理冒号、括号、全角空格、英文缩写与单位间距，明确术语表 | 可见消息符合统一规则；技术错误码保持原样，复制路径不被排版替换 |
| R03-08 | 用户文本缩放 | 核查系统文本放大和自选字体对固定高度控件的影响 | 至少默认/较大文本模式下输入、按钮、表头不裁切；不通过缩小字号强行适配 |

## R04 输入框、校验与编辑体验

类型：交互增强；优先级：P2；旧任务衔接：Q06、Q07、Q23。

主要入口：Themes/WpfUiProduction.xaml；Settings/GameSaveCenterSettingsView.xaml.cs；Infrastructure/SettingsValidation*；各编辑 VM。所有条目继承公共验收，先查最新实现再动代码。

| ID | 任务 | 实施要求 | 具体完成条件 |
| --- | --- | --- | --- |
| R04-01 | 组合输入状态 | 识别 IME composition，搜索与提交在候选确认后再进入业务处理 | 拼音输入、候选翻页、Enter 选词不产生错误筛选或误提交；保留英文即时反馈 |
| R04-02 | 错误摘要导航 | 把字段级错误与顶部摘要互相链接，聚焦并滚动到第一个可修复字段 | 跨 Tab 错误点击可达且读屏读出原因；隐藏字段不阻止保存而无解释 |
| R04-03 | 未保存离开保护 | 明确取消、切页、关闭窗口时草稿的生命周期，复用现有确认方式 | 有效草稿不静默丢失；选择继续编辑回到原焦点，确认放弃才撤销 |
| R04-04 | 粘贴标准化 | 对目录、端口、排除模式等分别处理前后空白、引号和换行 | 多行误粘贴不会变成隐形无效路径；标准化前后可理解，原始值可恢复 |
| R04-05 | 数字输入边界 | 对重试次数、容量和时间等数值字段检查空值、上下界和溢出 | 键盘、粘贴、滚轮得到相同验证结果；不能用静默 clamp 掩盖不合法输入 |
| R04-06 | 清空与撤销 | 搜索清空后焦点留在输入框；普通编辑保留 Undo/Redo 与选择区 | Ctrl+A/C/V/Z/Y、清空按钮和 Esc 行为一致，撤销不触发危险命令 |
| R04-07 | 异步校验竞态 | 路径可用性等后台校验与当前字段版本关联 | A 慢校验晚于 B 返回不能覆盖 B；切页/关闭取消，失败不阻塞 UI 线程 |
| R04-08 | 保存反馈闭环 | 区分保存中、已保存、部分生效需重启和失败，避免 Toast 一闪即过 | 反馈与真实写入结果一致；失败保留草稿和修复入口，重复点击不并发保存 |

## R05 选择器、弹层与焦点

类型：交互增强；优先级：P2；旧任务衔接：Q08、Q09、Q10、Q15、Q24。

主要入口：Views/AcrylicProductionShellView.xaml.cs；Themes/WpfUiProduction.xaml；ViewModels/GamePickerViewModel.cs。所有条目继承公共验收，先查最新实现再动代码。

| ID | 任务 | 实施要求 | 具体完成条件 |
| --- | --- | --- | --- |
| R05-01 | 弹层焦点范围 | 梳理 ComboBox、游戏选框和详情浮层的 Tab/Shift+Tab 范围 | 打开后焦点进入内容；关闭回触发器；不跳到被遮挡的主页面危险动作 |
| R05-02 | 选项虚拟化焦点 | 过滤后保留有效选择并将键盘活动项滚入可见区 | 2000 项下方向键、PageDown、Home/End 正确；过滤删除选中项有清晰回退 |
| R05-03 | 弹层边缘适配 | 校验屏幕底部、左右边缘和短窗 Popup 翻转与最大高度 | 选项与滚动条不落到工作区外；翻转无闪屏，选定项仍可见 |
| R05-04 | 多选摘要 | 批量选择用数量、范围和例外表达，避免堆叠长标签 | 0/1/多选、跨页部分选择的摘要准确；清空不会顺便改筛选条件 |
| R05-05 | 复选框三态 | 区分全选、部分选中和未选，明确作用域是当前页还是全部结果 | 筛选切换、分页追加和部分失败后状态准确；Space 可切换且 UIA 有正确状态 |
| R05-06 | 开关保存语义 | 即时生效与需要保存的设置分开表达并补失败回退 | 开关后真实值、显示值、持久化值一致；回滚有原因且不反复闪动 |
| R05-07 | Tooltip 时序 | 调整长说明出现/消失时序、键盘展示和遮挡位置 | 快速划过不造成气泡雨；长路径可完整读，Esc 可关闭且不抢输入焦点 |
| R05-08 | 弹层资源热切换 | 打开弹层时切主题/关闭父窗，验证资源与生命周期 | 无旧主题残留、悬空 Popup 或泄漏订阅；父窗隐藏后所有附属层正确关闭 |

## R06 表格与列表专业化

类型：交互/视觉增强；优先级：P2；旧任务衔接：Q11、Q12、Q19、Q20、Q22。

主要入口：Themes/WpfUiProduction.xaml；Infrastructure/DataGridScrollDiagnostics.cs；Views/SaveCenterView.xaml；Views/TaskCenterView.xaml；Views/MediaCenterView.xaml。所有条目继承公共验收，先查最新实现再动代码。

| ID | 任务 | 实施要求 | 具体完成条件 |
| --- | --- | --- | --- |
| R06-01 | 列宽用户记忆 | 在既有列布局上保存用户调整的有效列宽，按视图区分并约束最小宽度 | 重启恢复，窗口变窄不产生不可见主列；提供重置布局，版本升级忽略无效列键 |
| R06-02 | 排序提示与稳定性 | 让排序箭头、主键/次键和默认顺序清楚且同值稳定 | 刷新不随机洗牌；数字和时间按值排序，未知值规则固定并测试 |
| R06-03 | 选中焦点区分 | 区分选中、键盘当前、悬停、失焦选择和错误行 | 浅深主题下同时存在仍可辨识；选中底色不吞掉徽章与危险动作 |
| R06-04 | 复制单元格与整行 | 为技术字段和诊断行提供明确复制入口及快捷键约定 | 复制含完整原值与稳定分隔符，批量选择无重复；不复制隐藏凭据 |
| R06-05 | 列头说明 | 单位、缩写与状态列提供简短说明，长表头有布局策略 | 容量单位一致；无信息价值的列不占首屏；排序按钮与说明不互相吞事件 |
| R06-06 | 行内进度稳定 | 进度变化只更新必要区域，未知总量和正在取消另行表达 | 进度更新不重置选择/滚动；完成数与总数变化无倒退假象 |
| R06-07 | 空表保留结构 | 分别设计首次空、筛选空、全部处理完和读取失败 | 仍保留必要表头与可达恢复操作；空态不会把失败伪装成无数据 |
| R06-08 | 详情与行高预算 | 长诊断在详情区展开，列表保持可扫描密度 | 扩展详情不撑开所有行；选中项更新后详情对象正确，四行门禁仍成立 |

## R07 滚动与响应布局

类型：布局增强；优先级：P2；旧任务衔接：Q11、Q12、Q19、Q20、Q24。

主要入口：Views/MediaCenterView.xaml.cs；Views/AcrylicProductionShellView.xaml.cs；各页面 XAML；UiAudit/UiLayoutAnalyzer.cs。所有条目继承公共验收，先查最新实现再动代码。

| ID | 任务 | 实施要求 | 具体完成条件 |
| --- | --- | --- | --- |
| R07-01 | 滚动所有权 | 逐页明确主滚动、表格滚动和详情滚动，保留现有滚动条系统 | 滚轮到边界的传递可预测；不吞滚轮、不出现页面与表格同步跳动 |
| R07-02 | 锚点删除回退 | 在刷新、删除、筛选和加载更多时按稳定 ID 恢复视口 | 锚点对象不存在时选邻近项；不能跳回首行或错选同索引的其他对象 |
| R07-03 | 短窗底栏可达 | 在 1040×700 及更紧凑内容区测顶部提示条叠加后的主操作 | 保存/取消/加载更多可滚到；浮层不盖住最后一行或横向滚动条 |
| R07-04 | 横向滚动端点 | 覆盖超长表格内容下 Ctrl/Shift/触控板与横向条端点 | 滚到最右能看到末列和动作；回到最左无像素漂移，保持 Media 特定虚拟化配置 |
| R07-05 | 详情断点稳定 | 用内容可读预算校准侧栏/下方详情切换，避免临界宽度反复抖动 | 连续拖动越过断点无重复重排风暴；详情焦点、选择、滚动位置保留 |
| R07-06 | 状态横幅预算 | 加载失败、Stale 和维护告警同时出现时合理分配空间 | 横幅不把主表压成零；能关闭的横幅有明确含义，不能隐藏真实失败状态 |
| R07-07 | 触控板小增量 | 验证精细滚动、反向快速滚动和滚轮加速，减少不必要布局刷新 | 滚动连续且末端稳定；观测 Layout 次数和可见容器数，不用主观描述代替数据 |
| R07-08 | resize 压力序列 | 把正常→窄→短→宽→恢复连成测试，包含打开的详情和菜单 | 无持久错误 MaxHeight、空白内容或丢焦点；停止拖动后布局尽快稳定 |

## R08 动效节奏与生命周期

类型：动效增强；优先级：P2；旧任务衔接：Q18、Q25。

主要入口：Infrastructure/GscMotion.cs；Themes/MotionTokens.xaml；Views/DashboardView.xaml.cs；Views/AcrylicProductionShellView.xaml.cs。所有条目继承公共验收，先查最新实现再动代码。

| ID | 任务 | 实施要求 | 具体完成条件 |
| --- | --- | --- | --- |
| R08-01 | 中途反向连续 | 按实际当前值接管 hover/press/侧栏开合动画，统一反向策略 | 快速进入离开、连点开合不跳终点；取样轨迹连续，最终状态匹配最后输入 |
| R08-02 | 热关闭动画 | 监听系统与应用动画开关变化，正在运行的时钟立即安全结束 | 关闭后 UI 落在稳定可见状态；重新开启不会重放所有旧动画 |
| R08-03 | 离屏与隐藏停机 | 页面隐藏、窗口最小化、Tab 切换时暂停无意义循环动效 | 不可见 spinner 不继续占用资源；恢复时只重启动需要的动画 |
| R08-04 | 业务完成节奏 | 取消/失败/成功反馈按实际任务状态切换，不用固定延迟假装完成 | 慢请求无假完成；快速任务不闪一帧 spinner；读屏通知与最终状态一致 |
| R08-05 | 页面切换轻量化 | 检查整页入场是否造成重复布局或列表重新建树，必要时仅动外层 | 切页前后绑定与滚动保留，Measure/Arrange 增幅可解释；无全页 blur 动画 |
| R08-06 | 数字变化动效 | 只对有价值的计数变化提供轻反馈，技术文本保持稳定可复制 | 高频进度不持续弹跳；无动画模式即时更新，99→100 不推挤邻项 |
| R08-07 | 对话框遮罩同步 | 遮罩与卡片进入退出共用状态机，退出期间阻止重复命令 | Esc/取消/关闭竞争下 completion 只完成一次；退出后遮罩不残留 |
| R08-08 | 变换所有权 | 为共用可变 Freezable 定义每控件所有权，处理现有外部 transform | 两个控件引用同一未冻结变换时，动画一者不移动另一者；保留已有几何效果 |

## R09 主题、图标与像素细节

类型：视觉增强；优先级：P2；旧任务衔接：Q03、Q13、Q14、Q24。

主要入口：Infrastructure/AdaptiveThemePalette.cs；Themes/DesignTokens.xaml；Themes/Typography.xaml；Themes/WpfUiProduction.xaml。所有条目继承公共验收，先查最新实现再动代码。

| ID | 任务 | 实施要求 | 具体完成条件 |
| --- | --- | --- | --- |
| R09-01 | 主题转换闪白 | 切换浅/深主题时检查背景、Popup、图标和占位图的更新顺序 | 无一帧白底黑洞式闪烁；局部资源不污染宿主其他插件 |
| R09-02 | 图标语义统一 | 整理备份/恢复/上传/校验/归类/忽略的图标映射与尺寸 | 同含义跨页一致；无字体缺字方框，禁用状态仍可辨识 |
| R09-03 | 一像素描边 | 检查分数 DPI 下边框、分隔线、选中指示与圆角连接 | 100/125/150/175/200% 真实或明确模拟分开记录，线条无明显忽粗忽细 |
| R09-04 | 阴影层次预算 | 给浮层、卡片、输入框分配有限阴影层级，避免全部浮起 | 深浅主题保持空间关系，低 Tier/高对比回退正确；阴影不扩大滚动范围 |
| R09-05 | 焦点轮廓合成 | 对键盘焦点叠加 hover/selected/error 做最终画面检查 | 轮廓不被 Clip/圆角遮挡；非颜色标识足够，不能只用很浅底色变化 |
| R09-06 | 高对比真实配色 | 高对比使用系统语义资源并检查图标、进度与禁用文本 | 不依赖玻璃或阴影传达边界；切回普通主题后资源恢复 |
| R09-07 | 缩略图占位一致 | 加载、失败、无图、视频和损坏文件占位各有语义但尺寸相同 | 图片迟到不改变行高；fallback 不挤占操作区，状态具有文字说明 |
| R09-08 | 主题背景压力 | 在不同明暗宿主背景和色偏下测透明控件的实际合成 | 相同语义文字在最差背景仍达项目门槛；报告不能所有复杂背景都被不透明层遮住却宣称已测透明压力 |

## R10 导航与效率功能

类型：功能增强；优先级：P2；旧任务衔接：Q05、Q19、Q24。

主要入口：Views/AcrylicProductionShellView.xaml.cs；ViewModels/DashboardViewModel.cs；ViewModels/FindingNavigationResolver.cs。所有条目继承公共验收，先查最新实现再动代码。

| ID | 任务 | 实施要求 | 具体完成条件 |
| --- | --- | --- | --- |
| R10-01 | 上下文返回 | 在告警→任务→游戏详情之间保留来源入口与返回目标 | 返回恢复游戏、页签、筛选和滚动；对象删除后安全回退并解释 |
| R10-02 | 定位当前游戏 | 为跨页操作提供明确当前游戏名称与快速定位入口 | 从任务或媒体详情定位同一 PlayniteId；重名游戏不能选错 |
| R10-03 | 搜索快捷键 | 为当前页搜索提供一致快捷键，避免覆盖 Playnite 全局常用键 | 弹层/对话框打开时作用域正确；输入框编辑快捷键不被导航吞掉 |
| R10-04 | 快捷键帮助 | 提供简短可发现的键盘操作说明，复用实际 command 元数据 | 帮助与当前页面可用动作一致，禁用命令不显示成可用快捷键 |
| R10-05 | 筛选预设 | 允许保存常用只读筛选组合，例如失败任务、未归类媒体 | 不保存临时对象引用；删除/重命名预设可撤销或确认，非法旧配置可回退 |
| R10-06 | 筛选来源提示 | 从概览跳转后展示带入的筛选条件，并可一键恢复默认 | 用户不会误以为列表丢数据；清除带入条件不清除其他编辑草稿 |
| R10-07 | 侧栏信息密度 | 折叠侧栏保留图标、当前态、Tooltip 和键盘可达性 | 折叠前后选中工作区一致；徽标不遮图标，长名称展开时不挤主区 |
| R10-08 | 最近操作续接 | 提供有限的最近访问对象入口，区分访问记录与任务历史 | 只保存稳定 ID 和必要状态；已移除对象可清理，不泄漏本地绝对路径到标题 |

## R11 备份历史与版本比较

类型：功能增强；优先级：P2；旧任务衔接：Q19；既有 BackupDiff/RetentionPreview。

主要入口：Views/SaveCenterView.xaml；ViewModels/DashboardViewModel.cs；Worker/Services/BackupOrchestrator.cs；Contracts 中备份 DTO。所有条目继承公共验收，先查最新实现再动代码。

| ID | 任务 | 实施要求 | 具体完成条件 |
| --- | --- | --- | --- |
| R11-01 | 版本信息摘要 | 给版本行提供时间、大小、文件变化、保护和校验摘要，复用现有 DTO | 本地/远端来源清楚；未知校验不能显示绿色已安全；长摘要不推宽表格 |
| R11-02 | 双版本对比选择 | 在已有差异能力上提供清晰的 A/B 版本选择与交换方向 | 增删改含义随方向正确变化；相同版本有明确信息，不触发真实恢复 |
| R11-03 | 差异列表搜索 | 对版本差异按新增/修改/删除和路径筛选，支持完整路径复制 | 大清单使用有限加载；计数与筛选匹配，未知差异与零变化分开 |
| R11-04 | 版本说明编辑 | 在既有元数据机制上支持简短备注或命名，避免更改归档文件名 | 取消保留原值；重启可读；重复版本名不影响稳定 BackupId 选择 |
| R11-05 | 保护操作解释 | 让锁定/保护版本的作用、解除条件和保留策略关系清楚 | 保护图标与真实状态一致；保留预览始终跳过受保护版本 |
| R11-06 | 备份前变更摘要 | 展示此次备份范围和已识别路径，缺数据时明确尚未扫描 | 预览不产生归档；真实执行时重新校验条件，不能用过期预览保证安全 |
| R11-07 | 备份结果分层 | 区分本地成功、镜像失败、上传排队和远端已校验 | 部分成功提供对应补救动作；不因云失败把已成功本地版本隐藏 |
| R11-08 | 历史时间导航 | 在现有历史列表上提供日期范围和跳转近期/较早版本 | 本地时区、夏令时和同一秒多版本顺序稳定；清空范围恢复全部 |

## R12 恢复流程的可理解性与保护

类型：功能增强；优先级：P1/P2；旧任务衔接：Q19；既有 RestoreReadiness/RestoreOrchestrator。

主要入口：Views/SaveCenterView.xaml；Worker/Services/RestoreReadinessService.cs；Worker/Services/RestoreOrchestrator.cs；Worker/Services/RemoteBackupStagingService.cs。所有条目继承公共验收，先查最新实现再动代码。

| ID | 任务 | 实施要求 | 具体完成条件 |
| --- | --- | --- | --- |
| R12-01 | 恢复分步摘要 | 把选择版本、可恢复性检查、目标核对和执行结果分成清晰阶段 | 每阶段显示真实状态；用户能知道何时才会覆盖文件，失败保留当前步骤 |
| R12-02 | 校验结果解释 | 展示归档可读性、文件数、哈希覆盖和最近检查时间 | 未提供哈希不等于校验成功；部分覆盖、旧结果、损坏结果分别说明 |
| R12-03 | 目标路径核对 | 显示原路径与重映射路径的可读对照，突出将被写入的目录 | 长路径可完整复制；路径相似或盘符不同不会混淆；预览不写用户目录 |
| R12-04 | 恢复保护备份 | 明确执行前保护备份的阶段、结果和失败中止原因 | 保护备份失败不可继续危险恢复；重试仍遵守同游戏锁和最新状态 |
| R12-05 | 远端下载进度 | 远端恢复中区分下载到隔离区、校验和准备覆盖 | 下载完成不显示为恢复完成；取消后隔离残留状态准确可见 |
| R12-06 | 恢复冲突说明 | 游戏运行、操作锁、磁盘不足和目标无权限分别给出解决步骤 | 不能用一个泛化失败覆盖全部原因；没有依据时不建议用户关闭安全机制 |
| R12-07 | 预览失效重验 | 在确认到执行之间检查版本/映射/目标变化，必要时重新预览 | 切换版本或设置后旧确认不能应用到新对象；相同大小不同文件也可识别失效 |
| R12-08 | 恢复结果报告 | 展示执行目标、恢复文件范围、保护备份和失败阶段，提供脱敏复制 | 部分完成与全部完成区分；报告可追溯任务 ID，不暴露凭据 |

## R13 云队列与同步状态

类型：功能增强；优先级：P2；旧任务衔接：Q22；既有 CloudTransferCoordinator/CloudRetry。

主要入口：ViewModels/DashboardViewModel.CloudTransfers.cs；Views/MaintenanceView.xaml；Contracts/CloudTransferDtos.cs；Worker/Services/CloudTransferCoordinator.cs。所有条目继承公共验收，先查最新实现再动代码。

| ID | 任务 | 实施要求 | 具体完成条件 |
| --- | --- | --- | --- |
| R13-01 | 队列阶段展示 | 对等待时段、等待网络、上传中、验证中、已验证分别给出状态 | 与 Worker 真实阶段一致；上传成功不得冒充远端校验，沿用 GuaranteeDisplay |
| R13-02 | 下次重试时间 | 将重试倒计时、绝对时间和失败原因放在同一详情中 | 系统时钟变化不产生负倒计时；关闭页面不留下每行常驻计时器 |
| R13-03 | 手动重试范围 | 明确只重试失败项、选中项或整个队列，保持幂等请求身份 | 重复点击不生成重复上传；部分成功后重试不会重新处理已成功项 |
| R13-04 | 暂停与允许时段 | 区分用户暂停、策略时段外和队列空闲，提供对应恢复入口 | 暂停状态持久化语义明确；改允许时段后说明何时生效，不意外取消进行中上传 |
| R13-05 | 远端证据详情 | 展示远端对象、最后尝试、最后成功校验与来源设备 | 未知字段保持未知；脱敏显示远端路径，复制诊断不包含认证参数 |
| R13-06 | 离线恢复反馈 | 网络恢复后队列从等待过渡到执行，解释退避与限流 | 不瞬间并发冲击所有任务；重新在线不会重复通知每一条旧失败 |
| R13-07 | 队列筛选与汇总 | 按游戏、状态、设备和时间筛选并保持全局/当前筛选计数清楚 | 摘要不与筛选结果矛盾；分页追加无重复，刷新不丢选择 |
| R13-08 | 失败分类帮助 | 对认证、无空间、远端不存在、校验差异和限流提供不同下一步 | 只显示已识别原因；可展开原始错误码，未知错误保留诊断而非猜测 |

## R14 媒体归类与批量操作

类型：功能增强；优先级：P2；旧任务衔接：Q20；既有 MediaClassification/MediaSync。

主要入口：Views/MediaCenterView.xaml；ViewModels/DashboardViewModel.MediaClassification.cs；ViewModels/DashboardViewModel.Media.cs；Worker/Services/MediaSyncService.cs。所有条目继承公共验收，先查最新实现再动代码。

| ID | 任务 | 实施要求 | 具体完成条件 |
| --- | --- | --- | --- |
| R14-01 | 归类建议解释 | 在现有建议预览中显示来源规则、游戏会话和进程映射依据 | 用户可判断为什么建议该游戏；无依据显示待判断，不能伪造置信度 |
| R14-02 | 预览选择编辑 | 支持在批量预览中排除个别项和调整目标，提交前显示总数 | 排除项不会执行；筛选后选择作用域明确，确认对象用稳定 ID |
| R14-03 | 部分成功处理 | 批量归类/忽略后保留失败项并给出逐项原因及仅重试失败入口 | 成功项不重复执行；选择、计数和列表模式与实际结果一致 |
| R14-04 | 撤销边界说明 | 利用现有恢复忽略/归类历史能力提供可撤销操作入口 | 无法撤销的移动明确说明；撤销再次核对目标，不覆盖后来人工决定 |
| R14-05 | 重复媒体识别视图 | 先核实已有重复检测，按现有哈希或元数据展示疑似重复组 | 疑似与确定重复分开；仅查看/选择，不默认删除任何真实媒体 |
| R14-06 | 批量目标防误选 | 归类到游戏时展示封面、平台和唯一对象信息 | 重名游戏可区分；输入过滤后已选目标不悄悄变化 |
| R14-07 | 媒体详情浏览 | 详情提供上一项/下一项并保留列表锚点，视频信息和图片尺寸清楚 | 文件已移走或打不开有回退；详情关闭回原行，预览加载可取消 |
| R14-08 | 来源规则试运行 | 为现有来源规则增加样本匹配预览、命中原因和排除解释 | 试运行不移动文件、不持久化归类结果；复杂规则有超时和数量预算 |

## R15 任务中心与长操作反馈

类型：功能增强；优先级：P2；旧任务衔接：Q16、Q17、Q22。

主要入口：Views/TaskCenterView.xaml；ViewModels/DashboardViewModel.TaskPageState.cs；Worker/Services/TaskCoordinator.cs；Contracts 任务 DTO。所有条目继承公共验收，先查最新实现再动代码。

| ID | 任务 | 实施要求 | 具体完成条件 |
| --- | --- | --- | --- |
| R15-01 | 任务阶段可读 | 让备份、下载、校验、恢复、清理任务显示各自真实阶段 | 阶段切换与后端事件一致；未知进度不用虚假百分比，真实错误不被阶段标题遮住 |
| R15-02 | 取消过程展示 | 区分可取消、正在取消、已取消和无法中断的收尾阶段 | 连点取消只发一次；成功/取消竞争后的最终状态一致，不永久停在取消中 |
| R15-03 | 任务详情时间线 | 把已有开始、阶段、重试、结束记录整理为紧凑时间线 | 时间顺序稳定，UTC/本地清楚；缺事件显示未知，不编造中间过程 |
| R15-04 | 重复通知归并 | 同一任务持续失败或进度事件合并，结束时提供一次有价值通知 | 屏幕不被 Toast 刷满；仍能从历史找到全部错误，重要新失败不被静音 |
| R15-05 | 任务来源定位 | 从任务详情返回游戏、版本、媒体批次或云队列对象 | 对象已删除时保留诊断并说明无法定位；不会跳到同名其他对象 |
| R15-06 | 耗时与吞吐 | 仅在有可靠数据时显示耗时、速率和剩余时间，使用平滑采样 | 停顿、暂停、未知总量不显示荒谬 ETA；无需每行毫秒级刷新 |
| R15-07 | 失败结果复制 | 提供一键复制任务摘要和错误码，长堆栈放可选择详情 | 短摘要能用于反馈，脱敏默认正确；剪贴板失败可重试且不丢用户选择 |
| R15-08 | 清理历史范围 | 为已有历史清理能力提供日期/状态预览和影响说明 | 不删除运行中任务或必要恢复账本；用户确认与真正执行集合一致 |

## R16 设置、策略与配置体验

类型：功能增强；优先级：P2；旧任务衔接：Q23；既有策略模板/设置验证。

主要入口：Settings/GameSaveCenterSettingsView.xaml；Settings/GameSaveCenterSettingsView.xaml.cs；ViewModels/DashboardViewModel.cs；Worker/Configuration。所有条目继承公共验收，先查最新实现再动代码。

| ID | 任务 | 实施要求 | 具体完成条件 |
| --- | --- | --- | --- |
| R16-01 | 设置搜索定位 | 给已有设置分类增加轻量搜索与匹配字段定位 | 命中字段可见并可编辑；搜索不改变配置，清空后恢复原分类 |
| R16-02 | 策略差异预览 | 编辑游戏策略或套用模板前展示与当前值的差异 | 继承值与显式覆盖值分开；取消无写入，保存只改预期字段 |
| R16-03 | 模板应用范围 | 在现有模板批量应用上展示目标游戏、排除项和变更数量 | 空选择和跨筛选选择不误用全部游戏；部分失败可逐项恢复处理 |
| R16-04 | 恢复默认粒度 | 分别提供单字段、单分类和全部默认的清晰范围 | 确认文案列影响；敏感连接字段不被无意清空，取消保留草稿 |
| R16-05 | 路径编辑一致 | 将浏览、校验、打开、复制组织为同一目录编辑模式 | 网络/不存在/无权限路径都有反馈；打开动作只处理当前有效路径 |
| R16-06 | 生效条件说明 | 配置项旁标注即时生效、下次任务生效或需重启 | 不笼统要求所有修改重启；真实生效条件与服务消费路径对应 |
| R16-07 | 配置导入预览 | 复用已有导入能力，先展示版本、兼容性和将覆盖的字段 | 旧版本未知字段不破坏配置；凭据不进入可分享导出；失败可回到原配置 |
| R16-08 | 保存冲突处理 | 检查两个设置入口或后台更新与用户草稿的冲突 | 基线变化有明确提示或按字段合并；禁止静默覆盖用户最后修改 |

## R17 维护与诊断可操作性

类型：功能增强；优先级：P2；旧任务衔接：Q22；既有 DiagnosticsPackage/Integrity/Retention。

主要入口：Views/MaintenanceView.xaml；ViewModels/DashboardViewModel.MaintenanceActions.cs；Worker/Services/DiagnosticsPackageService.cs；Worker/Services/RetentionSimulationService.cs。所有条目继承公共验收，先查最新实现再动代码。

| ID | 任务 | 实施要求 | 具体完成条件 |
| --- | --- | --- | --- |
| R17-01 | 健康结果分层 | 把需立即处理、建议处理和信息项按真实影响分组 | 已解决项退出待处理；同问题跨来源不重复计数，详情保留证据时间 |
| R17-02 | 诊断包预览 | 生成前列明将包含的类别和脱敏范围，生成后提供文件大小与位置 | 不包含真实存档、媒体、数据库或凭据；用户可查看结果，不自动上传 |
| R17-03 | 检查进度预算 | 巡检显示本轮范围、暂停原因、最近成功时间与下轮计划 | 游戏运行/锁占用延后有解释；取消与结束准确，不伪装整库已检查 |
| R17-04 | 保留预览对比 | 现有保留预览展示候选、保护项、预计释放与实际释放区别 | 隔离失败/删除失败不能计为实际释放；执行前过期预览重新计算 |
| R17-05 | 隔离账本入口 | 把已存在的隔离恢复信息呈现为可理解的待处理列表 | 原路径/隔离路径/状态可核查；提供受控恢复入口，不默认删除残留 |
| R17-06 | 存储分析导航 | 从容量汇总跳转到具体游戏或版本，解释统计范围 | 逻辑索引大小与磁盘实际占用分开；失联路径不计成零而无提示 |
| R17-07 | 检查项一键定位 | 每条可修复问题进入对应配置、任务或版本页并突出目标 | 目标不存在时保持诊断；回到维护后保留筛选和滚动 |
| R17-08 | 维护报告可读性 | 导出报告按摘要、待处理、已验证、未知组织，并列出软件身份 | 同一报告中的时间/计数一致；脱敏测试覆盖 URL 参数和 Windows 用户路径 |

## R18 性能测量与资源预算

类型：性能增强；优先级：P1/P2；旧任务衔接：Q25；既有大库/缩略图/分页基准。

主要入口：ViewModels/GamePickerViewModel.cs；Infrastructure/DebouncedRefresh.cs；Converters/AsyncThumbnailLoader.cs；tests/GameSaveCenter.RenderHarness。所有条目继承公共验收，先查最新实现再动代码。

| ID | 任务 | 实施要求 | 具体完成条件 |
| --- | --- | --- | --- |
| R18-01 | 连续输入基准 | 在 2000/10000 项上回放连续英文输入、粘贴、删除及 IME 完成事件 | 输出每次 UI 更新延迟、过滤次数和分配量；20ms debounce 选择有测量依据 |
| R18-02 | 真实 Dispatcher 基准 | 区分 VM 数据完成与画面可见反馈，记录两段延迟 | 受控窗口显示来源明确；画面延迟不由 FilteredCount 代替，原始样本可复算 |
| R18-03 | 缩略图滚动预算 | 快速滚过后只保留必要解码，检查缓存与取消 | 活动请求、缓存条目和内存有上限；迟到结果不写入复用后的错误行 |
| R18-04 | 表格容器预算 | 在 2k/10k/20k 数据下记录已实现容器数及滚动更新成本 | 容器数受视口约束；保留 Media Standard/Item/禁列虚拟化的现有例外并解释 |
| R18-05 | 后台事件合并 | 高频任务/媒体事件以有界批次投递 UI，最终状态立即可见 | 不丢完成/失败事件；队列不能无限增长，关闭页面后停止无用刷新 |
| R18-06 | 页面重访成本 | 测首次打开和反复切回同页的加载、绑定和布局耗时 | 热态重访不重复整库读取；缓存失效有依据，不显示过期对象状态 |
| R18-07 | 长时资源曲线 | 在既有耐久脚本上记录句柄、订阅、时钟、工作集与缓存 | 30 分钟后停止输入能回落或稳定；保留原始时间序列，不只写最终内存 |
| R18-08 | 低性能降级触发 | 校对现有无玻璃/无动画回退及渲染 Tier 行为 | 功能与文本可读性不降级；只能在真实低 Tier 或明确模拟下说明相应证据 |

## R19 异步竞态与故障恢复

类型：可靠性增强；优先级：P1/P2；旧任务衔接：既有 L25–L28；Q17、Q20、Q22、Q25。

主要入口：ViewModels/DashboardViewModel.WorkspaceStates.cs；ViewModels/DashboardViewModel.Media.cs；Infrastructure/WorkerIpcClient.cs；Worker/Services/TaskReconcileService.cs。所有条目继承公共验收，先查最新实现再动代码。

| ID | 任务 | 实施要求 | 具体完成条件 |
| --- | --- | --- | --- |
| R19-01 | 旧请求晚返回 | 补充跨游戏/页签/筛选切换时的代际一致性回归 | A 慢成功不能覆盖 B 新失败；标题、数据、选择、更新时间同属一个上下文 |
| R19-02 | 刷新失败保留草稿 | 把只读数据刷新与用户编辑分开，避免后台替换编辑对象 | 刷新失败后草稿仍在；成功返回不无条件覆盖未保存字段 |
| R19-03 | 重复执行幂等 | 对已有写操作检查 request ID 与重试恢复，重点覆盖 UI 重复触发 | 响应丢失时不会重复执行；状态未知明确显示，不能擅自重试恢复或删除 |
| R19-04 | 取消关闭顺序 | 窗口关闭、请求取消、Worker 断开同时发生时有确定收尾 | 无 UI 线程异常或永久 IsBusy；下一次打开能恢复真实任务状态 |
| R19-05 | Worker 重启恢复 | 扩展现有重启回归，验证进行中任务、进度订阅和队列恢复 | 中断任务按真实持久化状态显示；不伪装完成，重连不重复订阅 |
| R19-06 | 分页快照变化 | 翻页期间新增/删除/排序变化使用稳定游标或明确重载策略 | 无重复/漏项假象；末页变化不产生无限加载，选择对象 ID 稳定 |
| R19-07 | 外部文件变化 | 媒体/备份详情打开期间文件被移走、占用或损坏有可理解回退 | 不崩溃，不写错误路径；可重新定位/刷新，保留诊断上下文 |
| R19-08 | 慢调用可取消 | 给网络与外部工具等待设置可观察超时和取消边界 | 取消能解除 UI 等待；不可确认的写操作结果标未知，避免假成功或盲目重试 |

## R20 概览、空态与首次使用

类型：功能/视觉增强；优先级：P2；旧任务衔接：Q17、Q19、Q22；既有 OverviewPriorityResolver。

主要入口：Views/OverviewView.xaml；Views/OverviewView.xaml.cs；ViewModels/OverviewPriorityResolver.cs；Controls/WorkspaceStatePresenter.cs。所有条目继承公共验收，先查最新实现再动代码。

| ID | 任务 | 实施要求 | 具体完成条件 |
| --- | --- | --- | --- |
| R20-01 | 概览下一步 | 概览只突出当前最需要处理的一项，链接到真实解决入口 | 无游戏、未匹配、可备份、失败待处理分别正确；优先级变化不反复跳动 |
| R20-02 | 指标统计范围 | 所有概览数字显示统计范围/更新时间，区分当前游戏与全库 | 点击后的列表能解释数字来源；未知/未加载不显示为确定的零 |
| R20-03 | 首次配置引导 | 在现有页面提供最小必要步骤和已完成标记 | 未配外部工具时知道下一步；引导可返回，不能自动更改用户配置 |
| R20-04 | 零结果恢复 | 筛选无结果提供清除条件与当前条件摘要 | 按钮仅改筛选，不重置用户数据；离线错误不会被当成零结果 |
| R20-05 | Stale 可理解 | 旧数据状态展示最后成功时间、刷新失败原因与重试入口 | 用户能区分旧缓存和当前真值；不遮挡已经可用的安全只读操作 |
| R20-06 | 部分可用状态 | 当 Worker/云端/外部工具部分不可用时保留独立可用区 | 相关动作准确禁用并有解释；一个故障不把整页变成无用错误页 |
| R20-07 | 最近活动密度 | 把重复进度合并为活动摘要，失败与完成可展开详情 | 首页易扫描且无动画刷屏；隐藏细节仍可追溯任务，计数真实 |
| R20-08 | 状态语气统一 | 为加载、失败、空、完成和需要操作建立简短文案模板 | 说明发生什么与可做什么，不责怪用户、不暴露内部实现术语替代解释 |

## R21 键盘与辅助功能闭环

类型：可访问性增强；优先级：P1/P2；旧任务衔接：Q24；现有 UIA/KeyboardFocus 测试。

主要入口：Views/AcrylicProductionShellView.xaml；Views/DashboardView.xaml；Themes/WpfUiProduction.xaml；Controls；tests/GameSaveCenter.Playnite.Tests。所有条目继承公共验收，先查最新实现再动代码。

| ID | 任务 | 实施要求 | 具体完成条件 |
| --- | --- | --- | --- |
| R21-01 | 八入口纯键盘 | 逐页用 Tab/Shift+Tab/方向键/Enter/Esc 完成主要只读与安全夹具操作 | 真实焦点轨迹有记录；核心命令可达，无焦点陷阱或遮挡页面的命中 |
| R21-02 | 控件名称与值 | 检查图标按钮、复合选择器、开关与进度条 UIA 名称/状态/值 | 名称描述动作不重复图标字形；变化状态可读取，关联标签明确 |
| R21-03 | 验证错误播报 | 错误出现时仅通知必要变化并关联字段 | 不把每次键入都播报成长段错误；修复后旧错误状态消失 |
| R21-04 | 异步完成播报 | 后台任务完成/失败、列表加载结束提供节制的辅助通知 | 不抢走输入焦点；重复进度不刷屏，关键结果有可再次读取入口 |
| R21-05 | 禁用与隐藏区别 | 按产品语义决定动作应禁用、折叠还是显示解释 | 键盘和读屏都能理解不可用原因；隐藏内容不进入 Tab 顺序 |
| R21-06 | 可选择技术文本 | 诊断、路径、错误码和版本信息可用键盘选择复制 | 不必精准鼠标拖拽；选中文本与背景对比清楚，复制不触发导航 |
| R21-07 | 焦点可视回归 | 测试滚动后、切主题后、弹层关闭后的焦点轮廓 | 焦点始终在可见有效控件上；控件移除时回退到合理位置 |
| R21-08 | 单屏与跨屏分账 | 记录当前单屏能力和待多屏实测项，准备可执行场景清单 | 单屏 DPI 模拟不冒充物理跨屏；跨屏需验证 Popup 位置、字体、焦点及资源释放 |

## R22 精细交互与一致性补充

类型：交互增强；优先级：P2；旧任务衔接：Q05、Q06、Q15、Q16、Q19–Q23。

主要入口：各 Views；Infrastructure 中格式化/状态映射；ViewModels/RelayCommand.cs；Settings。所有条目继承公共验收，先查最新实现再动代码。

| ID | 任务 | 实施要求 | 具体完成条件 |
| --- | --- | --- | --- |
| R22-01 | 时间显示统一 | 统一相对时间、完整时间和时区提示，保留可复制原始时间 | 昨天/刚刚边界稳定；系统时间改变不会排序倒置，任务时长用单调时间 |
| R22-02 | 容量单位统一 | 明确 B/KB 或 KiB 等既有单位口径与有效数字策略 | 同一对象在概览、列表、详情和报告数值一致；小值不四舍五入成零而误导 |
| R22-03 | 复制反馈轻量 | 复制成功在原控件附近短暂反馈，失败有可重试信息 | 不弹模态对话框、不改变布局；连续复制不堆 Toast，读屏能得知结果 |
| R22-04 | 打开路径失败 | 文件/目录打开失败提供存在性与权限检查结果及复制路径入口 | 失效路径不导致未处理异常；不为修复打开失败修改权限或用户文件 |
| R22-05 | 批量数量防歧义 | 所有批量动作同时显示所选数量、当前结果数量与隐藏选择 | 跨筛选保留选择时明显提示；全选当前页与全部结果不能混淆 |
| R22-06 | 长任务离页提示 | 用户离开正在执行的页面时保留任务可追踪入口 | 离页不取消后台任务，若确需取消则语义明确；回来恢复阶段和进度 |
| R22-07 | 确认框信息结构 | 危险动作确认先列对象/范围/后果，技术细节可展开 | 按钮名称明确，不只用是/否；取消默认安全，Enter 不意外确认危险操作 |
| R22-08 | 状态样式一致索引 | 整理成功/警告/错误/运行/暂停/未知的颜色、图标、文案对应 | 同一状态跨页视觉一致；不只靠颜色，未知不借用成功图标 |

## R23 发布收口与持续维护

类型：交付验证；优先级：P1；旧任务衔接：Q00、Q24、Q25；所有本轮已实施任务。

主要入口：scripts/build.ps1；scripts/package.ps1；scripts/real-host-audit.ps1；docs/design/reviews；docs/ai。所有条目继承公共验收，先查最新实现再动代码。

| ID | 任务 | 实施要求 | 具体完成条件 |
| --- | --- | --- | --- |
| R23-01 | 每组可审阅交付 | 每组 8 项按独立功能边界分批提交，关联实现与验收条目 | 中文 commit/push，工作树可复现；不把用户文件或生成目录混入提交 |
| R23-02 | 生产资源状态矩阵 | 汇总所有已修改共享控件的浅/深、正常/悬停/按压/焦点/禁用状态 | 报告指出真正实例化的样式与状态；多个派生样式不能只用基类样例替代 |
| R23-03 | 代表页面终审 | 按概览/存档/媒体/工具/任务/维护/设置/壳层挑选高风险状态 | 实际信息可读、操作可达、空错加载都有证据；明确与 Demo 的对应或例外 |
| R23-04 | 非空隔离宿主 | 以固定构建身份、合成非空库运行真实嵌入 Playnite 复测 | manifest 区分嵌入与专用窗口；当前变更不能借旧包签收，保留精选原图 |
| R23-05 | 帧性能证据闭环 | 在允许的环境取得呈现帧数据；不可用时保留代理与待验区分 | Rendering 回调、Stopwatch 与真实呈现帧分别标记，不绕过被拒绝的系统跟踪权限 |
| R23-06 | 安装与回退可核查 | 针对实际发布候选验证程序集、包、隔离安装和回退说明 | 六份程序集身份一致；不以用户真实存档执行恢复来测试安装，回退路径有据可查 |
| R23-07 | 任务去重与收尾 | 回查 Q/R 两套队列，已满足项必须给行为/证据定位并注明复用 | 未实施、代码完成、已验收、外部阻塞分开计数；不能靠改状态宣称全部完成 |
| R23-08 | 下一轮准入清单 | 仅保留新发现的可复现问题或明确用户价值，给优先级与成本依据 | 本轮无意义想法可标不适用并说明；留可续读入口、证据路径和真实未验边界 |

## 持续推进账本

使用 [ROUND3_PROGRESS.md](reviews/ui-finesse-round3-20260915/ROUND3_PROGRESS.md)。本文件发布时 192 项全部为“待开始”，不代表已有服务从零开始。每个条目先去重；真正已满足时记录复用依据，已不适用时给具体原因。
