## 0.6.49 旧 Worker 与 900+ 游戏回归

- [ ] 安装后确认 `playnite.log` 加载 `GameSaveCenter, version 0.6.49`，并确认 Worker 为 `0.6.49.0`。
- [ ] 若安装前已有旧 Worker 存活，确认新插件先检测版本不匹配并替换，而不是复用旧版进程。
- [ ] 在 900+ 游戏库启动阶段观察 `worker-launch.log`，确认没有旧版连续每游戏一次的 `ludusavi.exe find`；同时单独禁用 `LudusaviPlaynite` 做 A/B。
- [ ] 确认当前版本 Worker 短暂 Ping 超时时不会被误杀，版本不匹配时则会被替换。

## 0.6.48 900+ 游戏与 DataGrid 回归

- [ ] 安装后确认 `playnite.log` 加载 `GameSaveCenter, version 0.6.48`，并确认 Worker `0.6.48.0`。
- [ ] 在 900+ 游戏库导入/关闭阶段观察 `worker-launch.log`，确认瞬时 0/部分库快照不会让已有 Worker 进入杀进程重启路径。
- [ ] 在 1600×900、1280×720、1024×700、850×650、700×620 DIP 下检查存档/媒体/任务/维护表格：横向滚动可拖动、行列仍虚拟化、下方检查器可由页面滚动到达。
- [ ] 在表格存在长路径、超长任务详情或行详情时验证横向滚动和行详情不会裁切；浅色、深色、跟随 Playnite、高对比度、关闭透明及 125%/150% DPI 均需回归。
- [ ] 若仍崩溃，保存完整堆栈并先确认日志不是旧的 0.6.22 安装，也要与独立 `LudusaviPlaynite` 插件分开做 A/B。

## 0.6.47 900+ 游戏 Worker 保留进程回归

- [ ] 安装后确认 `playnite.log` 加载 `GameSaveCenter, version 0.6.47`，并确认 Worker `0.6.47.0`。
- [ ] 在 900+ 游戏库中打开/关闭 Dashboard，若短时 Ping 超时，确认 `worker-launch.log` 没有因为健康探测失败杀掉仍存活的 Worker。
- [ ] 记录第一次打开耗时、Worker ready 时间以及是否出现管道超时；失败时同时禁用独立 `LudusaviPlaynite` 做 A/B 对照。
- [ ] 确认旧版 `0.6.22` 的 XAML 资源崩溃不再出现；若日志仍显示 `0.6.22`，先重新安装 `artifacts/GameSaveCenter-0.6.47.pext`。

## 0.6.46 Playnite 崩溃隔离回归

- [ ] 安装后确认 `playnite.log` 加载 `GameSaveCenter, version 0.6.46`。
- [ ] 在 Dashboard 加载、卸载、重复打开/关闭以及主题切换时悬停导航、卡片和按钮，确认无 `Unhandled exception`。
- [ ] 打开 900+ 游戏库，确认 Worker/缓存加载失败只显示状态，不导致 Playnite 扩展崩溃；记录 `extensions.log` 和 `worker-launch.log`。
- [ ] 若仍崩溃，保存完整堆栈并确认不是独立 `LudusaviPlaynite` 插件或旧版 GameSaveCenter。

## 0.6.44 表格可读高度与双层滚动回归

- [ ] 在 1600×900、1280×720、1024×700、850×650、700×620 DIP 检查存档、媒体、任务和维护表格是否至少显示多行。
- [ ] 滚动表格内部时只移动表格行；继续向下滚动页面时，能到达检查器、第二张表和底部操作。
- [ ] 浅色、深色、跟随 Playnite、高对比度和关闭透明下，表头、圆角行、滚动条和文字均可读。
- [ ] Windows 125%/150% DPI 下无裁剪、重叠或只剩单行的表格。

## 0.6.43 900+ 游戏启动竞态回归

- [ ] 安装 `0.6.43`，确认 `playnite.log` 加载的是 `GameSaveCenter, version 0.6.43`，不是 0.6.22。
- [ ] 在 Playnite 库仍处于导入阶段时启用插件；确认 `worker-launch.log` 不在启动阶段连续出现每游戏一次的 `ludusavi.exe find`。
- [ ] 900+ 游戏且 Dashboard 未打开时，确认扩展不会自动启动 Worker；打开 Dashboard 后先显示 SQLite 缓存或明确空状态，不等待整库匹配。
- [ ] 检查 `extensions.log`：GameSaveCenter 不应在启动阶段创建 967 次 Ludusavi 请求；独立 `LudusaviPlaynite` 的请求需单独记录，不能误归因给本插件。
- [ ] 打开 Dashboard 后手动执行一次刷新，确认这是显式操作，任务/状态可见，期间 Playnite 主界面仍可操作。
- [ ] 重复打开/关闭 Dashboard，确认没有跨线程异常、XAML 资源异常或 Worker 重启循环。
- [ ] 记录启动耗时、首次打开耗时、Worker ready 时间和 Ludusavi find 总数，与 0.6.22 基线对比。

## 0.6.42 超大游戏库启动与表格可读性回归

### PERF-060 500+ 游戏库缓存优先

- [ ] 使用 500、900、1000 款游戏的 Playnite 数据根启动，确认未打开 GameSaveCenter 前不启动其 Worker；打开后先显示 SQLite 缓存，不出现整库 Ludusavi 请求风暴。
- [ ] 确认 `extensions.log` 中 GameSaveCenter 不会因普通库事件反复提交整库同步；点击 Dashboard 的显式刷新后才允许出现批量匹配，并且 UI 不被阻塞。
- [ ] 单独禁用/启用 `LudusaviPlaynite` 做 A/B，对比 `extensions.log` 的 `findTitle` 数量和 Playnite 首次可交互时间。

### UI-059 表格视口与圆角主题

- [ ] 在 1600、1280、1024、850、700 DIP 以及 125%/150% DPI 下确认历史、媒体、任务和诊断表格至少显示多个可读行，底部操作可通过页面滚动访问。
- [ ] 浅色、深色、跟随 Playnite、蓝色宿主、高对比度和关闭透明下确认表格表面、交替行、滚动条、表头和圆角边框一致，没有默认白色 DataGrid。

## 0.6.41 共享上下文操作与低高度回归

### UI-058 禁用操作保留布局

- [ ] 在存档、修改器、媒体和任务工作区切换选中项，确认暂不可用的上下文按钮仍占据稳定位置，不因 disabled 状态消失或造成卡片跳动。
- [ ] 在浅色、深色、跟随 Playnite、蓝色宿主、高对比度和关闭透明下确认 disabled 文本与图标仍可读，且没有系统默认白色按钮。
- [ ] 在 1600、1280、1024、850、700 DIP 以及 125%/150% DPI 下重复操作，确认页面级滚动和表格内部滚动仍可达。

## 0.6.40 页面信息保留与主题兜底回归

源码/自动化当前基线：Core 13、Worker 23、Playnite 92，共 128 项 Release 测试；Release 构建 0 错误（仅 NuGet 漏洞源不可达警告）。真实 Playnite 主题、DPI、键盘和大库回归仍须在具备独立数据根及 PID 边界的环境执行。

### UI-057 低高度信息保留与安全兜底

- [ ] 在 700 DIP 和 125%/150% DPI 下打开 Settings，确认标题说明和保存提示仍可见、可换行，滚动可到达全部表单。
- [ ] 在存档中心低高度窗口确认自动恢复安全提示仍显示，并能通过页面滚动访问历史与操作。
- [ ] 人为触发界面构造失败或加载隔离视图时，确认黑色、白色和高对比度系统主题下文字/背景均可读，不出现固定黑底白字。

## 0.6.39 页面滚动、表格密度与大型库回归

### UI-056 页面级滚动与摘要保留

- [ ] 在 1600、1280、1024、850、700 DIP 下打开 Settings、Overview、Media、Tasks、Maintenance，确认页面摘要不再因高度阈值消失，滚轮/滚动条可到达下方表格和操作区。
- [ ] 确认 SettingsScroller、SidebarNavigationScrollViewer 与六个工作区页面滚动条使用同一套圆角动态主题资源；浅色、深色、跟随 Playnite、蓝色宿主、高对比度和关闭透明均无白色系统滚动条。
- [ ] 确认表格在 700 DIP 和 125%/150% DPI 下仍显示多个完整行，行内滚动与外层页面滚动不互相吞掉输入焦点。

### UI-207 当前游戏上下文与设置页回归

- [ ] 在宽屏 Settings 确认 5 个分类位于约 232 DIP 的左侧有限导航区，内容表单在独立 `SettingsScroller` 内滚动；在窄窗口确认分类改为顶部横向滚动，标题说明和保存提示仍可见。
- [ ] 启动/切换一个真实 Playnite 游戏，确认 Dashboard 当前游戏随 `GameStarted` 事件定位；手动选择另一游戏后普通刷新不抢回，停止游戏后仍保持选择；重新打开页面确认 `GamePickerSelectedGameId` 恢复。
- [ ] 在 GamePicker 中使用“已安装”、搜索、平台和状态筛选，确认当前选择被隐藏时出现恢复提示；“清除搜索”和“显示当前游戏”能恢复可见；列表不因加载真实 Icon 出现明显卡顿。
- [ ] 在 Dashboard、Overview、Save、Trainer、Media 的当前游戏表面确认真实 Playnite Icon 可见；缺失/远程 Icon 确认回退为手柄 glyph。重复执行 Light/Dark/Follow Playnite/高对比度、125%/150% DPI、键盘 Tab/Enter 与连续缩放。

## 0.6.38 大型游戏库、Worker 冷启动与主题令牌最终 UI 回归

### GSC-012 900+ 游戏库启动隔离（0.6.38）

- [ ] 确认 Playnite `playnite.log` 加载的是 `GameSaveCenter 0.6.39`，而不是旧版 0.6.22/0.6.38。
- [ ] 关闭独立 `LudusaviPlaynite` 扩展，冷启动 900+ 游戏 Playnite；确认仅启用 GameSaveCenter 不会在前 60 秒提交整库 Ludusavi 匹配，也不会因 Worker 健康探针失败卡住数分钟。
- [ ] 打开 Dashboard，确认已有 SQLite 缓存可在约 5 秒内显示或进入可用空状态；同步在后台释放，任务事件监听延迟 60 秒。
- [ ] 修改一款游戏的安装路径/平台描述后再次同步，确认旧匹配被清理并重新排队；不变游戏不会反复更新 `updated_utc`。
- [ ] 重新启用独立 `LudusaviPlaynite` 做对照，记录 60 秒内 `find` 请求数、CPU/磁盘占用和命名管道超时；两套集成不应同时在 Playnite 启动阶段发起全库请求。
- [ ] 连续打开/关闭侧栏至少 5 次，确认没有重复 Worker、XAML 资源异常、崩溃对话框或旧版本 DLL 被加载；同时确认 Worker 进程数始终为 1。

### GSC-013 旧版崩溃与 Worker 超时回归（0.6.38）

- [ ] 任何日志出现 `Wpf.Ui.Controls.Button`、`GscSoftShadowColor`、`ContentDialogHost` 或只读 DTO TwoWay 绑定错误时，停止功能测试，确认安装目录已被新 PEXT 完整替换并检查程序集版本 `0.6.39.0`。
- [ ] 人为阻止 Worker 管道创建，确认 Dashboard 首次打开不会等待超过 5 秒缓存请求，Worker 启动失败在真实 30 秒内返回明确状态，不出现 4–5 分钟无响应。

源码/自动化当前基线：Core 13、Worker 23、Playnite UI 91，共 127 项 Release 测试；真实 Playnite 主题、DPI、键盘和大库回归仍须在具备独立数据根及 PID 边界的环境执行。

## 0.6.37 大型游戏库、Worker 单实例与主题令牌最终 UI 回归

### GSC-012 900+ 游戏库启动隔离（历史 0.6.37）

- [ ] 确认 Playnite `playnite.log` 加载的是 `GameSaveCenter 0.6.37`，而不是旧版 0.6.22。
- [ ] 关闭独立 `LudusaviPlaynite` 扩展，冷启动 Playnite，确认仅启用 GameSaveCenter 不会在前 60 秒提交整库 Ludusavi 匹配；大型库在 Dashboard 或游戏启动前也不应自动启动 Worker。
- [ ] 打开 GameSaveCenter Dashboard，确认先显示已有缓存；60 秒空闲后才开始后台同步，手动点击“刷新”才立即接管并执行同步；后台每轮最多处理 64 个已安装/近期游玩条目。
- [ ] 重新启用独立 `LudusaviPlaynite` 做对照，记录 60 秒内 `find` 请求数、CPU/磁盘占用和命名管道超时；两套集成不应同时在 Playnite 启动阶段发起全库请求。
- [ ] 连续打开/关闭侧栏至少 5 次，确认没有重复 Worker、XAML 资源异常、崩溃对话框或旧版本 DLL 被加载；同时确认 Worker 进程数始终为 1。

### GSC-013 旧版崩溃回归

- [ ] 任何日志出现 `Wpf.Ui.Controls.Button`、`GscSoftShadowColor`、`ContentDialogHost` 或只读 DTO TwoWay 绑定错误时，停止功能测试，确认安装目录已被新 PEXT 完整替换并重新检查程序集版本 `0.6.37.0`。

## UI-055 WPF-UI 输入、Popup 与内部滚动回归

- [ ] 在浅色、深色、跟随 Playnite、高对比度和关闭透明模式打开设置、存档、修改器和维护中心，确认 TextBox/ComboBox/Button/CheckBox 均保持圆角、动态前景和可见焦点，不出现宿主白色 Popup。
- [ ] 在诊断摘要、长路径、搜索和多行只读输入中制造超出内容，确认滚动条出现在控件内部，页面不产生不可见的裁切；ComboBox 超过 320 DIP 时 Popup 内部滚动可用。
- [ ] 在 700、850、1024、1280、1600 DIP 与 Windows 125%/150% DPI 下验证 Popup 定位、圆角边缘、列表拖动和键盘方向键/Enter/Esc 行为。

### UI-056 表格高度与来源规则滚动

- [ ] 在首页、存档、媒体、任务和维护中心确认表格初始可见高度至少显示约三行，数据变多后滚动条出现在表格内部，页面标题、工具栏和操作区不会被表格挤走。
- [ ] 在媒体“来源规则”中制造较长目录和多条来源，确认上方表单在有限高度内局部滚动、下方来源表格保持星号行和内部滚动，窗口缩放不会出现双重无限滚动或内容裁切。

源码/自动化当前基线：Core 13、Worker 21、Playnite UI 65，共 99 项 Release 测试；真实 Playnite 主题、DPI、键盘和大库回归仍须在具备独立数据根及 PID 边界的环境执行。

## UI-050 共享页签、滚动与勾选主题回归

- [ ] 在 1600、1280、1024、850、700 DIP 下进入存档、修改器、媒体、任务和维护中心；确认页签为圆角胶囊，最后一个页签可以通过横向滚动访问，内容区没有页面级水平溢出。
- [ ] 在首页风险项较多、GamePicker 游戏数为 1000、修改器/FLiNG 结果较多时确认局部纵向滚动条出现且可拖动，列表保持 Recycling，不遮挡操作按钮。
- [ ] 在浅色、深色、跟随 Playnite、高对比度和关闭透明下确认页签、复选框勾选标记、选中行和焦点环均使用可读的动态前景；不得出现固定白色/黑色文字。
- [ ] 使用 Tab、方向键、Enter、Esc 访问页签、GamePicker 和列表；页面卸载后重复打开不产生 Dispatcher、绑定或资源解析异常。

## UI-051 快速游戏切换与详情回写回归

- [ ] 在存档、媒体和修改器中心连续快速选择至少 20 款游戏，确认详情最终只显示最后一次选择的游戏；旧游戏的历史、媒体、来源、工具不会在新选择后回写。
- [ ] 在上一项详情仍加载时切换工作区、打开/关闭 GamePicker、按 Esc 或卸载 Dashboard；确认待处理详情回调被取消/忽略，重新打开后不会出现 Dispatcher、绑定或集合线程异常。
- [ ] 使用键盘快速输入、方向键和 Enter 连续切换，确认不会为 `SelectedItem`/`SelectedGame` 各排队一份重复详情请求，业务 Worker 任务不受影响。
- [ ] 在 700、850、1024、1280 DIP 及 125%/150% DPI 下重复上述步骤，确认滚动条、焦点环和当前游戏上下文仍可见。

## UI-052 大库 GamePicker 批量刷新回归

- [ ] 使用 100、500、1000 款游戏打开 Dashboard，确认缓存摘要先显示，刷新期间 UI 不逐项闪烁、长时间卡住或重复创建 GamePicker 项。
- [ ] 在刷新游戏库后确认 GamePicker 列表只发生一次批量更新，搜索、平台筛选、状态筛选、排序和当前游戏回退仍正确。
- [ ] 在 1600、1280、1024、850、700 DIP 及 125%/150% DPI 下打开 GamePicker 并快速滚动，确认虚拟化/Recycling 生效、列表滚动条可见、操作按钮不被覆盖。
- [ ] 反复打开/关闭 Dashboard 后确认批量刷新没有残留事件、Dispatcher 或 CollectionView deferred-refresh 异常。

## UI-053 GamePicker 键盘与确认行为回归

- [ ] 打开 GamePicker 后使用方向键连续浏览，确认抽屉不会提前关闭；按 Enter 后关闭并保留最后选择。
- [ ] 在搜索框、筛选器和列表之间使用 Tab，按 Esc 可关闭抽屉；鼠标点击列表项只关闭一次，不产生重复详情加载。
- [ ] 在无选中项、空状态、快速输入和 125%/150% DPI 下验证 Enter/Esc、焦点环和 Tooltip 仍然可见且不会抛出事件异常。

## UI-049 工作区功能入口补齐回归

- [ ] 媒体中心选择截图/录像后确认图片/视频预览、备注和收藏保存、批量收藏/备注、重新归类及来源新增/启停/移除均可用；媒体目录长路径仍有 Tooltip。
- [ ] 修改器中心导入包含多个 EXE 的 ZIP/目录时显示主程序确认面板；活动版本、管理员权限、启动延迟和退出后关闭保存后仍能被 Worker 使用。
- [ ] 维护中心设备状态显示摘要和决策记录；远端恢复必须按“下载并校验 → 创建快照并恢复”顺序工作，未校验时恢复按钮不可用。
- [ ] 在 1600、1280、1024、850、700 DIP 与 125%/150% DPI 下确认 FLiNG 结果/版本上下堆叠、任务摘要收起、列表滚动、焦点环和键盘导航。

## UI-048 四个新增物理工作区回归

- [ ] 独立 Playnite 实例依次进入存档、修改器、媒体、维护，确认对应 `SaveWorkspaceTab`、`TrainerWorkspaceTab`、`MediaWorkspaceTab`、`MaintenanceWorkspaceTab` 是唯一可见内容，旧标签不重复显示。
- [ ] 存档中心验证历史版本选择、备注/锁定、恢复、比较、撤销、候选路径接受/忽略和策略保存仍调用真实命令；窄窗口下表格、按钮和滚动区域不重叠。
- [ ] 修改器中心验证多个工具/CT、启用/自动启动开关、启动/保存/打开目录/解除绑定、FLiNG 搜索和下载绑定；1000 个游戏库下列表仍为 Recycling。
- [ ] 媒体中心验证待归类、当前媒体、来源规则、预览和打开目录；长路径 Tooltip、缩放、主题和媒体归类命令可用。
- [ ] 维护中心验证诊断、关注项、设备状态、审计、进程映射、复制/刷新/保存/删除命令；Worker 断开和页面卸载时无 Dispatcher 或绑定异常。
- [ ] 在 1600、1280、1024、850、700 DIP 与 125%/150% DPI 下检查四个工作区的页签、DataGrid、空状态、焦点环和键盘导航。

## UI-043 首页物理工作区回归

- [ ] 独立 Playnite 实例打开首页，确认渲染的是 `OverviewWorkspaceTab`，旧隐藏 Tab 不会同时显示或抢占选中内容。
- [ ] 在 1600、1280、1024、850、700 DIP 与 125%/150% DPI 下检查首页双栏/堆叠切换、最近任务滚动、风险提醒和关注原因按钮。
- [ ] 点击关注原因行，确认仍跳转维护中心并选中同一 `ValidationFindingDto`；刷新、Worker 断开和页面重开后不出现重复订阅或绑定错误。
- [ ] 浅色、深色、跟随 Playnite、高对比度、关闭透明和关闭动画下确认首页使用共享资源，普通卡片不出现大面积模糊。

## UI-121/UI-122 首页整体状态横幅与状态胶囊回归

- [ ] 首页「今日工作台」TODAY 横幅按真实 `Snapshot.*` 显示三种状态：有需处理项目（黄）、整体状态安全（绿）、Worker 异常（红），不出现 Demo 假数据。
- [ ] 胶囊行按 `WarningGames` 互斥显示「无需处理」或「N 项需处理」，隐藏的一侧整颗胶囊一起消失，不得残留空白胶囊框。
- [ ] Worker/Ludusavi 胶囊随健康状态切换文案与状态点；云端队列、待归类媒体胶囊显示真实计数。
- [ ] 在 1600、1280、1024、850、700 DIP 与 125%/150% DPI 下检查横幅与胶囊不裁切、可换行，窄窗口右侧胶囊不把左侧标题挤出视口。

## UI-044 任务中心物理工作区回归

- [ ] 独立 Playnite 实例打开任务中心，确认任务中心使用 `TaskWorkspaceTab`，旧隐藏 Tab 不会同时显示。
- [ ] 验证任务状态、游戏和类型筛选只改变本地视图，不取消、重排或重新执行后台任务；100、500、1000 游戏库下任务表仍保持滚动和 Recycling。
- [ ] 选择运行中、失败、已完成任务，确认状态点、进度、详情和复制/安全重试/取消动作均与真实任务一致；Worker 断开或页面关闭时无 Dispatcher 异常。
- [ ] 在 1600、1280、1024、850、700 DIP 与 125%/150% DPI 下检查摘要卡、筛选器、长详情文本、键盘 Tab/Enter/Esc 和浅深/高对比度/关闭透明/动画模式。

## UI-042 全局游戏上下文回归

- [ ] 使用 100、500、1000 款游戏验证首次打开先显示 SQLite/Worker 缓存，GamePicker 不因键盘搜索逐字符请求 Worker。
- [ ] 验证顶部唯一 GamePicker：Expanded、Standard、Compact、Narrow 都能打开同一搜索/筛选/排序抽屉；首页不再显示重复游戏 ComboBox，任务中心和维护中心保持全局视角。
- [ ] 验证已安装、全部、已匹配、有备份、需处理、未匹配、平台筛选和名称/最近游玩/最近备份排序；删除当前游戏或筛选为空时能回退到第一个可见游戏。
- [ ] 连续快速输入、快速切换游戏、Esc 关闭抽屉、Tab/方向键/Enter 选择；确认旧筛选被取消且不会出现 Dispatcher 回调或绑定错误。
- [ ] 在 1600、1280、1024、850、700 DIP 与 125%/150% DPI 下检查顶部入口、抽屉高度、虚拟化/Recycling、长名称 Tooltip、焦点环和状态摘要。
- [ ] 首页“需要关注”卡片必须显示具体游戏、标题/原因摘要；打开维护中心后 FindingsGrid 自动滚动到首个 Warning/Attention finding。

## UI-005 全功能视觉重构与响应式最终验收

- [ ] 运行根目录一键构建安装：restore、Release build、Core 13 + Worker 21 + Playnite 53（总计 87）项测试、package、Worker smoke 全部成功；确认安装 DLL 与 stage SHA-256 一致。
- [ ] 首次打开 Dashboard，连续关闭/重开至少 10 次，并依次进入首页、存档、修改器、媒体、任务、维护和设置；日志不得出现 XamlParseException、资源键缺失、Dispatcher、绑定回写或 WPF-UI Host 异常。
- [ ] 在 1600×900、1366×768、1280×720、1100×700、980×640，并在 1280/980/880 DIP 临界点上下连续拖动窗口；标题、操作、全局游戏选择器和页面内容不得重叠或突然越界。
- [ ] 在 Compact/Narrow 验证六个导航项保持 48×48 居中；选中背景不越出侧栏；Worker/Ludusavi W/L 状态卡保持 48×50、状态点可见且均不裁切。键盘、Tooltip 和 Automation Name 仍可访问。
- [ ] 在存档、修改器和媒体中心切换至少 20 个游戏；选择状态跨页面保持，Expanded 完整游戏库与非 Expanded “游戏库”入口均保留搜索、状态筛选、排序和大型库 Recycling。
- [ ] 对存档页验证备份历史、选中版本详情、锁定、备注、比较、恢复、撤销、候选路径和保留策略命令仍可达；恢复必须保持确认、PreRestore、校验和互斥安全链路。
- [ ] 对修改器/媒体/任务/维护/设置逐项验证原有按钮、开关、输入、筛选、导入导出、错误/空状态、重试、设备决策和日志入口，无命令丢失或假成功。
- [ ] 在 100%/125%/150%/200% DPI、Light/Dark/Follow Playnite、至少一个社区主题、高对比度、关闭玻璃和关闭动画下重复关键路径；文本、Popup、焦点环、DataGrid、ScrollBar、Snackbar 均可读且不污染 Playnite 其他页面。

## UI-002 共享控件与主题回归

- [ ] 在独立 Playnite 测试实例中确认设置卡片、普通/主按钮、文本与数值输入、ComboBox、CheckBox、Slider、Tooltip、ProgressBar、DataGrid、ListBox、TabControl、Popup 和滚动条均未回退到宿主默认样式。
- [ ] 分别以鼠标和键盘验证 Normal、Hover、Pressed、Disabled、Focus、选择和验证错误状态；Tab 焦点环应可见，长路径/错误文本应能通过 Tooltip 或详情读取。
- [ ] 在浅色、深色、跟随 Playnite、高对比度、关闭透明与关闭动画条件下复验；环境光应在降级条件下隐藏，列表/表格滚动不得存在大型 BlurEffect 或失去 Recycling 虚拟化。
- [ ] 在 1600×900 至 980×640 与 100%/125%/150%/200% DPI 检查设置页滑块、策略数值框、Dashboard 页签/表格与弹层，记录任何裁切、重叠或文字对比不足。

## UI-003 响应式工作区、键盘与紧凑工具栏

- [ ] 在独立 Playnite 数据根从 1600×900 缩至 980×640：检查侧栏仅在高度不足时滚动，所有六个工作区仍能经键盘到达；紧凑模式的三个图标操作显示 Tooltip，并以 Enter/Space 执行原命令。
- [ ] 在 100%/125%/150%/200% DPI 验证设置页的长路径、两列备份格式设置和滚动条：内容不得被裁切；因空间不足出现横向滚动时，滚动条和 Tab 顺序必须可用。
- [ ] 以浅色、深色、跟随 Playnite、高对比度、关闭透明和关闭动画打开 Dashboard 与 Settings；检查环境光正确降级、焦点环可见、没有 XAML/绑定/Dispatcher 异常。

## UI-001 WPF-UI 局部兼容性 POC

- [ ] 在独立 Playnite 测试实例安装本次 PEXT，不停止或覆盖用户日常实例；确认扩展加载时无 `FileNotFoundException`、XAML 资源或程序集绑定错误。
- [ ] 在不点击“加载界面探针”时确认 Dashboard 与维护中心正常打开；随后点击加载，若 WPF-UI 资源或宿主不可用，确认恢复面板和“加载界面探针”重试入口仍可见，且可切换到其他维护页。
- [ ] 打开维护中心“界面探针”，依次验证 Button、ToggleSwitch、TextBox、NumberBox、ComboBox Popup、列表键盘焦点、ProgressRing 与标准 DataGrid 不影响宿主页面。
- [ ] 触发 POC Dialog 和 Snackbar，分别使用 Esc/关闭按钮关闭；模拟宿主不支持时确认在插件内部显示错误且 `playnite.log` 有诊断，而非未处理异常。
- [ ] 测试浅色、深色、跟随 Playnite、100%/125%/150%/200% DPI、关闭透明和高对比度；关闭 POC 后打开 Playnite 其他页面，确认不存在全局样式污染。

- [x] 2026-07-31 本地开发安装报告确认 `extension.yaml` 为 `0.6.22`、主 DLL 为 `0.6.22.0`；Playnite `playnite.log` 已记录加载 `GameSaveCenter, version 0.6.22`，Dashboard 侧栏也显示 `v0.6.22`。
- [x] 2026-07-31 在真实 Playnite 的匹配游戏策略中将分钟框临时编辑为 `1440`（完整显示），再输入 `0` 验证共享输入框立刻显示红色范围错误边框，最后恢复 `30`；全程未点击保存策略、未执行备份、恢复、删除或云端操作。
- [ ] 在浅色、深色和跟随 Playnite 中检查 Dashboard/设置页的状态图标、环境光、安全提示、浮层和主按钮；关闭毛玻璃或启用高对比度时，页面不能保留高成本环境光或失去文字对比度。

## 0.6.21 云端退避、数值输入与完整 WPF UI 回归

- [x] 2026-07-31 本地开发安装报告已确认 extension.yaml 为 `0.6.21`、主 DLL 为 `0.6.21.0`；Playnite `playnite.log` 也记录 `Loaded plugin: GameSaveCenter, version 0.6.21`。
- [x] 2026-07-31 在 1600×900 的真实 Playnite 中打开 Dashboard/存档中心，Worker 正常；策略分钟框从 `30` 临时编辑为 `1440`、失焦后完整显示，再恢复为 `30`，未点击保存策略、未执行备份/恢复/云端操作。
- [ ] 使用隔离测试游戏和测试存档目录：将“游玩中备份”间隔依次改为 `12`、`120`、`1440`，离开输入框或点击保存后数值完整保留；输入 `0`、`1441` 和文本时显示错误且不保存。
- [ ] 使用隔离 Rclone 目标断网/配置错误触发一次本地成功后的云端失败；确认 SQLite 队列保存并依次安排 1、5、15、60、240、720 分钟，不在未配置期间每 30 秒产生任务。
- [ ] 恢复测试目标后确认单向 copy 成功、队列被清除、真实用户存档和远端目录未被删除或镜像。
- [ ] 在 1600×900、1366×768、1280×720、1100×700、980×640 和 100%/125%/150%/200% DPI 检查 Sidebar、工具栏、策略、DataGrid、Tab、ScrollBar、ComboBox 和所有数值框；浅色、深色、跟随 Playnite、关闭毛玻璃/动画及键盘 Tab 焦点均可用。

## 0.6.20 Dashboard 跨线程刷新崩溃回归

- [ ] 安装后确认扩展管理器为 `0.6.20`，主 DLL 文件版本为 `0.6.20.0`。
- [ ] 保持 Dashboard 打开，分别完成、失败和取消备份/媒体同步/修改器下载任务各至少十次；不得弹出插件崩溃窗口。
- [ ] 将自动刷新设为最短间隔，并在 Worker 响应缓慢时反复切换页面、关闭重开 Dashboard；`extensions.log` 不得出现“调用线程无法访问此对象”、`DashboardView.OnViewModelPropertyChanged` 或 `AsyncMethodBuilderCore.ThrowAsync`。
- [ ] Worker 重启、事件通道短暂重连、后台同步失败时，状态提示仍可显示且 Playnite 保持可操作。

## 0.6.18 Worker 任务事件推送回归

- [ ] 安装后确认扩展管理器为 `0.6.18`，主 DLL 文件版本为 `0.6.18.0`。
- [ ] 打开任务中心并把自动刷新间隔临时设为 60 秒；启动备份、媒体同步或修改器下载后，Queued、Running、进度和终态仍应在数秒内更新，不能等待 60 秒。
- [ ] 任务正常结束、失败和取消各验证一次：任务行、进度、错误详情和通知不得重复或丢失。
- [ ] 保持面板打开时重启 Worker：事件通道可短暂断开，但随后必须自行恢复；手动刷新或下一轮长轮询后任务状态与 SQLite 一致。
- [ ] 关闭 GameSaveCenter 页面后继续执行一个长任务：Worker 不得因没有事件订阅者报错、阻塞或增加明显 CPU；重新打开页面后应由 SQLite 快照显示最终状态。
- [ ] 在 1000 游戏库和连续高频进度任务下观察 Playnite：面板可保持操作，事件延迟或丢弃时不得影响实际备份、恢复或媒体任务正确性。

## 0.6.2 多设备摘要与冲突回归

- [ ] 安装后确认扩展管理器与 `extension.yaml` 为 `0.6.2`，主 DLL 为 `0.6.2.0`。
- [ ] 未配置 Rclone 时，刷新设备状态只生成本机摘要并清楚说明未读取远端，不得失败或泄露路径/凭据。
- [ ] 使用两个测试设备和空测试 Remote：两端刷新后能读取对方 sidecar，且 Remote 仅出现各自 `DeviceState/*.json` 摘要。
- [ ] 两台设备同一游戏在十分钟内产生不同摘要时，设备状态显示“需要人工决定”；不得自动下载、恢复、删除或覆盖任何存档。
- [ ] sidecar 仅含游戏 ID/名称、备份 ID、时间、总大小与文件数；不得包含存档路径、文件清单、Rclone 目标或凭据。

## 0.6.1 关注项、云传输与任务增量回归

- [ ] 安装后确认扩展管理器与 `extension.yaml` 为 `0.6.1`，主 DLL 为 `0.6.1.0`。
- [ ] 人为保留一条 Warning 或 Error finding；点击首页“需要关注”卡片后必须进入维护中心“异常与日志”，选中对应条目，并能看到游戏名、详情和建议处理方式。
- [ ] 没有 finding 时点击该卡片不得异常，并显示“当前没有需要处理的关注项”。
- [ ] 管理面板打开且没有任务运行时，连续观察三轮自动刷新：Worker 不应每 10 秒重建完整 Dashboard；一分钟后仍应完成一次缓存校准。
- [ ] 启动备份、媒体同步或修改器下载，确认任务状态变化可在下一轮刷新显示，成功/失败仍只通知一次。
- [ ] 配置可丢弃测试存档与 rclone，故意让上传保持运行后执行恢复：恢复必须先等待上传结束，恢复期间不得启动新的云复制。
- [ ] Worker 仍记录游戏会话或已记录游戏 PID 存活时，恢复必须被拒绝；进程退出后可继续进入 PreRestore 流程。
- [ ] 使用超过 2 GiB 的模拟 FLiNG 响应或超过 ZIP 条目/解压体积上限的测试包，下载或导入必须失败且不遗留新版本目录。

## 0.5.11 页签右圆角回归

- [ ] 安装后确认扩展管理器与 `extension.yaml` 为 `0.5.11`，主 DLL 为 `0.5.11.0`。
- [ ] 在诊断/异常与日志、修改器/已安装/FLiNG 在线库、媒体页三级页签中检查选中与未选中按钮：四个圆角都完整，尤其右上与右下不得被裁成直线。
- [ ] 在 100%、125%、150%、200% DPI 下检查页签间距为稳定 8 DIP，右侧描边不缺失，页签内容布局不回归居中。

# Windows 真机验证计划

每项结果记录为：`通过 / 失败 / 跳过`，并保存错误日志。不要在同一轮同时开启本地备份、恢复和云端双向行为。

## UI-004 生产 WPF-UI 局部迁移回归

- [ ] 在 Windows 工作区先运行 `python scripts/validate-source.py`、Release restore/build、全部 66 项测试（Core 13、Worker 21、Playnite 32）、package 与 Worker `0.6.22.0` smoke；任何 XAML 编译、WPF-UI API、程序集复制或资源错误都必须先修复，不能直接安装试错。
- [ ] 仅在 ENV-001 已证明独立 AppData、扩展目录、测试库和 PID 边界后安装本次 PEXT；不停止、覆盖或连接用户日常 Playnite/Worker。
- [ ] 依次打开 Dashboard 六个工作区与 Settings，检查 6 个指标卡、所有 WPF-UI 动作按钮、策略/工具/媒体/设置开关、文本输入和 ComboBox；原 Command、Binding、Tooltip、Automation Name、Disabled/隐藏行为和数值校验必须保持。
- [ ] 打开 Dashboard 后关闭再打开一次，并打开 Settings；`playnite.log` 不得新增 `Wpf.Ui.Controls.Button`、`GscSoftShadowColor`、`StaticResourceHolder` 或 `XamlParseException`。这是 GSC-083/GSC-084 的实际加载回归，不能以单元测试替代。
- [ ] 分别触发成功/警告/错误通知：成功与警告显示 Snackbar，错误仍显示可打开详情的本地 Toast；模拟 WPF-UI 宿主异常时回退入口可用且 `playnite.log` 有诊断。
- [ ] 分别触发普通与危险确认：插件内 Dialog 的确认/取消/Esc、危险按钮外观、重复请求安全取消均正确；确认未注册 WPF-UI `ContentDialogHost`，原 `TaskCompletionSource` 不得挂起调用方。
- [ ] 导出设置到本地与网络测试路径、导入正常/超 1 MiB/缺失路径/非法 JSON 文件；UI 不冻结，报告使用 MessageBox，取消设置页仍保持原有保存语义。
- [ ] 在 Light、Dark、Follow Playnite、高对比度、关闭透明和关闭动画下切换两页；验证 Card、Button、ToggleSwitch、输入、Popup、焦点环与现有表格主题一致，关闭插件页后 Playnite 其他页面不受影响。
- [ ] 在 1600×900、1366×768、1280×720、1100×700、980×640 与 100%/125%/150%/200% DPI 使用 Tab、Shift+Tab、方向键、Enter、Space、Esc；长中文、开关状态、下拉 Popup、窄屏横向访问和浮层均不裁切。
- [ ] 使用约 1000 游戏、1000 媒体和大量任务数据滚动并切换筛选；DataGrid/ListBox 仍为 Recycling，不能因 WPF-UI 按钮或卡片迁移出现全量实例化、N+1、明显卡顿或失去键盘选择。

## 0.6.13 远端隔离下载与恢复回归

- [ ] 两个测试设备分别上传完整 Ludusavi 库，刷新设备状态后远端设备与 Backup ID 显示正确。
- [ ] “下载并校验”只写入本机 `RemoteBackups` 隔离目录，不改变正式备份目录和实际游戏存档。
- [ ] 正常下载通过 Rclone 哈希检查；中断、篡改远端文件或错误 Backup ID 均拒绝签发可恢复暂存。
- [ ] 切换设备比较记录会清除当前 UI 暂存选择；伪造设备名或暂存 ID 被 Worker 拒绝。
- [ ] 第二次确认后先创建并锁定本机 PreRestore，再从隔离库恢复所选版本。
- [ ] 模拟目标恢复失败时使用本机 PreRestore 回滚；游戏仍运行时恢复被阻止。
- [ ] 七天过期暂存不可恢复；下一次暂存会尽力清理过期目录。

## 0.6.12 媒体缩略图与录像预览回归

- [ ] 扩展管理器显示 `0.6.13`，主 DLL 文件版本为 `0.6.13.0`。
- [x] 自动测试验证 100 张缩略图均冻结、转换后文件可删除且缓存不超过 96 项。
- [ ] 使用 1000+ 图片快速滚动，确认只为可见行加载缩略图，长时间滚动内存趋于稳定。
- [ ] 依次选择 MP4、WMV、AVI、MOV、损坏录像与缺失文件；页面不得崩溃，系统“打开媒体”入口仍可用。
- [ ] 切换截图与录像后旧视频停止占用资源；内嵌预览静音，系统播放器可正常有声播放。
- [ ] 在 960×640、100%–200% DPI 和浅/深/社区主题下检查缩略图列、预览区与批量按钮布局。

## 0.6.11 媒体域模块拆分回归

- [ ] 扩展管理器显示 `0.6.11`，主 DLL 文件版本为 `0.6.11.0`。
- [x] Release 编译、源码门禁、Worker SQLite 与 Playnite 设置测试通过。
- [ ] 在媒体页回归扫描、筛选、收藏/备注、批量操作、重新归类、收件箱归类与忽略。
- [ ] 确认错误传播、确认框、成功提示和本地文件打开行为与 0.6.10 一致。

## 0.6.10 设置迁移自动化与宿主回归

- [ ] 扩展管理器显示 `0.6.10`，主 DLL 文件版本为 `0.6.10.0`。
- [x] 自动测试验证 SchemaVersion=1 往返、旧字段默认值、非法枚举/数值、超大输入与缺失路径报告。
- [x] 自动测试验证非法导入保持原编辑值，缺失路径检查不创建文件或目录。
- [ ] 在设置页导入后点击取消，重新打开设置页应恢复原值。
- [ ] 再次导入并保存，重启 Playnite 与 Worker 后路径、功能开关和备份参数仍一致。

## 0.6.9 多设备人工决策回归

- [ ] 扩展管理器显示 `0.6.9`，主 DLL 文件版本为 `0.6.9.0`。
- [ ] 对真实分叉分别记录四种决策与备注，刷新和重启 Worker 后仍正确显示。
- [ ] 决策操作只新增 SQLite/审计记录；Rclone 远端及本地存档目录不得发生文件变化。
- [ ] sidecar 的备份 ID 更新后历史决策仍可见，但用户能重新记录最新判断。

## 0.6.8 媒体批量元数据回归

- [ ] Playnite 扩展管理器显示 `0.6.8`，主 DLL 文件版本为 `0.6.8.0`。
- [ ] 使用 Ctrl/Shift 选择多个媒体，批量收藏与取消收藏后所有所选记录和收藏摘要同步变化。
- [ ] 输入备注并批量应用，刷新、切换游戏和重启后均持久化；未选记录保持不变。
- [ ] 在搜索及类型/收藏筛选结果中批量操作，只更新用户实际选择的项目。
- [ ] 批量操作前后归档和原始文件路径、大小、哈希均不变化。
- [ ] 在窄窗口与 125%/150%/200% DPI 下批量按钮可换行且不遮挡预览或重新归类操作。

## 0.6.7 媒体页崩溃、检索与预览回归

- [ ] Playnite 扩展管理器显示 `0.6.7`，主 DLL 文件版本为 `0.6.7.0`。
- [ ] 打开媒体页并在有媒体、无媒体的游戏之间连续切换至少 20 次，不出现扩展崩溃窗口。
- [ ] 核对总量、截图、录像、收藏与空间占用正常显示，并切换跟随 Playnite、浅色、深色三种主题。
- [ ] 检查 `playnite.log`，不得出现 `MediaStorageSummaryDto.TotalSizeDisplay`、TwoWay 或 OneWayToSource 相关未处理异常。
- [ ] 依次按文件名、备注和来源搜索，结果即时变化且不触发媒体目录重扫。
- [ ] 全部、截图、录像、收藏四种筛选结果正确；收藏状态保存后筛选立即更新。
- [ ] 选择 PNG/JPEG/BMP 截图时显示预览，原文件仍可被移动或删除，证明没有残留文件句柄。
- [ ] 选择录像、损坏图片或不支持格式时不崩溃，仍可使用“打开媒体”。
- [ ] 使用 4K/8K 截图和 1000+ 媒体验证选择切换、滚动与内存占用。
- [ ] 在 960×640 与 125%/150%/200% DPI 下检查筛选区、预览和操作按钮不重叠。

## 0.6.6 设置迁移与媒体元数据回归

- [ ] Playnite 扩展管理器显示 `0.6.6`，主 DLL 文件版本为 `0.6.6.0`。
- [ ] 设置导出 JSON 包含 `schemaVersion: 1`，且不包含 Rclone 密码或凭据正文。
- [ ] 修改设置后导入 JSON，界面显示导入值；点击取消后原值恢复。
- [ ] 再次导入并保存，重启 Playnite 和 Worker 后设置仍存在。
- [ ] 把程序路径改为不存在的位置，迁移报告逐项提示且不自动创建文件。
- [ ] 核对媒体总量、类型、收藏数和空间占用与 SQLite/归档目录一致。
- [ ] 收藏并写入备注，切换游戏、刷新和重启后元数据持久化。
- [ ] “打开媒体”调用系统默认程序；“在目录中显示”只定位归档文件。
- [ ] 归档文件缺失时显示可理解错误，不影响其他媒体记录。
- [ ] 在 1000+ 媒体记录下验证摘要查询、列表加载和滚动无明显卡顿。

## 0.6.5 任务事件、云端重试与修改器导入回归

- [ ] 安装后确认扩展管理器与 `extension.yaml` 为 `0.6.5`，主 DLL 为 `0.6.5.0`。
- [ ] 关闭 Dashboard 后触发自动备份：任务完成或失败后应在数秒内通知且只通知一次；空闲时日志不得每 5 秒读取完整任务历史。
- [ ] Worker 在长轮询期间重启后，通知监测应自动恢复，不补发 Playnite 启动前的历史任务。
- [ ] 制造 `RCLONE_COPY_FAILED` 后点击重试：新任务类型应为“云端上传”，不得创建新的 Ludusavi 本地历史版本。
- [ ] 云端重试开始、成功、再次失败时，游戏列表分别显示待上传、已上传、上传失败。
- [ ] 导入含两个以上候选 EXE 的 ZIP 和目录：必须出现插件内主程序选择器；取消不得创建工具记录或复制正式版本目录。
- [ ] 选择非最大 EXE 完成导入，启动时必须运行用户选中的文件。
- [ ] 为同一 FLiNG 工具下载两个版本，在 Inspector 切换活动版本并保存；手动启动和随游戏启动均使用所选版本。
- [ ] 在 960×640、125%/150%/200% DPI 检查主程序选择栏不覆盖工具列表与 Inspector。

## 0.5.10 页签与布局回归

- [ ] 安装后确认扩展管理器与 `extension.yaml` 为 `0.5.10`，主 DLL 为 `0.5.10.0`。
- [ ] 任务中心“任务”、维护中心“诊断/异常与日志”、媒体中心三级页签、修改器中心两级页签均显示完整四角圆角，页签下方没有横向滚动条伪装成的贯穿线。
- [ ] 维护中心诊断按钮和摘要卡片位于内容顶部，不再垂直居中。
- [ ] 媒体中心“来源与规则”表单位于内容顶部，不再漂浮在页面中央。
- [ ] 修改器已安装与 FLiNG 在线页从顶部开始排版，列表与设置面板不再整体居中。
- [ ] 存档、媒体、任务表格复选框仍保持 0.5.9 的圆角紫色样式。

## A. 构建门禁

- [ ] `dotnet --info` 显示 .NET 8 或更高版本的稳定版 SDK（.NET 9 可用）。
- [ ] `dotnet restore GameSaveCenter.sln` 成功。
- [ ] `dotnet build GameSaveCenter.sln -c Release` 无错误。
- [ ] `dotnet test tests/GameSaveCenter.Core.Tests -c Release` 全部通过。
- [ ] `scripts/package.ps1` 生成 Worker 与扩展目录。
- [ ] Playnite 启动后插件无加载错误。

## 0.5.9 控件几何、表头与搜索交互回归

- [ ] 安装后确认扩展管理器与 `extension.yaml` 为 `0.5.9`，主 DLL 为 `0.5.9.0`。
- [ ] 在约 1000 款游戏、2600 条媒体和长任务列表中拖动纵横滚动条；Thumb 必须始终为规则圆角矩形，不能呈尖角、透镜形或被轨道裁切。
- [ ] 检查存档、修改器、媒体、首页和维护页一级/二级页签：四角必须一致，不能出现仅左上圆角或宿主蓝线穿过按钮。
- [ ] 检查每个 DataGrid：外框上圆角与首末表头吻合，表头背景不得越过外框；所有锁定/选择复选框使用共享圆角主题模板。
- [ ] 检查刷新、全部备份、同步媒体及各操作按钮：字体图标与文字在同一水平线，整体水平/垂直居中。
- [ ] 聚焦游戏和 FLiNG 搜索框时 Watermark 立即隐藏；输入后清除按钮出现并垂直居中，点击清空后焦点仍在输入框。
- [ ] 在深色、浅色、跟随 Playnite、100%/125%/150%/200% DPI 下重复上述检查，确认设置页同类输入控件也不回退为宿主默认样式。

## 0.5.8 WPF UI、滚动条与插件内反馈回归

- [ ] 安装后确认扩展管理器与 `extension.yaml` 为 `0.5.8`，主 DLL 为 `0.5.8.0`，Playnite 左侧显示新的“手柄 + 存档”图标。
- [ ] 使用约 1000 款游戏打开存档/媒体中心，纵向滚动条 Thumb 必须保持规则圆角胶囊，拖动、轨道点击和鼠标悬停均正常。
- [ ] 在存档、媒体、任务、诊断表格检查：表头和普通列水平/垂直居中，路径/详情列左对齐，状态点与文字同一基线，进度条与百分比居中。
- [ ] 拖动 DataGrid 列宽并触发水平滚动；长路径、文件名和错误信息省略后悬停可查看完整 Tooltip。
- [ ] 在最大化、普通窗口和 980 DIP 附近缩放，页面按钮不得与 Playnite 最小化、最大化、关闭按钮重叠，紧凑侧栏品牌图标不得裁切。
- [ ] 局部页签在浅色、深色、跟随 Playnite 三种模式下呈圆角 Pill，Normal/Hover/Selected/Focused 状态清晰。
- [ ] 分别执行恢复、撤销恢复、解绑修改器、忽略媒体与取消任务；确认框应位于插件内部，ESC 可取消，危险操作默认焦点落在安全按钮。
- [ ] 管理面板打开时执行自动备份、媒体同步和修改器下载，结果应显示静默 Toast 且不抢焦点；关闭面板时继续回退 Playnite 通知。
- [ ] 验证 100%、125%、150%、200% DPI 与系统关闭动画/高对比度，确认动画降级、图标、滚动条、弹窗和 Toast 均可用。

## 0.5.7 大型游戏库缓存与增量同步回归

- [ ] 安装后确认扩展管理器和 `extension.yaml` 为 `0.5.7`，主 DLL 为 `0.5.7.0`。
- [ ] 使用现有约 1000 款游戏的 SQLite 数据启动 Playnite；首次点击 GameSaveCenter 时应先显示缓存列表，不得等待全部 Ludusavi 匹配。
- [ ] 连续关闭并重新打开面板三次，列表与首页应在数秒内可操作；Worker 日志不得再次出现约 1000 次 Ludusavi Find。
- [ ] 连续触发库更新、安装和卸载事件；五分钟内相同库指纹只执行一次游戏 Upsert。
- [ ] 修改一款游戏的名称或平台 ID，只允许该游戏重新匹配；新增游戏只匹配新增项。
- [ ] 打开首页、任务和维护中心时，不读取当前游戏的备份、媒体、候选路径和修改器列表。
- [ ] 在存档、媒体、修改器中心切换同一游戏，分别只加载当前模块数据，不固定发出五个顺序 IPC。
- [ ] 打开已有存档历史时先显示 SQLite 索引；点击历史刷新后才校准 Ludusavi，真实备份完成后新历史仍立即出现。
- [ ] 保持 Dashboard 打开至少三轮自动刷新，确认没有 Ludusavi version 外部进程每十秒启动。
- [ ] 记录冷启动、首次打开、二次打开和切换游戏耗时，与 0.5.6 对比并保存 Worker 日志。

## 0.5.6 设计系统与信息密度回归

- [ ] 安装后确认扩展管理器和 `extension.yaml` 为 `0.5.6`，主 DLL 为 `0.5.6.0`。
- [ ] 在深色、浅色和跟随 Playnite 三种主题中选择游戏、修改器、媒体、任务及下拉选项；文字不得变黑，Tab 键焦点应为圆角紫色环且不出现系统虚线框。
- [ ] 在 1280×720、1100×700、960×640 及 125%、150%、200% DPI 检查 1280 / 980 / 880 DIP 布局切换；图标侧栏的 Logo、导航和状态均有 Tooltip。
- [ ] 首页不再重复六张统计卡片中的数字，只显示待处理结论和最近八条任务；点击任务中心后可按状态、游戏和中文任务类型筛选。
- [ ] 媒体中心三个局部页签相互独立，表格不显示 `GameSaveCenter.Contracts.*`，文件名与路径省略但 Tooltip 可读。
- [ ] 已安装修改器页同时显示工具列表和设置 Inspector；FLiNG 列表主标题为语义版本而非完整下载文件名。
- [ ] 维护中心默认只显示摘要，展开“查看完整诊断信息”后才显示等宽技术文本。
- [ ] 检查纵横滚动条 Thumb 为圆角且长度合理；后台自动刷新至少两轮，标题、工具栏和内容不得震动或跳位。

## 0.5.5 后台反馈与固定周期回归

- [ ] 安装后确认扩展管理器和 `extension.yaml` 为 `0.5.5`，主 DLL 为 `0.5.5.0`。
- [ ] 启用“任务完成或失败时显示 Playnite 通知”，关闭 GameSaveCenter 管理面板后启动已匹配游戏；5 分钟策略至少保持 16 分钟，三次到期应分别在约 5、10、15 分钟（允许轮询带来最多约 5 秒延迟）产生任务结果。
- [ ] 测试期间不改存档也必须看到“游玩中定时备份：存档无变化，历史未新增”；该结果是成功执行，不应新增重复 ZIP 历史。
- [ ] 修改存档后下一轮应新增历史并通知成功；每个任务只通知一次，重新打开 Dashboard 不得重复通知。
- [ ] 游戏运行中把间隔从 5 改为 2 分钟并保存，下一轮从保存后的当前时间重新倒计时；关闭再启用游玩中备份也不得立即补跑旧到期时间。
- [ ] 使用较慢的备份目标验证上一轮尚未结束时不会堆积同游戏定时备份；Worker 日志应记录 skipped overlapping 和下一次计划。
- [ ] 分别执行手动备份、恢复、修改器下载、媒体同步，核对成功、Same 或失败均有准确反馈；即时的修改器导入/解绑、策略保存和媒体归类也有成功提示。
- [ ] 关闭通知设置后执行任务不得提示；重新启用时不得集中补发关闭期间的旧任务。重启 Playnite 也不得补发数据库中的历史任务。

## 0.5.4 崩溃与游玩中备份回归

- [ ] 安装后确认扩展管理器和 `extension.yaml` 为 `0.5.4`，主 DLL 为 `0.5.4.0`。
- [ ] 下载或导入修改器后点击“已安装”、切换 FLiNG 搜索结果和可下载版本；`extensions.log` 不得出现 `GameToolDto.TypeDisplay` 或只读属性的 TwoWay `XamlParseException`。
- [ ] 为一个可测试且已匹配 Ludusavi 的游戏启用“游玩中备份”，间隔设为 1 分钟并保存；保持游戏会话至少 70 秒。Worker 日志应先记录 1 分钟计划，再记录 timed backup；无变化时任务应成功显示“游玩中定时备份：存档无变化，历史未新增”。
- [ ] 关闭游戏后确认退出后备份仍独立执行；不要将 Playnite 对极短进程的 0 秒停止事件误判为一分钟定时备份失败。

## 0.5.3 紧凑模式、滚动条与媒体/FLiNG 回归

- [x] Release 构建、12 项 Core 测试及源码门禁通过；开发安装目录已核验 `extension.yaml 0.5.3`、DLL `0.5.3.0`。
- [ ] 在 1050 DIP 以下检查图标侧栏：六个导航图标完整可点击，Worker 仅保留状态灯，Ludusavi/Worker 文本不应裁切。
- [ ] 在有和没有水平溢出的 DataGrid、TextBox、ComboBox Popup 中检查 ScrollBar：无溢出时不显示；出现时 Thumb 长度与内容比例相符，不得显示为无意义的小方块。
- [ ] 媒体中心待归类和当前游戏媒体表各至少显示约四行；窗口较矮时使用该页面的垂直滚动条，表格底边不得裁切。
- [ ] 搜索 FLiNG 后确认右侧自动出现首个结果的版本；点击其他搜索结果后右侧版本随之刷新。
- [ ] 等待至少一个后台自动刷新周期：页面、详情卡片和顶部工具栏不得移动、闪动或重新换行。

## 0.5.2 模块化 UI 与控件主题回归

- [x] Release 构建、12 项 Core 测试、打包及开发安装通过；后续版本继续在安装目录核验清单与 DLL 一致性。
- [ ] 在首页、存档、修改器、媒体、任务、维护中心切换时，只显示当前模块相关标签与操作。
- [ ] 任务与维护中心不显示游戏浏览器列，且主内容不会横向裁剪。
- [ ] 分别在 1600×900、1366×768、1280×720、1100×700、960×640 检查 Wide/Medium/Compact 切换。
- [ ] 100%、125%、150%、200% DPI 下验证导航图标模式、顶部游戏选择器、内部滚动与无重叠按钮。
- [ ] 在深色、浅色、跟随 Playnite 和关闭透明效果时打开设置页；所有 ComboBox 主体、Popup、滚动条及进度条均不得出现白色系统样式。
- [ ] 等待至少一个后台自动刷新周期，确认不再插入底部进度条或使页面上下震动。

## B. 基础 IPC 与配置

- [ ] Worker 只启动一个实例。
- [ ] 命名管道 Ping 成功。
- [ ] 设置保存后重启 Playnite仍然存在。
- [ ] Worker 数据库位于 `%LOCALAPPDATA%\GameSaveCenter`。
- [ ] 日志中没有 Token、密码或完整 Rclone 配置内容。

## C. 游戏库与启动方式

至少测试：

- [ ] Steam 普通启动；
- [ ] Xbox/Game Pass 普通启动；
- [ ] Epic/Ubisoft/EA/GOG 中实际使用的平台；
- [ ] Playnite Game Action 启动；
- [ ] 直接从平台客户端启动；
- [ ] 一个 MOD loader（例如 SKSE/SMAPI/MO2/Mod Engine）；
- [ ] loader 退出但游戏继续运行时，会话不应提前结束；
- [ ] 同一游戏不会同时出现两个逻辑会话。

## D. Ludusavi 备份

选择一个可丢弃测试存档：

- [ ] 游戏匹配名称正确；
- [ ] 手动单游戏备份成功；
- [ ] 全部备份不会处理未匹配游戏；
- [ ] 游戏退出后生成最终备份任务；
- [ ] 游玩超过配置间隔后生成定时备份；
- [ ] 没有变化时 Ludusavi 行为符合预期；
- [ ] 备份历史、文件数、大小和备注可见；
- [ ] 锁定状态在 Ludusavi 中真实生效；
- [ ] 体积/文件数异常会产生 finding。

## E. 媒体增量同步

- [ ] Steam F12 截图归类到正确 AppID 游戏；
- [ ] 重复运行同步不会再次复制相同文件；
- [ ] 同图改名仍由 SHA-256 去重；
- [ ] Xbox Game Bar 截图正确归类或进入待人工处理；
- [ ] Epic/Ubisoft/EA/GOG 的自定义目录生效；
- [ ] `*.png` 等匹配模式生效；
- [ ] 共享目录不会把明显属于其他游戏的文件归入当前游戏；
- [ ] 错误归类可在 UI 重新分配；
- [ ] 删除源截图不会删除归档副本；
- [ ] 正在写入的视频不会复制半成品。

## F. Rclone 单向云端复制

使用测试 Remote 和空目录：

- [ ] 初始全局云端开关关闭时不会上传；
- [ ] 开启后只调用 `copy`，不调用 `sync/delete/purge`；
- [ ] 本地断网时本地备份仍成功，云任务失败可见；
- [ ] 恢复网络后重新复制成功；
- [ ] `rclone check --one-way` 可验证测试目录；
- [ ] 删除本地测试副本不会自动删除云端文件；
- [ ] 多电脑使用不同 `<MachineName>` 子目录。

## G. 安全恢复

只测试可完全丢弃的存档：

- [ ] 游戏、平台客户端写盘进程和 MOD manager 已关闭；
- [ ] 恢复前创建新的 PreRestore；
- [ ] PreRestore 有备注并锁定；
- [ ] 预览失败时不写入真实存档；
- [ ] 指定历史版本恢复成功；
- [ ] 恢复后游戏能够读取；
- [ ] 撤销恢复回到 PreRestore；
- [ ] 人为制造失败时回滚路径可用；
- [ ] 恢复过程中没有云端任务覆盖本地。

## H. 自动识别与 WGS

- [ ] 候选扫描不会无界扫描整盘；
- [ ] 缓存、日志、shader、截图目录被降权；
- [ ] `.sav/.dat/.profile` 等变化被加权；
- [ ] 接受候选只生成规则草案；
- [ ] 没有静默改动 Ludusavi 自定义配置；
- [ ] Xbox WGS 只在确认映射后备份；
- [ ] 首轮不执行 Xbox WGS 覆盖恢复。

## I. 发布门禁

- [ ] 所有严重错误已关闭或写入已知限制；
- [ ] ZIP 不包含存档、截图、数据库、日志、Token 或密码；
- [ ] ZIP 包含 `.git/HEAD` 和完整提交历史；
- [ ] `DEVELOPMENT_PROGRESS.md` 与实际结果一致；
- [ ] 发布版本号、extension.yaml、程序集版本和文件名一致。

## 0.2.0 缺陷回归门禁

### A. 设置持久化与刷新

1. 在设置页填写 Ludusavi 路径并保存。
2. 完全退出 Playnite，终止 Worker，再重新打开 Playnite。
3. 确认底栏显示实际 Ludusavi 版本，测试游戏不需要 PowerShell 注入即可“已就绪”。
4. 点击刷新，确认游戏数、匹配数和当前选择不丢失。
5. 检查 `%LOCALAPPDATA%\GameSaveCenter\worker-settings.json`，确认非敏感路径和策略已保存。

### B. ZIP 多版本

1. 使用 `GameSaveCenter Test`，源文件依次写入 `version=1`、`version=2`、`version=3`。
2. 每次变化后点击立即备份。
3. 历史应出现至少三个不同 BackupId；刷新和重启后仍存在。
4. 任务详情分别显示首个版本/新的历史版本，不得显示 `LUDUSAVI_NOT_CONFIGURED`。
5. 再次备份未变化文件，任务应成功并提示“存档无变化，历史未新增”。

### C. 时间与错误详情

1. 任务、历史、媒体、审计时间应与 Windows 当前本地时间一致。
2. 临时把 Ludusavi 路径改成不存在的文件并保存，执行备份。
3. 任务详情应显示 `LUDUSAVI_NOT_CONFIGURED` 和明确说明；恢复正确路径后刷新应自动恢复匹配。

### D. 历史刷新与恢复

1. 选中一个历史版本后点击刷新，当前游戏历史不得清空。
2. 只对可丢弃测试存档执行安全恢复。
3. 确认恢复前生成锁定的 PreRestore，恢复后文件内容正确，撤销恢复可回到原状态。

### E. UI 视觉回归

分别测试 Playnite 浅色、深色和至少一个第三方主题：

- 标题、标签、禁用按钮、表格文字均清晰；
- 卡片、按钮、输入框和选中状态保持圆角与弱边框；
- 不出现硬编码黑字压在深色背景上的情况；
- 100%、125%、150% 缩放下不截断主要操作；
- 插件内不绘制 macOS 红黄绿窗口按钮。



## 0.3.1 主题、毛玻璃与动画回归

1. 在 Playnite 浅色主题打开管理面板，确认：
   - 文字清晰，卡片为浅色半透明表面；
   - 紫蓝环境光不遮挡内容；
   - 左侧导航与详情标签同步。
2. 切换到深色主题并重新打开页面，确认：
   - 无黑字压在深色背景；
   - 玻璃表面不是纯白块；
   - 状态色在深色背景仍可辨认。
3. 在 Windows 高对比度模式下打开页面，确认环境光消失、表面变为不透明主题背景。
4. 在设置页关闭“启用界面动画”，重新打开管理面板，确认页面、游戏、标签、卡片和按钮不再播放过渡动画。
5. 关闭“启用毛玻璃与环境光效果”，确认卡片改为不透明主题背景且功能不受影响。
6. 分别将毛玻璃强度设置为 20、60、100，确认设置页实时变化，保存并重启 Playnite 后仍保留。
7. 检查页面进入、游戏切换、标签切换、任务选择、状态胶囊、指标卡、导航和按钮动画，不应出现卡顿、闪烁或布局跳动。
8. 在 100%、125%、150% DPI 下检查侧栏、六个指标卡、按钮和详情标签是否截断或重叠。

## 0.3.0 任务监控与诊断回归

### A. 自动刷新与通知

1. 设置管理面板刷新间隔为 5 秒并保存。
2. 打开 GameSaveCenter，启动一个持续至少 15 秒的测试任务。
3. 不点击刷新，任务进度应自行变化；Playnite 主界面不得卡死。
4. 自动任务成功、失败或取消后应出现一条 Playnite 通知，不得重复刷出相同任务通知。
5. 关闭自动刷新后，任务页不再轮询，但手动刷新仍可用。

### B. 任务取消

1. 创建两个同游戏任务，使第二个保持 Queued。
2. 选中 Queued 任务点击取消，最终状态应为 Cancelled，重启后仍保留。
3. 选中 Running 任务点击取消，任务应在安全边界结束；不得留下永久 Running 状态。
4. 已完成任务的取消按钮必须禁用。
5. 在 Running 任务期间强制结束 Worker，重启后旧任务应显示 Failed / `WORKER_RESTARTED`，不得永久 Running。

### C. 诊断中心

1. 诊断页显示 Worker、Ludusavi 版本以及当前 ZIP/压缩/完整/差异策略。
2. 点击复制诊断，剪贴板内容不包含 Rclone 远端路径、密码或 Token。
3. 数据目录、存档目录、媒体目录和 Worker 日志按钮能够打开正确位置。
4. Ludusavi 路径失效时，诊断摘要和最近失败任务应明确显示原因。


## 0.3.4 一键入口编码与安装版本回归

1. 不打开 PowerShell，直接双击仓库根目录 `GameSaveCenter-Run.cmd`。
2. 确认控制台不出现乱码命令、`rofile` 或 `se` 等错误。
3. 等待脚本自动关闭旧 Playnite/Worker、完成构建测试、安装并重新启动 Playnite。
4. 检查 `artifacts/last-dev-install.txt`：源码、清单为 `0.3.4`，DLL 为 `0.3.4.0`。
5. 检查 Playnite 扩展管理显示 `0.3.4`，再进行导航、指标卡和按钮悬停回归。
6. 失败时上传 `artifacts/one-click-install.log`，不需要复制控制台全部内容。

## 0.3.5 历史、主题和大型库回归

1. 打开管理面板，底部不得再出现 `DurationDisplay` TwoWay 绑定错误；保持页面 30 秒，任务和时间应自动更新。
2. 对已匹配测试游戏执行备份；任务成功后无需重启，历史页应出现与磁盘 `backup-*.zip` 对应的记录。
3. 切换到另一游戏再切回、手动刷新、关闭并重新打开页面，历史仍应存在。
4. 在约 965 款游戏库中搜索游戏名、平台和 Ludusavi 名称；依次测试已就绪、未匹配、运行中、需关注、有历史筛选以及四种排序。
5. 任务表格中进度条保持固定宽度，百分比单独显示，不与详情重叠。
6. 无任务运行时底部不得出现空进度轨道；手动操作或后台刷新时只显示独立进度胶囊。
7. 分别使用默认深色、默认浅色和至少两个第三方 Playnite 主题：输入框不得变成纯黑遮挡块，标题、正文、禁用态和表格均可读。
8. 在 100%、125%、150% DPI 下观察标题、列表和按钮文字；静止后应清晰，不因悬停缩放持续发虚。

## 0.4.0 主题模式、会话候选与任务重试回归

### A. 版本和设计资源

1. 双击 `GameSaveCenter-Run.cmd`，安装报告必须显示清单 `0.4.0`、DLL `0.4.0.0`。
2. 安装目录必须包含 `Themes/DesignTokens.xaml`，管理面板与设置页均能打开。
3. `python scripts/validate-source.py` 应通过，且 UI 改动已阅读 `docs/design/APPLE_WPF_IMPLEMENTATION_PROMPT.md` 与 `UI_CHANGE_GATE.md`。

### B. 三种主题模式

1. 选择“跟随 Playnite”，分别在默认深色、默认浅色和第三方主题打开管理面板。
2. 选择“浅色”并保存，不重启 Playnite，重新打开或切回面板后局部 UI 应保持稳定浅色且文字清晰。
3. 选择“深色”并保存，验证同样的即时重算与重启持久化。
4. 三种模式下检查输入框、ComboBox 下拉项、禁用按钮、任务表格、候选表格和侧栏状态。
5. 高对比度或关闭毛玻璃时，固定主题不得导致透明文字、黑块或丢失键盘焦点。

### C. 会话前后存档候选

1. 准备一个尚未匹配 Ludusavi 的可丢弃测试游戏，并开启“会话存档路径检测”。
2. 启动游戏后等待 1–2 分钟，创建或修改测试存档，然后正常退出。
3. 打开该游戏的“候选存档路径”，应出现包含“会话前后快照发现新增/修改”理由的候选。
4. 候选必须显示可信度、状态、路径和完整判断依据；长路径使用 Tooltip。
5. 点击“忽略候选”，切换游戏、刷新、重启 Worker 后状态仍为“已忽略”。
6. 再选择另一候选点击“接受并生成规则草案”，状态应为“已接受”，后续扫描不得重新插入同一路径为 Pending。
7. 已匹配 Ludusavi 的游戏启动时不得执行大范围候选快照。
8. 强制结束 Worker 后重新启动，超过两天的测试快照应被清理；当前有效候选不得丢失。

### D. 任务复制与安全重试

1. 制造一个可恢复的 Backup 失败任务，选中后“复制详情”应包含游戏、任务类型、真实错误和任务 ID。
2. 修正失败原因后点击“安全重试”，应新建任务并完成备份，不修改原任务历史。
3. 对 MediaSync 的 Failed/Cancelled 任务重复测试。
4. 对 Restore、UndoRestore 或未知任务类型，“安全重试”必须禁用。
5. Running/Queued 任务仍使用取消按钮，Succeeded 任务的取消和重试均禁用。

### E. Game Bar / Windows 公共媒体会话归类

1. 使用文件名不包含游戏名的测试截图，在单一游戏会话期间生成，退出后触发媒体同步。
2. 截图时间落在会话开始前 2 分钟至结束后 10 分钟时，应归类到本次游戏。
3. 同时保持另一游戏会话运行，重复生成截图；时间推断应自动关闭，除非文件名明确匹配，否则不得误归类。
4. 手动“同步媒体”没有 SessionId 时仍只使用文件名/明确来源，不得仅凭最近时间归类。
5. 验证已归档媒体 SHA-256 去重仍有效，源文件删除不会删除归档。


## 0.4.1 全局媒体收件箱回归

### A. 构建、安装与旧库升级

1. 双击 `GameSaveCenter-Run.cmd`，安装报告必须显示清单 `0.4.2`、DLL `0.4.2.0`。
2. 使用保留 0.4.0 `state.db` 的环境启动 Worker；不得出现 `classification_state`、`classification_reason` 或 `ix_media_classification` 迁移错误。
3. 重启 Worker 两次，迁移必须幂等，既有已归类媒体仍显示在原游戏下。
4. `python scripts/validate-source.py`、真实 `dotnet build/test`、PEXT 打包与 Playnite 加载全部通过。

### B. 歧义媒体进入收件箱

1. 准备一个文件名不包含任何游戏名的测试截图，放入 Game Bar Captures、Windows Screenshots 或标记为“共享目录”的自定义来源。
2. 在没有 SessionId 的手动媒体同步中，文件必须复制到媒体库 `_Inbox/Pending` 并显示在“待归类媒体收件箱”。
3. 列表应显示拍摄时间、类型、来源、文件名、大小和无法归类原因；源文件仍在原目录。
4. 文件名同时匹配两款游戏时，原因应明确提示多游戏歧义，不得自动选择其中之一。
5. 首次面对超过 200 个歧义历史文件时，一轮最多新增 200 个；再次同步应跳过已导入哈希并继续补齐更旧项目。

### C. 保守自动归类

1. 文件名只明确匹配一款游戏时，应直接进入该游戏媒体目录，不进入收件箱。
2. 文件名不匹配，但带明确 SessionId、时间位于开始前 2 分钟至结束后 10 分钟且无其他游戏会话重叠时，应按会话归类。
3. 存在重叠游戏会话时，时间推断必须关闭；无法唯一文件名匹配的项目进入收件箱。
4. 同一路径被系统来源和共享自定义来源同时发现时，只归档一次。

### D. 人工归类与忽略

1. 在收件箱选择媒体与目标游戏，点击“确认归类”；归档文件应移动到目标游戏的 Screenshots/Clips 年月目录，SQLite 状态变为 `Assigned`，收件箱立即移除。
2. 目标路径已存在相同 SHA-256 时可安全复用；存在不同内容时必须停止并报错，禁止覆盖。
3. 将 Pending 归档副本临时移走但保留原始截图，再执行归类；程序应重新复制归档，原始截图不能被移动或删除。
4. 点击“忽略并保留副本”，文件应移动到 `_Inbox/Ignored`，状态为 `Ignored`，不再出现在收件箱；原始文件和忽略归档均保留。
5. 关闭管理面板、刷新、重启 Playnite/Worker 后，Assigned/Inbox/Ignored 状态与路径保持一致，审计日志可追踪人工操作。

### E. 任务、主题与性能

1. 制造 `MediaInbox` Failed/Cancelled 任务，“安全重试”应只扫描共享来源，不为每个游戏创建专属媒体任务。
2. 在约 965 款游戏库中打开截图与录像页，目标游戏下拉可用，切换筛选不影响全局收件箱选择和计数。
3. 默认深色、默认浅色、至少两个第三方主题以及 100%、125%、150%、200% DPI 下，表格、长文件名、空状态和按钮无裁剪、黑块或低对比。
4. 使用大目录观察 CPU、磁盘和 UI 响应；扫描运行在 Worker，Playnite UI 不得卡死，取消与真实任务状态应保持可用。
## 0.4.2 Playnite 侧栏崩溃热修复回归

1. 关闭 Playnite，使用全新 0.4.2 源码执行 `GameSaveCenter-Run.cmd`，构建、测试、打包和安装必须全部成功。
2. 在附加组件页确认 GameSaveCenter 显示 `0.4.2`，不要继续测试仍显示 `0.4.1` 的安装。

3. 点击 GameSaveCenter 侧栏，必须成功创建 Dashboard；不得出现扩展崩溃窗口、`XamlParseException` 或找不到 `GscStatusPill`。
4. 连续关闭并重新打开侧栏至少 5 次，依次切换概览、游戏、备份、任务、截图与录像、诊断和异常日志标签。
5. 打开截图与录像页，待归类数量胶囊应具有玻璃背景和细边框；不存在透明空块、缺失边框或资源警告。
6. 分别切换跟随 Playnite、固定浅色、固定深色；关闭并重开侧栏后资源仍能解析。
7. 检查 `playnite.log` 与 `extensions.log`，本轮不得新增 GameSaveCenter XAML 资源解析异常。

## 0.4.3 Worker 路径与响应式界面回归

- [x] 使用错误的 `WorkerExecutable=ludusavi.exe` 旧配置启动，确认自动迁移为打包 Worker，并保留有效 Ludusavi 路径。
- [x] 确认只存在一个 Worker，Ludusavi CLI 隐藏运行，不再连续打开 GUI 窗口。
- [x] 确认 `worker-launch.log` 在启动前创建并包含启动目标、初始化和退出诊断。
- [x] Windows Release build/test/publish/package/install 通过，清单 `0.4.3`、DLL `0.4.3.0`。
- [x] 1366×768 深色主题下搜索框完整、输入过滤生效，两个筛选框及 Popup 不出现系统白色表面。
- [x] 1366×768 下统计卡片自动收起，右侧存档历史表格仍可见且底部状态栏不重叠。
- [ ] 在 1600×900、1280×720、1100×700 和允许的最小尺寸逐项拖动缩放。
- [ ] 在 100%、125%、150%、200% DPI 与浅色/跟随 Playnite 主题复测 ComboBox、Tab 和 DataGrid。
- [ ] 使用至少三个真实 ZIP 历史验证表格滚动、选择、恢复按钮区和备注区。

## 0.5.0 修改器中心回归

1. 一键构建安装后确认附加组件版本 `0.5.1`、DLL `0.5.1.0`，侧栏可正常打开。
2. 旧 `gamesavecenter.db` 启动两次，确认四张新表迁移幂等且既有存档、媒体、任务不丢失。
3. 对同一游戏分别导入两个 EXE、一个 ZIP 和两个 CT；刷新、切换游戏、重启 Worker 后绑定仍存在。
4. 准备包含 `..\escape.exe` 的测试 ZIP，确认整体拒绝且目标目录外没有新增文件。
5. 每项分别测试手动启动、延迟 0/8/30 秒、管理员权限、自动启动关闭和文件缺失提示。
6. 只勾选其中一个修改器和一个 CT 随游戏启动，确认其他工具不启动；启动失败不得阻断游戏。
7. 勾选退出关闭后，只关闭本次会话启动的 PID；预先手动运行的同名程序不得被结束。
8. 带 Easy Anti-Cheat 或 BattlEye 启动路径的测试游戏不得自动启动工具，审计中显示阻止原因。
9. 刷新 FLiNG 目录，搜索 `Elden Ring`，展开版本并下载；确认真实任务进度、取消、失败重试和下载后自动绑定。
10. 断网时保留上次目录搜索；官网结构变化时显示解析失败，不影响本地工具启动。
11. 深色、浅色、第三方主题及 100%–200% DPI 下检查修改器卡、搜索结果、版本列表和按钮无裁剪或水平页面滚动。
## 0.6.19 媒体控制与来源规则回归

- [ ] 安装后确认扩展管理器为 `0.6.19`，主 DLL 文件版本为 `0.6.19.0`。
- [ ] 在插件设置中依次关闭 Steam、Xbox Game Bar、Windows Screenshots、游戏相邻目录和自定义来源；每次手动同步后只应跳过对应来源，其余启用来源仍可归档。
- [ ] 关闭全局“同步新增截图和录像”后，工具栏手动同步应给出明确提示且不创建媒体任务；重新打开后应恢复工作。
- [ ] 对一个游戏关闭“启用当前游戏自动任务”：启动、退出和游玩间隔均不得自动创建备份或媒体任务；手动备份和手动媒体同步仍然可用。
- [ ] 只打开“游玩中归档媒体”、关闭“游玩中定时备份”，保持游戏运行超过策略间隔，确认会创建 MediaSync 任务但不会创建 Backup 任务。
- [ ] 在“来源与规则”暂停、恢复并移除一个自定义来源；确认移除后原始目录、已归档媒体和收件箱项目均仍存在。
- [ ] 用 125%、150% 与深/浅色主题检查新复选框、来源操作按钮和提示文本，不能出现裁剪、默认白色控件或绑定错误。

## 0.6.22 全局 GamePicker 与列表滚动回归

- [ ] 在 100、500、1000 款游戏库分别打开六个工作区；顶部只出现一个全局 GamePicker，任务中心和维护中心不显示游戏选择器。
- [ ] 在 Expanded、Standard、Compact、Narrow 四档切换游戏；当前游戏、筛选、排序和选中状态保持一致，删除当前游戏后自动回退到有效游戏。
- [ ] 连续快速输入搜索词和快速切换游戏；列表使用本地缓存，UI 不得为每次按键创建 Worker 请求，旧筛选结果不得覆盖新结果。
- [ ] 在修改器、FLiNG 搜索结果和版本列表中填充超过可视区域的数据，确认内部纵向滚动、Recycling 虚拟化和键盘方向键/Enter/Esc 正常；不得出现页面级横向滚动。
- [ ] 首页“需关注”大于 0 时，点击指标/按钮可进入维护中心并定位到具体游戏、原因和建议；键盘 Tab/Enter 可访问，屏幕阅读器名称清晰。
- [ ] 在 1600、1280、1024、850、700 DIP 与 125%、150% DPI 下检查 DataGrid、ComboBox、TextBox、Popup、Tab、长文本 Tooltip 和空状态。
- [ ] 分别验证浅色、深色、跟随 Playnite、高对比度、关闭透明和关闭动画；共享列表和表格不得使用 BlurEffect，主题切换不出现白色系统控件。
- [ ] 运行 `GameSaveCenter-Run.cmd` 完成真实 Release 构建、安装、Playnite 加载、重复打开关闭和 Worker 断开恢复；未完成上述步骤前不得标记 UI 重构为真机完成。

### 0.6.22 UI-058 共享表格与低高度回归补充

- [ ] 在五个提取工作区分别填充超过视口的任务/历史/媒体/诊断数据；确认表头、圆角行、悬停、选中、焦点和滚动条在浅色、深色、跟随 Playnite、高对比度下可读。
- [ ] 在 700 DIP 高度和 125%、150% DPI 下打开修改器中心；确认工具列表保持可见，工具设置通过自身纵向滚动访问，不能覆盖 Tab、GamePicker 或页面操作。
- [ ] 在 1600、1280、1024、850、700 DIP 下拖动窗口；确认 220 DIP 表格视口不会造成页面级裁剪，低高度时摘要/次要区按现有响应式规则收起。
- [ ] 验证 DataGrid 行/列虚拟化和 Recycling 未被共享模板破坏；滚动到长列表末尾后仍可选择、排序、复制和执行原有命令。

### 0.6.22 UI-059 检查器滚动与系统偏好动态回归补充

- [ ] 在媒体中心给选中媒体填充长文件名、来源、时间、归档路径和操作按钮；在 700/640 DIP 高度下确认检查器自身滚动，媒体 DataGrid 仍可见、可滚动且不出现页面级横向滚动。
- [ ] 在维护中心加载长诊断摘要和多条 Findings；确认诊断文本在 90–220 DIP 范围内滚动，Findings 表格和刷新/导出操作不会被推走。
- [ ] Playnite 保持打开时切换 Windows 高对比度、关闭透明效果和关闭动画，再返回插件；Dashboard 与设置页应即时重建主题和动效降级，不需要重启插件。
- [ ] 关闭 Dashboard/设置页后重复打开并切换上述系统设置；检查 Playnite 日志和内存占用，确认没有重复事件回调或卸载视图继续更新。

### 0.6.22 UI-060 概览与任务详情低高度回归补充

- [ ] 在任务中心选中带超长错误详情的失败任务，并在 700/640 DIP 高度下换行操作按钮；确认任务详情自身滚动，任务表仍可滚动到末尾。
- [ ] 在概览制造超过可视区域的关注项，使用 1024/850/700 DIP 宽度检查堆叠布局；确认风险提醒内部滚动，最近活动表格和刷新操作仍可见。
- [ ] 在 125%、150% DPI 及浅色、深色、跟随 Playnite、高对比度下重复上述测试，检查圆角表面、Tooltip、键盘焦点和滚动条对比度。

### 0.6.22 UI-061 表格排序视觉回归补充

- [ ] 在概览、存档、媒体、任务和维护中心分别点击可排序表头；确认升序/降序箭头显示方向正确、颜色与当前主题对比清晰。
- [ ] 在 125%、150% DPI、高对比度和第三方 Playnite 主题下重复排序；确认箭头不裁切、不覆盖表头文字，列宽调整和键盘焦点仍可用。

### 0.6.22 UI-062 GamePicker 键盘回归补充

- [ ] 打开顶部 GamePicker，焦点位于搜索框时连续输入、按 Enter 和 Esc；确认 Enter 保持当前选择并关闭抽屉，Esc 关闭抽屉且不触发业务命令。
- [ ] 使用 Tab 从搜索框进入筛选器和列表，使用方向键选择游戏；在浅色、深色、高对比度和动画关闭模式下焦点提示仍清晰。

### 0.6.22 UI-063 存档中心低高度回归补充

- [ ] 在历史版本中填写长备注并缩小到 1024/850/700 DIP；确认备注、锁定、恢复、比较和撤销操作在自己的滚动区可达，历史表格仍位于上方并能滚动到末尾。
- [ ] 在候选路径中准备多行判断依据和较长路径；确认依据文本与候选操作按钮分别滚动，不产生页面级横向滚动，也不遮挡候选表格。
- [ ] 在 125%、150% DPI 和浅色、深色、跟随 Playnite、高对比度下重复上述测试；检查滚动条、按钮焦点、Tooltip 和动态高度没有裁剪。

### 0.6.22 UI-064 表格文本对齐回归补充

- [ ] 在存档、媒体、任务和维护工作区检查游戏名、路径、详情、原因和诊断消息左对齐且带省略/Tooltip；状态点、进度和操作按钮仍保持居中或 Stretch。
- [ ] 在 850/700 DIP、125%/150% DPI、浅色/深色/跟随 Playnite/高对比度下检查左对齐文本不覆盖排序箭头、不造成水平页面溢出。

### 0.6.22 UI-065 维护中心设备页回归补充

- [ ] 在设备状态页填充长远端状态和决策说明，并在 1024/850/700 DIP 下缩放；确认设备对比表仍可见、可滚动，决策与远端恢复操作可通过各自滚动区访问。
- [ ] 在 125%、150% DPI、浅色/深色/跟随 Playnite/高对比度下检查 ComboBox、TextBox、恢复按钮的圆角、焦点、Tooltip 和滚动条对比度。

### 0.6.22 UI-066 顶部操作栏回归补充

- [ ] 在 Expanded、Standard、Compact、Narrow 和 700/850 DIP 下确认顶部操作栏按钮不被裁剪；空间不足时只出现局部水平滚动，不产生页面级水平滚动。
- [ ] 在 125%、150% DPI、第三方主题和高对比度下检查操作栏滚动条、Tab 顺序、Tooltip、图标和主按钮焦点。

### 0.6.22 UI-067 修改器目录联动回归补充

- [ ] 进入修改器中心的 FLiNG 在线库，搜索并选择一个结果；确认右侧版本列表立即加载，不需要切换旧页面或重新打开 Dashboard。
- [ ] 快速切换两个 FLiNG 搜索结果；确认旧版本列表不会覆盖最后选中的结果，下载按钮始终对应当前结果。
- [ ] 在 700/850 DIP、125%/150% DPI、浅色/深色、高对比度和动画关闭下重复选择；确认结果列表、版本列表、键盘焦点和内部滚动均可用。

### 0.6.22 UI-068 首页摘要回归补充

- [ ] 在 Expanded、Standard、Compact、Narrow 及 700/850 DIP 下确认首页只出现一套摘要卡片；最近活动和需关注区域不会被旧统计条重复占用或推到视口外。
- [ ] 当需关注数量大于 0 时，确认 `OverviewView` 的关注原因列表、查看需关注项按钮和维护中心定位仍然可用，且没有因为移除旧统计卡而丢失入口。

### 0.6.22 UI-069 工作区提取与媒体筛选回归补充

- [ ] 重复打开和关闭 Dashboard，确认只构造六个生产工作区，不出现旧 Overview/存档/修改器/媒体/任务/诊断/日志 Tab 的重复内容或旧事件异常。
- [ ] 在 1600、1280、1024、850、700 DIP 及 125%/150% DPI 下分别进入六个工作区；确认表格、列表、检查器和顶部 GamePicker 不互相覆盖，局部滚动仍可访问。
- [ ] 在媒体中心“当前游戏媒体”输入文件名/备注/来源关键词并切换“全部/截图/录像/收藏”；确认结果使用本地筛选即时更新，不产生每次输入一次 Worker 请求，清空搜索后恢复完整列表。
- [ ] 在维护中心诊断、设备状态和进程映射页检查长游戏名、远端设备、决策和 EXE 文本；确认省略、Tooltip、横向内部滚动和键盘焦点正常。
- [ ] 在浅色、深色、跟随 Playnite、高对比度、关闭透明和关闭动画模式下重复以上步骤；确认没有遗留白色 ComboBox/Popup、黑块、BlurEffect 列表行或不可读状态。

### 0.6.22 UI-070 共享表格密度回归补充

- [ ] 在概览、存档、媒体、任务和维护中心分别准备至少 5 条记录；确认默认表格可同时显示多行，行高约 54 DIP，表头约 44 DIP，更多记录可通过表格内部滚动访问。
- [ ] 在 1600、1280、1024、850 和 700 DIP 宽度下确认表格仍位于有限 Grid 区域，不被底部详情、检查器或操作区推走；确认不存在页面级水平滚动条。
- [ ] 在 125% 和 150% DPI、浅色、深色、跟随 Playnite、高对比度、关闭透明和关闭动画下检查行背景、表头、排序箭头、滚动条、焦点环和长文本 Tooltip。
- [ ] 检查至少 5 行数据的交替行、悬停行和选中行层次；浅色/深色应使用低对比动态色，高对比度应回退为系统可读背景，不覆盖选中高亮。
- [ ] 使用 500/1000 款游戏或大量任务/媒体数据滚动到列表末尾；确认 Recycling/CanContentScroll 仍然生效，未因外层滚动或圆角样式导致明显卡顿。

### 0.6.22 UI-071 紧凑窗口留白与共享行高

- [ ] 在 1600、1280、1024、850、700 DIP 宽度/高度组合下确认外壳边距会收缩但不发生裁剪，工作区表格仍占据剩余星号行并在内部滚动。
- [ ] 确认 Expanded、Standard、Compact、Narrow 下详情卡片、Trainer/Media 标签间距合理；低高度时页面副标题和非必要留白隐藏，表格标题和至少五行数据仍可读。
- [ ] 在 125% 和 150% DPI、浅色/深色/跟随 Playnite、高对比度、关闭透明和关闭动画下确认共享 `DataGridRow` 行高、圆角、选择态和滚动条资源没有退回默认主题。

### 0.6.22 UI-073 低高度表格最小视口

- [ ] 在 700/650/600 DIP 高度下确认表格最小视口分别收缩到约 220/180 DIP 的安全范围，检查器和操作区仍可通过局部滚动访问。
- [ ] 在 760 DIP 以上确认表格恢复约 280 DIP 的宽松视口；大量记录仍由 DataGrid 内部滚动访问，页面不产生无限高度测量或水平溢出。

### 0.6.22 UI-074 工作区主题传播

- [ ] 在每个工作区切换跟随 Playnite、浅色、深色和高对比度，确认文字、状态色、表格交替行、Popup 和滚动条不退回静态默认颜色。
- [ ] 关闭透明效果后确认六个工作区均使用不透明降级表面；恢复透明后只在侧栏、浮层和 Toast 使用材质效果。

### 0.6.22 UI-075 列表项键盘焦点与圆角

- [ ] 在 FLiNG 搜索结果、版本列表、已安装修改器列表中使用 Tab/方向键选择，确认焦点环清晰、选中项有圆角背景且不出现宿主默认蓝色方块。
- [ ] 在浅色、深色、高对比度和关闭透明模式下确认列表项仍保持可读的悬停/选中/禁用状态。

### 0.6.22 UI-076 工作区入口键盘焦点

- [ ] 使用 Tab 在顶部当前游戏入口、设置分类和工作区页签之间移动，确认焦点环不被圆角模板裁剪且不会使用宿主蓝色默认样式。

### 0.6.22 UI-077 诊断展开器共享模板

- [ ] 在维护中心展开/收起“查看完整诊断信息”，确认标题、箭头、圆角边框、焦点环和深浅主题一致。
- [ ] 使用超长诊断文本确认正文同时支持纵向/横向滚动，展开器不会把下面的表格推出窗口或造成页面级裁剪。
- [ ] 在高对比度、关闭透明和关闭动画下确认展开器降级为不透明可读表面，且不会创建 BlurEffect。

### 0.6.22 UI-078 ListBox 滚动与虚拟化

- [ ] 在游戏库、修改器已安装、FLiNG 搜索结果和版本列表中确认纵向滚动条可见、横向页面不溢出。
- [ ] 使用 500/1000 款游戏或大量工具数据滚动到末尾，确认 `CanContentScroll` 与 Recycling 生效且滚动不明显卡顿。
- [ ] 在浅色、深色、跟随 Playnite、高对比度和关闭透明下确认列表项仍保持圆角、选中、悬停和键盘焦点状态。

### 0.6.22 UI-079 滚动条主题强调色

- [ ] 在至少两种 Playnite 强调色（例如蓝色和紫色）下拖动/悬停纵向与横向滚动条，确认 Thumb 悬停色与当前强调色一致。
- [ ] 在高对比度、关闭透明和深色模式下确认 Thumb 仍清晰可见，不出现固定紫色或低对比度灰块。
### UI-080 列表滚动与焦点回归

- 在 1600、1280、1024、850、700 DIP 以及 125%/150% DPI 下验证游戏选择器、修改器列表、FLiNG 结果和版本列表的滚轮/触控板滚动仅在列表内部发生。
- 使用 Tab、Shift+Tab、方向键和 Enter 在列表中移动，确认焦点不会跳到外层工作区，且 Recycling 虚拟化仍然生效。
### UI-081 悬停色主题回归

- 在浅色、深色、跟随 Playnite 蓝色/紫色强调色和高对比度下，将鼠标移入全局游戏选择器，确认悬停背景跟随当前调色板，不出现固定浅色或紫色残留。
- 高对比度下确认悬停不会覆盖系统选中/焦点显示。
### UI-082 首页低高度回归

- 在 1024、850、700 DIP 高度和 125%/150% DPI 下打开首页，确认统计/风险次级面板出现内部滚动，最近活动表格仍可见且拥有自己的滚动条。
- 制造至少 10 条关注原因，滚动风险面板到底部确认“打开维护中心”按钮可达；宽屏模式不应出现额外空滚动条。
- 在 1280×720、1100×700 等宽屏短窗（宽度 ≥1040、高度 <760）下打开首页，确认右栏「今日概览」卡拉伸填满、风险卡沉底，右栏底部不再出现约 150px 死空白；堆叠模式（宽度 <1040）外层滚动上限与风险内部滚动行为保持不变。

### UI-083 DataGrid 局部滚动回归

- 在游戏历史、媒体、任务、诊断和设备状态表格中使用滚轮/触控板，确认滚动只发生在表格内部，页面外层不横向漂移。
- 使用 Tab、Shift+Tab、方向键在表格行与单元格之间移动，确认焦点保持在当前表格并且大量数据仍使用 Recycling 虚拟化。

### UI-084 空状态与筛选后无结果回归

- 清空任务、存档历史、路径候选、当前游戏媒体、媒体来源、已安装工具、FLiNG 搜索结果、版本列表、诊断和设备比较数据，确认每个表面显示清楚的原因与下一步动作，而不是空白区域。
- 在任务/媒体/FLiNG 列表应用筛选使结果为空，确认空状态随 `ICollectionView.IsEmpty` 或集合变化即时切换，不产生新的 Worker 请求。
- 使用 Tab、方向键和鼠标确认空状态不会拦截 DataGrid/ListBox 的焦点；恢复数据后提示自动消失，局部滚动、虚拟化和表格高度不改变。
- 在 1600/1280/1024/850/700 DIP、125%/150% DPI、浅色/深色/跟随 Playnite/高对比度/关闭透明/关闭动画下重复检查文案对比度和圆角表面。

- 为待归类媒体分别准备 0 条和至少 1 条记录；确认空状态只在 0 条时出现，有媒体时不会覆盖表格行。

### 0.6.26 P0-001/P0-002 大型 Playnite 库与页面安全降级回归

- [ ] 确认 Playnite 附加组件页和 `playnite.log` 加载的是 `GameSaveCenter 0.6.26`，不是此前日志中的 `0.6.22`。
- [ ] 在 900+ 游戏库启动后，优先看到已持久化 Dashboard；`worker-launch.log` 记录 `Library descriptors persisted` 后，窗口不应等待全部 Ludusavi `find` 完成。
- [ ] 观察通知轮询：Worker 启动或后台匹配期间最多偶发一次调试日志，后续应按退避重试，不应每秒连续超时。
- [ ] 重复打开/关闭 GameSaveCenter；即使人为制造资源构造异常，也应显示插件诊断降级页，而不是 Playnite 扩展崩溃窗口。

- [ ] 使用 900–1000 款游戏的真实 Playnite 库启动并启用 GameSaveCenter；确认 Worker 先进入可用状态，Dashboard 能先显示 SQLite 缓存，不等待全部 Ludusavi 匹配结束。
- [ ] 检查 `worker-launch.log`：大批量同步应出现“descriptors persisted”和“matches queued in the background”，而不是在 IPC 请求内连续等待数分钟。
- [ ] 区分 GameSaveCenter 与独立 `LudusaviPlaynite` 扩展的负载；如果两者同时启用，确认 Playnite 仍可操作，并记录两者各自的请求数量。
- [ ] 在 Worker 启动、忙碌、重启和断开期间观察 `extensions.log`；通知轮询应按 5–60 秒退避，不应每秒重复输出命名管道连接超时。
- [ ] 确认大型库启动后前 25 秒只完成 Worker/设置初始化，随后才出现后台 Ludusavi 匹配；首次打开 Dashboard 应先显示 SQLite 缓存。
- [ ] 重复打开/关闭 GameSaveCenter、切换六个工作区、启动/停止游戏；确认缓存可读、后台匹配完成后摘要自动更新，且没有 XAML、ContentDialogHost、Dispatcher 或未处理异常。
- [ ] 与 100 款、500 款库分别比较首次打开时间、Playnite UI 响应和 Worker 日志；记录从插件侧栏点击到首页首屏可用的耗时。

### UI-124/UI-126/UI-127/UI-128 检查器宽屏填充与窄屏堆叠滚动回归

- [ ] 在任务中心、维护中心审计、修改器中心「可下载版本」的 Expanded/Standard 宽屏布局中确认检查器填满剩余行高、内部滚动条不出现空轨道；表格保持自己的内部滚动与虚拟化。
- [ ] 在 1024、850、700 DIP 宽度下确认三个检查器自动堆叠到列表下方，内层 ScrollViewer 出现受控滚动（约 180–420 DIP），长内容可完整滚动，不把页面推出窗口。
- [ ] 在维护中心设备状态页确认「人工决策/受保护远端恢复」仍是同一个 Inspector 滚动所有者内两段内容，页面没有第二个纵向滚动条。
- [ ] 在维护中心各表格确认首列与「建议处理」末列表头在浅色、深色、跟随 Playnite、高对比度下保持声明式主题，无白色表头回归，且不出现 VisualTreeHelper 周期扫描。
- [ ] 在 1600×900、1280×720、1100×700、980×640 与 125%/150% DPI 下重复上述检查；浅色/深色/高对比度/关闭透明/关闭动画均需可读可操作。
